# Quarantine

This subtree holds legacy PEM-era and SSH-era material that is intentionally
excluded from the active extracted payload.

The supported runtime payload is copied from `daylily_ec.resources.payload`
with this subtree removed at extraction time. Do not depend on any contents
here for supported operator flows.
