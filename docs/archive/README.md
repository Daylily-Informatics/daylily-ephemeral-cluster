# Archive

This directory holds historical or quarantined reference material that is not part of the supported operator path.

Current archive contents:

- `legacy-dayoa-env/`: retired environment-bootstrap assets preserved for historical inspection only

Use the top-level README and the docs in `../` for the supported current workflow.
