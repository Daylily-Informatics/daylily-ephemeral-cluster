# hyb-hg003 HG003 DayOA 1.0.18 Validation Ledger

Date opened: 2026-05-23T00:41:01Z

## Control

Controlling request: run independent HG003-a ILMN and ONT alignstats/SNV-concordance workflows on `hyb-hg003` using already staged data and DayOA `1.0.18`; after both independent workflows succeed, run the blessed ILMN+ONT `sentdhiom` hybrid SNV-concordance pipeline. Do not restage data, do not physically concatenate FASTQs, and do not export or clean up outputs.

Ledger path: `docs/plans/20260523T004101Z_hyb_hg003_hg003_dayoa_1018_validation_ledger.md`

## Fixed Settings

- Profile: `lsmc`
- Region: `us-west-2`
- Cluster: `hyb-hg003`
- Executing entity: `johnm`
- Project: `hyb-hg003`
- DayOA tag: `1.0.18`
- Genome build: `hg38_broad`
- Stage dir: `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z`
- Source samples TSV: `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z/20260522T203135Z_samples.tsv`
- Source units TSV: `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z/20260522T203135Z_units.tsv`

## Gate 0 Baseline

- Repo: `/Users/jmajor/.codex/worktrees/dyec-fsx-dra-mounts/daylily-ephemeral-cluster`
- Branch/status at open: `codex/analysis-id-export-catalog-validation...origin/codex/analysis-id-export-catalog-validation`; pre-existing dirty/untracked files remain out of scope.
- Cluster status: `CREATE_COMPLETE`; compute fleet `RUNNING`; headnode `i-03f1a49bbc4e39d4b` state `running`, type `r7i.4xlarge`, private IP `10.0.0.16`.
- Slurm baseline: `dyec headnode jobs --profile lsmc --region us-west-2 --cluster hyb-hg003` returned header only; no active jobs.
- Stage proof: `remote_stage_20260522T203135Z` exists on S3 and headnode-visible FSx, with 8 staged ILMN R1 FASTQs, 8 staged ILMN R2 FASTQs, staged `HG003_30x.cleaned.cram/.crai`, concordance data, and generated samples/units TSVs.
- Logical concatenation proof: generated `20260522T203135Z_units.tsv` has one `HG003-a` row, `LANEID=0`, 8 comma-separated `ILMN_R1_PATH` values, 8 comma-separated `ILMN_R2_PATH` values, and staged ONT CRAM metadata `ONT_CRAM_ALIGNER=ont`, `ONT_CRAM_SNV_CALLER=sentdont`.
- Capacity proof: `/fsx` on `hyb-hg003` reported about `8.7T` available at start.
- Historical risk: prior Altair hybrid failure on the older cluster was disk exhaustion in `sentdhiom_sr_align`, not evidence of comma-list FASTQ incompatibility.
- Safety boundary: no destructive AWS/resource action, no export, no cleanup, and no physical FASTQ concatenation are included.

## Tracking Rows

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| G0-001 | Baseline | Record repo, cluster, queue, staged-data, logical-concat, and capacity baseline before launching. | SUCCESS | contract_test | Gate 0 | orchestrator | Gate 0 Baseline. |  | Baseline captured before live workflow launches. |
| STAGE-001 | Inputs | Use existing staged HG003-a data at `remote_stage_20260522T203135Z`; do not restage or physically concatenate FASTQs. | SUCCESS | feature_implementation | Gate 0 | orchestrator | S3/headnode checks found the stage dir and generated TSVs; units TSV has one logical-concat row with 8 R1 and 8 R2 comma-separated staged FASTQ paths plus the staged ONT CRAM. |  | Existing staged data is the authoritative input. |
| DRY-ILMN-001 | ILMN dry-run | Dry-run ILMN `sent+dmd+sentd` alignstats/SNV-concordance on `hg38_broad`. | SUCCESS | contract_test | Gate 1 | orchestrator | Initial launch `hg003a_ilmn_snv_alignstats_1018_dryrun` reached `exit_code=1` at `2026-05-23T00:43:44Z`; `tmux.log` shows `Failed to create DAYOA environment` after pip failed preparing `chromedriver-binary-auto` because `/home/ubuntu/miniconda3/envs/DAYOA/lib/python3.11/site-packages/pip/_vendor/pyproject_hooks/_in_process/_in_process.py` was missing. Retry launch `hg003a_ilmn_snv_alignstats_1018_dryrun_retry1` created tmux/run dir after quarantining the partial env; status `exit_code=0`, completed `2026-05-23T00:51:44Z`; dry-run log shows `Job stats` total `36`, `This was a dry-run (flag -n)`, and `RETURN CODE: 0`; post-run `dyec headnode jobs` returned header only. |  | ILMN dry-run passed after serialized bootstrap repair. |
| DRY-ONT-001 | ONT dry-run | Dry-run ONT staged-CRAM `sentdont` alignstats/SNV-concordance on `hg38_broad`. | SUCCESS | contract_test | Gate 1 | orchestrator | Initial launch `hg003a_ont_snv_alignstats_1018_dryrun` reached `exit_code=1` at `2026-05-23T00:43:44Z`; `tmux.log` shows `Failed to create DAYOA environment` after conda failed linking `conda-forge::libegl-1.7.0` with `Cannot link a source that does not exist`. Retry launch `hg003a_ont_snv_alignstats_1018_dryrun_retry1` created tmux/run dir after the serialized ILMN retry rebuilt `DAYOA`; status `exit_code=0`, completed `2026-05-23T00:53:08Z`; dry-run log shows `Job stats` total `11`, `This was a dry-run (flag -n)`, and `RETURN CODE: 0`; post-run `dyec headnode jobs` returned header only. |  | ONT dry-run passed after serialized bootstrap repair. |
| BOOT-001 | Headnode bootstrap | Repair partial remote `DAYOA` conda environment before retrying dry-runs. | SUCCESS | plan_amendment | Gate 1 | orchestrator | Read-only SSM helper check: `DAYOA_DIR_EXISTS=yes`, size `496M`, not listed by `conda env list`, `python -V -> Python 3.11.15`, `python -m pip --version -> No module named pip.__main__`, `PIP_HOOK_EXISTS=no`. Verified no Slurm jobs, no matching workflow processes, and no tmux sessions. Quarantined the partial env to `/home/ubuntu/miniconda3/envs/DAYOA.failed-bootstrap-20260523T004950Z`; no staged data or analysis output was deleted. The serialized ILMN retry rebuilt `DAYOA` and reached dry-run `exit_code=0`. |  | Partial shared env was repaired by quarantine plus single-process rebuild. |
| RUN-ILMN-001 | ILMN real run | If ILMN dry-run passes, launch ILMN real workflow and monitor to terminal success/failure. | FAIL | feature_implementation | Gate 2 | orchestrator | Launched `hg003a_ilmn_snv_alignstats_1018`; run dir `/home/ubuntu/daylily-runs/hg003a_ilmn_snv_alignstats_1018`; repo `/fsx/analysis_results/johnm/hg003a_ilmn_snv_alignstats_1018/daylily-omics-analysis`. Status `exit_code=1`, completed `2026-05-23T00:56:53Z`; log shows `CreateCondaEnvironmentException` for `workflow/envs/alignstats_v0.2.yaml` and `CorruptedEnvironmentError` at `/fsx/resources/environments/conda/ubuntu/ip-10-0-0-16/21b4199862290439c3c25c57ddbd8c23_`, corrupted file `conda-meta/ca-certificates-2026.5.20-hbd8a1cb_0.json`; no ILMN Slurm jobs submitted. User requested ILMN restart. The incomplete cache path was 26K with no `conda-meta` and no referencing processes; quarantined to `/fsx/resources/environments/conda/ubuntu/ip-10-0-0-16/21b4199862290439c3c25c57ddbd8c23_.failed-20260523T012220Z`. Launched retry `hg003a_ilmn_snv_alignstats_1018_retry1`; run dir `/home/ubuntu/daylily-runs/hg003a_ilmn_snv_alignstats_1018_retry1`; repo `/fsx/analysis_results/johnm/hg003a_ilmn_snv_alignstats_1018_retry1/daylily-omics-analysis`. Retry passed env creation and submitted Slurm jobs. The actual ILMN `align/sent/dmd` path completed useful outputs: sample-level CRAM `20980201459` bytes, CRAI `2449542` bytes, sentd VCF `330459663` bytes, VCF index `1727513` bytes, `sent.dmd` alignstats JSON/TSV, `sent.na` alignstats JSON/TSV, and alignstats summary artifacts. Concordance prep emitted `concordance.done` plus `concordance.done.SKIPPED` with `No truthset ROI directories found; concordance skipped.` The retry terminalized `exit_code=1`, completed `2026-05-23T03:16:19Z`; no active Slurm jobs remained. | Original failure: shared Snakemake conda env cache contained an incomplete/corrupted `alignstats_v0.2` env, likely from concurrent env creation. Retry failure: the run also pursued an ONT-derived `align/ont/dmd` `sent_DNAscope` target; that target was killed near 95% on a regular 192-vCPU node with ~202 GB peak memory while rule request was `mem_mb=200000`, partition `i192,i192mem,i192bigmem`. | ILMN restart produced the main ILMN CRAM, sentd VCF, and alignstats, but the workflow is failed because of the ONT-derived `sent_DNAscope` kill and skipped concordance due no truthset ROI directories. |
| RUN-ONT-001 | ONT real run | If ONT dry-run passes, launch ONT real workflow and monitor to terminal success/failure. | FAIL | feature_implementation | Gate 2 | orchestrator | Launched `hg003a_ont_snv_alignstats_1018`; run dir `/home/ubuntu/daylily-runs/hg003a_ont_snv_alignstats_1018`; repo `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018/daylily-omics-analysis`. Status `exit_code=1`, completed `2026-05-23T01:22:03Z`. The staged ONT CRAM was detected as CRAM and symlinked; alignstats completed. The expanded workflow log for `sent_snv_ont` shows both attempts used `threads: 192` and `resources: mem_mb=300000 ... partition=i192mem,i192bigmem, vcpu=192`; `config/day_profiles/slurm/rule_config.yaml` has `sentdont: threads: 192`, `use_threads: 190`, `partition: "i192mem,i192bigmem"`, and `mem_mb: 300000`; rule log says `INSTANCE TYPE: r7i.metal-48xl`. `sent_snv_ont` first attempt progressed through DNAscopeONT phases but the Snakemake retry failed in `sent_snv_ont` with Sentieon `Return code 255` and `Failed to open output file /dev/shm/sentdont_tmp_20260523011953_19304/tmp5p7r3kwl/out_diploid_tmp.vcf.gz_214` and `_215`; no active ONT Slurm jobs remained after failure. | Sentieon DNAscopeONT temp output open failure under `/dev/shm` on a 192-vCPU memory node; not attributable to FSx capacity because `/fsx` still had `8.7T` available. | Independent ONT failed; hybrid gate remains closed. |
| ACCEPT-IND-001 | Independent acceptance | Verify independent ILMN and ONT output surfaces before hybrid launch. | BLOCKED | contract_test | Gate 3 | orchestrator | ILMN retry produced main ILMN CRAM/CRAI, sentd VCF/TBI, and alignstats outputs but terminalized with `exit_code=1`; ONT terminalized `exit_code=1`. | Independent workflows did not both succeed. | Do not launch hybrid from these runs. |
| DRY-HYB-001 | Hybrid dry-run | If both independent runs succeed, dry-run blessed ILMN+ONT `sentdhiom` hybrid pipeline. | BLOCKED | contract_test | Gate 4 | orchestrator | Not launched. | Independent ILMN and ONT gates failed. | Hybrid dry-run blocked. |
| RUN-HYB-001 | Hybrid real run | If hybrid dry-run passes, launch hybrid real workflow and monitor to terminal success/failure. | BLOCKED | feature_implementation | Gate 5 | orchestrator | Not launched. | Hybrid dry-run was blocked. | Hybrid real run blocked. |
| FINAL-001 | Closeout | Terminalize all rows and report whether objective is complete. | SUCCESS | contract_test | Gate 6 | orchestrator | Final status: all rows terminal; no active Slurm jobs; ILMN restart attempted and terminalized `exit_code=1`; ONT terminalized `exit_code=1`; hybrid not launched. | Independent gates failed. | Objective incomplete; remediation needed before hybrid. |

## Bigmem Rerun Amendment - 2026-05-23T03:30:59Z

User direction: rerun after forcing the failing high-memory ONT/Sentieon paths to `i192bigmem` only, with rule `mem_mb` set just below the advertised `i192bigmem` node memory. Monitor `/dev/shm` because the ONT failure opened temp outputs under `/dev/shm` and may be caused by tmpfs exhaustion or instance-level `/dev/shm` sizing.

Runtime config amendment scope: patch each launched DayOA clone before `bin/day_run`; this does not mutate the DayOA `1.0.18` tag or cluster configuration. The patched clone values are `sentdont.partition = "i192bigmem"`, `sentdont.mem_mb = 1450000`, `sentD.partition = "i192bigmem"`, and `sentD.mem_mb = 1450000`.

Bigmem baseline evidence: `scontrol show node i192bigmem-dy-all-1` reports `CPUTot=192` and `RealMemory=1494220`; `sinfo` reports 16 `i192bigmem` nodes in idle/powered-down states. `dyec headnode jobs --profile lsmc --region us-west-2 --cluster hyb-hg003` returned header only before this rerun.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| BIGMEM-G0-001 | Baseline | Record `i192bigmem` memory/partition baseline and no-active-job state before relaunch. | SUCCESS | plan_amendment | Gate 0 | orchestrator | `i192bigmem` has `CPUTot=192`, `RealMemory=1494220`; no active Slurm jobs before rerun. |  | Bigmem rerun baseline captured. |
| BIGMEM-DRY-ONT-001 | ONT dry-run | Dry-run ONT after patching `sentdont` and `sentD` to `partition=i192bigmem`, `mem_mb=1450000`. | SUCCESS | contract_test | Gate 1 | orchestrator | Launched `hg003a_ont_snv_alignstats_1018_bigmem_dryrun`; status `exit_code=0`, completed `2026-05-23T03:33:11Z`. Tmux log lines show `sentdont.partition=i192bigmem`, `sentdont.mem_mb=1450000`, `sentD.partition=i192bigmem`, and `sentD.mem_mb=1450000`; `rule sent_snv_ont` shows `threads: 192` and `resources: mem_mb=1450000 ... partition=i192bigmem, vcpu=192`. |  | Dry-run proves the failing ONT rule is forced to `i192bigmem` with the requested memory. |
| BIGMEM-RUN-ONT-001 | ONT real run | Launch ONT real rerun with patched bigmem-only config and monitor to terminal success/failure. | FAIL | feature_implementation | Gate 2 | orchestrator | Launched `hg003a_ont_snv_alignstats_1018_bigmem`; run dir `/home/ubuntu/daylily-runs/hg003a_ont_snv_alignstats_1018_bigmem`; repo `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_bigmem/daylily-omics-analysis`; status started `2026-05-23T03:37:32Z`, completed `2026-05-23T03:55:04Z`, `exit_code=1`. Patched clone config shows `sentdont 192 i192bigmem 1450000` and `sentD 192 i192bigmem 1450000`. Initial Slurm job `22` was lightweight `pre_prep_ont_cram` on `i192` with `MIN_MEMORY=20000M`. High-memory `sent_snv_ont` attempts ran as Slurm jobs `23` and `26` on `i192bigmem` with `CPUS=192`, `MIN_MEMORY=1450000M`, node `i192bigmem-dy-all-1`, instance type `r7i.48xlarge`. No active Slurm jobs remained after terminal failure. | `sent_snv_ont` failed twice on `i192bigmem`; the second attempt log shows `Error: DNAscope: Failed to open output file /dev/shm/sentdont_tmp_20260523035006_17231/tmpseo2e8kc/out_diploid_tmp.vcf.gz_1180`, Sentieon `Return code 255`, `FileNotFoundError` for the temp directory, and `sentieon-cli exit code: 1`. | Requested bigmem-only scheduling was applied, but ONT still failed. |
| BIGMEM-SHM-001 | `/dev/shm` monitoring | While a high-memory Sentieon ONT rule runs, capture compute-node `/dev/shm` size/used/inodes/tmpdir evidence. | SUCCESS | contract_test | Gate 2 | orchestrator | Job `23` ran `sent_snv_ont` on `i192bigmem-dy-all-1`; compute-node sample at `2026-05-23T03:48:30Z` showed `/dev/shm` as `745G` tmpfs with `214M` used and Sentieon temp dir `/dev/shm/sentdont_tmp_20260523034703_15047`. Sample at `2026-05-23T03:49:04Z` showed `310M` used and running `sentieon-cli dnascope-longread`. Job `23` failed and Snakemake retried as job `26`; sample at `2026-05-23T03:51:02Z` for job `26` showed `/dev/shm` `745G` total, `133M` used, `133M` Sentieon tmp dir, and the largest tmp file `17M`. The rule log then showed `Error: DNAscope: Failed to open output file /dev/shm/sentdont_tmp_20260523035006_17231/tmpseo2e8kc/out_diploid_tmp.vcf.gz_1180` and Sentieon `Return code 255`; after failure `/dev/shm` was empty. |  | Evidence captured; `/dev/shm` is about half of node memory, but observed usage was far below capacity at the sampled failure window. |
| BIGMEM-DRY-ILMN-001 | ILMN dry-run | Dry-run ILMN after patching `sentdont` and `sentD` to `partition=i192bigmem`, `mem_mb=1450000` if ONT rerun does not already clear independent-gate blockers. | NO_LONGER_NEEDED | contract_test | Gate 3 | orchestrator | ONT bigmem rerun terminalized `exit_code=1`; independent ONT gate remains failed. |  | Not launched because another ILMN dry-run cannot clear the current ONT blocker or open the hybrid gate. |
| BIGMEM-RUN-ILMN-001 | ILMN real run | Rerun ILMN with patched bigmem-only config if needed to obtain terminal workflow success. | NO_LONGER_NEEDED | feature_implementation | Gate 3 | orchestrator | ONT bigmem rerun terminalized `exit_code=1`; no active Slurm jobs remained. |  | Not launched because another ILMN run cannot clear the current ONT blocker or open the hybrid gate. |
| BIGMEM-FINAL-001 | Closeout | Terminalize new bigmem rows and report whether independent gates are restored. | SUCCESS | contract_test | Gate 4 | orchestrator | Bigmem amendment rows terminal: `SUCCESS` for baseline, dry-run, and `/dev/shm` evidence; `FAIL` for ONT real run; `NO_LONGER_NEEDED` for ILMN follow-on rows; no active Slurm jobs after `hg003a_ont_snv_alignstats_1018_bigmem`. | ONT `sent_snv_ont` still fails under bigmem-only scheduling. | Bigmem rerun objective complete; original independent ONT objective remains incomplete and hybrid remains blocked. |

## ONT Failure Debug And Lower-Coverage Rerun - 2026-05-23T07:03:45Z

User direction: explain the failing `DNAscope: Failed to open output file ... /dev/shm/.../out_diploid_tmp.vcf.gz_1180` path, confirm whether the ONT solo command is the blessed recipe, and try the same ONT solo pipeline on a lower-coverage HG003 dataset because this pipeline has worked in the past.

Debug interpretation: the failing file is not a final DayOA output path. DayOA rule `sent_snv_ont` sets `TMPDIR=/dev/shm/sentdont_tmp_$timestamp`, sets `SENTIEON_TMPDIR=$TMPDIR`, and runs `sentieon-cli dnascope-longread ... "$TMPDIR/{sample}.ont.sentdont.vcf.gz"`. Sentieon internally creates transient DNAscope shard outputs under a subdirectory such as `/dev/shm/sentdont_tmp_20260523035006_17231/tmpseo2e8kc/out_diploid_tmp.vcf.gz_1180`; that shard open fails before DayOA can reheader/copy the final VCF.

Blessed command confirmation: `dyec repositories commands --repository daylily-omics-analysis` reports command id `ont_snv_alignstats`, display name `ONT SNV + Alignstats`, targets `produce_alignstats`, `produce_sentdont_snv_vcf`, `produce_snv_concordances`, DayOA tag `1.0.18`, and `dy_command` exactly `bin/day_run produce_alignstats produce_sentdont_snv_vcf produce_snv_concordances -p -j 5 -k`. The failing runs used this target command; the bigmem rerun additionally patched only runtime resource keys in the cloned DayOA profile.

Lower-coverage staged input: `/fsx/staging/staged_sample_data/remote_stage_20260518T120758Z` exists and contains one ONT HG003 row: `RUNID=ONTGIAB3X`, `SAMPLEID=HG003`, `SEQ_PLATFORM=PROMETHION`, `ONT_CRAM=/fsx/control_data/genomic_data/organism_reads/H_sapiens/giab/agbt_2026/ont/HG003_3x.cleaned.cram`, `ONT_CRAM_ALIGNER=ont`, `ONT_CRAM_SNV_CALLER=sentdont`. CRAM size is `3157800957` bytes and CRAI size is `21478` bytes.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| DEBUG-ONT-001 | Failure interpretation | Identify what `/dev/shm/.../out_diploid_tmp.vcf.gz_1180` is and what layer produces it. | SUCCESS | contract_test | Gate 0 | orchestrator | Inspected `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_bigmem/daylily-omics-analysis/workflow/rules/sent_snv_ont.smk`; rule hard-codes `TMPDIR=/dev/shm/sentdont_tmp_$timestamp`, `SENTIEON_TMPDIR=$TMPDIR`, and sends `sentieon-cli dnascope-longread` output to `$TMPDIR/{sample}.ont.sentdont.vcf.gz`. |  | Failing path is an internal Sentieon DNAscope temporary shard, not the final DayOA VCF output. |
| DEBUG-BLESSED-001 | Blessed command | Confirm the ONT solo command being run is the catalog blessed recipe. | SUCCESS | contract_test | Gate 0 | orchestrator | `dyec repositories commands --repository daylily-omics-analysis` shows `ont_snv_alignstats` with `dy_command: "bin/day_run produce_alignstats produce_sentdont_snv_vcf produce_snv_concordances -p -j 5 -k"`, `git_tag: 1.0.18`, `compatible_data_modes: ont_solo`. |  | Current ONT command matches the blessed ONT solo recipe. |
| DEBUG-LOWCOV-G0-001 | Lower coverage input | Find staged lower-coverage HG003 ONT input without restaging. | SUCCESS | contract_test | Gate 0 | orchestrator | Found `/fsx/staging/staged_sample_data/remote_stage_20260518T120758Z`; units row has `RUNID=ONTGIAB3X`, `SAMPLEID=HG003`, `SEQ_PLATFORM=PROMETHION`, `ONT_CRAM=/fsx/control_data/genomic_data/organism_reads/H_sapiens/giab/agbt_2026/ont/HG003_3x.cleaned.cram`; CRAM `3157800957` bytes, CRAI `21478` bytes. |  | Use staged 3x ONT HG003 input for comparison. |
| DEBUG-LOWCOV-DRY-001 | Lower coverage dry-run | Dry-run blessed ONT solo command on 3x HG003 stage before live run. | SUCCESS | contract_test | Gate 1 | orchestrator | Launched `hg003_ont_3x_snv_alignstats_1018_dryrun`; status `exit_code=0`, completed `2026-05-23T07:05:06Z`. Dry-run used command `bin/day_run produce_alignstats produce_sentdont_snv_vcf produce_snv_concordances -p -j 5 -k -n`; job stats total `21`, with `sent_snv_ont` max threads `192`, and concordance ROI jobs present for `clinvar_genes`, `giabHC`, `giabHC_x_clinvar_genes`, `hg38`, and `hg38_m_giabHC`. |  | Lower-coverage dry-run passed. |
| DEBUG-LOWCOV-RUN-001 | Lower coverage real run | Run blessed ONT solo command on 3x HG003 stage and monitor to terminal success/failure. | SUCCESS | feature_implementation | Gate 2 | orchestrator | Launched `hg003_ont_3x_snv_alignstats_1018`; run dir `/home/ubuntu/daylily-runs/hg003_ont_3x_snv_alignstats_1018`; repo `/fsx/analysis_results/johnm/hg003_ont_3x_snv_alignstats_1018/daylily-omics-analysis`; started `2026-05-23T07:06:06Z`; completed `2026-05-23T07:31:03Z`; status `exit_code=0`. The first `sent_snv_ont` Slurm attempt retried, then the retry succeeded. Verification found no active Slurm jobs, Sentieon log `INSTANCE TYPE: m7i.metal-48xl`, `sentieon-cli exit code: 0`, `Elapsed-Time-min: m7i.metal-48xl 9`; outputs include SNV VCF `90755837` bytes, SNV TBI `1507622` bytes, SV VCF `996397` bytes, SV TBI `76698` bytes, alignstats JSON `5717` bytes, alignstats TSV `4248` bytes, `concordance.done`, and five ROI concordance MQC TSVs for `clinvar_genes`, `giabHC`, `giabHC_x_clinvar_genes`, `hg38`, and `hg38_m_giabHC`. |  | Lower-coverage blessed ONT solo workflow succeeded. |
| DEBUG-FINAL-001 | Closeout | Terminalize debug rows and report whether lower coverage reproduces the failure. | SUCCESS | contract_test | Gate 3 | orchestrator | Debug rows are terminal. Current 30x ONT runs failed in Sentieon DNAscope temporary shard creation under `/dev/shm`; bigmem-only scheduling did not clear the failure. The same blessed ONT solo command succeeded on the staged 3x HG003 ONT CRAM. | Failure is not explained by using the wrong ONT recipe and is not a general inability of the blessed ONT solo pipeline to run; evidence points to a 30x data-scale, Sentieon temporary-output, or high-thread concurrency failure mode. | Debug objective complete; hybrid remains blocked until the 30x ONT blocker is resolved or an explicitly accepted alternate input is selected. |

## Parallel Hybrid Sentieon Test Amendment - 2026-05-23T07:36:49Z

User direction: in parallel, fire off a test run of the complete hybrid ILMN+ONT Sentieon pipeline. This explicitly amends the earlier blocked hybrid gate for a separate test launch while preserving the prior independent-run failure evidence.

Hybrid test settings: profile `lsmc`, region `us-west-2`, cluster `hyb-hg003`, executing entity `johnm`, project `hyb-hg003`, DayOA tag `1.0.18`, genome `hg38_broad`, stage dir `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z`.

Blessed hybrid command confirmation: `dyec repositories commands --repository daylily-omics-analysis` reports command id `hybrid_ilmn_ont_snv`, display name `Hybrid ILMN+ONT SNV/SV`, targets `produce_snv_concordances`, `produce_sentdhiom_sv`, and `produce_sentdhiom_snv_vcf`, DayOA tag `1.0.18`, compatible mode `hybrid_ilmn_ont`, and `dy_command` exactly `bin/day_run produce_snv_concordances produce_sentdhiom_sv produce_sentdhiom_snv_vcf --config 'dedupers=["dmd"]' -p -j 100 -k`.

Hybrid test baseline: `git status --short --branch` reports branch `codex/analysis-id-export-catalog-validation...origin/codex/analysis-id-export-catalog-validation` with pre-existing untracked docs/plans/report/tmp artifacts; `dyec headnode jobs --profile lsmc --region us-west-2 --cluster hyb-hg003` returned header only before launch.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| HYBTEST-G0-001 | Baseline | Record explicit user override, command confirmation, stage settings, repo state, and no-active-job baseline before hybrid test launch. | SUCCESS | plan_amendment | Gate 0 | orchestrator | Hybrid test settings and baseline above. |  | Baseline captured before launching the parallel hybrid test. |
| HYBTEST-DRY-001 | Hybrid dry-run | Launch blessed hybrid ILMN+ONT Sentieon dry-run before the real test run. | SUCCESS | contract_test | Gate 1 | orchestrator | Launched `hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test_dryrun`; status `exit_code=0`, started `2026-05-23T07:37:41Z`, completed `2026-05-23T07:40:37Z`; command `bin/day_run produce_snv_concordances produce_sentdhiom_sv produce_sentdhiom_snv_vcf --config 'dedupers=["dmd"]' -p -j 100 -k -n`. |  | Hybrid dry-run passed. |
| HYBTEST-RUN-001 | Hybrid real test | If dry-run passes, launch complete hybrid ILMN+ONT Sentieon test run. | IN_PROGRESS | feature_implementation | Gate 2 | orchestrator | Launched `hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test`; run dir `/home/ubuntu/daylily-runs/hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test`; repo `/fsx/analysis_results/johnm/hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test/daylily-omics-analysis`; started `2026-05-23T07:41:37Z`; status `exit_code=null`. Latest check: Slurm job `45` is running `sentdhiom_sr_align` on `i192mem-dy-all-2` with `CPUS=192`, `MIN_MEMORY=300000M`, elapsed `16:20`; tmux log shows `Finished job 1`, `Finished job 0`, and `6 of 28 steps (21%) done`. |  | Hybrid real test is running in the high-thread `sentdhiom_sr_align` stage. |
| HYBTEST-FINAL-001 | Closeout | Report launched sessions, queue state, and terminal or current status. | SUCCESS | contract_test | Gate 3 | orchestrator | Hybrid dry-run succeeded and real hybrid test session was launched. Current status is intentionally non-terminal for `HYBTEST-RUN-001` because the user requested firing off the parallel test run, not waiting for full hybrid completion before reporting launch. |  | Fire-off objective complete; monitor `hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test` for terminal workflow status. |

Hybrid test input/rule clarification - 2026-05-23T08:08:00Z: live staged inputs are from `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z`, sample `HG003-a`, external sample `HG003`, male positive control, run `20260514-LH01106-0009-B23TVLGLT4`, experiment `20260514-ILMN-Altair-Run-3`, one logical `LANEID=0` row with 8 Illumina R1 FASTQs and 8 Illumina R2 FASTQs, plus ONT CRAM `/fsx/staging/staged_sample_data/remote_stage_20260522T203135Z/20260514-LH01106-0009-B23TVLGLT4_HG003-a-NOVASEQ-PF-gdna-20260514-ILMN-Altair-Run-3_HG003-a_0/HG003_30x.cleaned.cram`, `ONT_CRAM_ALIGNER=ont`, and `ONT_CRAM_SNV_CALLER=sentdont`. The current catalog command selected `produce_sentdhiom_snv_vcf` and `produce_sentdhiom_sv`; therefore the active rule family is `sentdhiom_*` and current Slurm job is `sentdhiom_sr_align`. The clone also contains refactored `sentdhiomr_*` rules and `produce_sentdhiomr_snv_vcf`, but those were not selected by the launched catalog command.

## Parallel hiomr 5x-To-Full Test Amendment - 2026-05-23T08:12:33Z

User direction: in a new agent, run a test via `hiomr`/`sentdhiomr` using 5x ILMN and 5x ONT source data; if that succeeds, check `/fsx` space and run the full ILMN+ONT dataset through `hiomr` only if there is sufficient space.

Delegated worker: `Hume`, agent id `019e53e4-3b07-7381-9c46-52f1fbff5c15`. Worker scope: identify existing staged 5x inputs, confirm exact refactored `sentdhiomr` command, dry-run, launch 5x real test if dry-run passes, then only launch full-data `hiomr` after 5x success and explicit `/fsx` capacity evidence. Worker must not perform destructive AWS actions or edit source.

Current concurrent load at amendment open: non-refactored hybrid test `hg003a_hybrid_ilmn_ont_sentdhiom_1018_parallel_test` is still running with `exit_code=null`; Slurm job `45` is `sentdhiom_sr_align` on `i192mem-dy-all-2`, `CPUS=192`, `MIN_MEMORY=300000M`, elapsed `22:19`.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| HIOMR-G0-001 | Baseline | Record new-agent delegation, active concurrent load, and non-destructive boundary for hiomr workstream. | SUCCESS | plan_amendment | Gate 0 | orchestrator | Amendment section above; worker `Hume` spawned with id `019e53e4-3b07-7381-9c46-52f1fbff5c15`. |  | hiomr workstream baseline captured. |
| HIOMR-5X-DRY-001 | 5x dry-run | Confirm 5x ILMN+ONT staged input and run refactored `sentdhiomr` dry-run. | SUCCESS | contract_test | Gate 1 | Hume | Session `hg003_hiomr_5x5x_1018_dryrun` completed `2026-05-23T08:20:19Z` with `exit_code=0`; command `bin/day_run produce_snv_concordances produce_sentdhiomr_sv produce_sentdhiomr_snv_vcf --config 'dedupers=["dmd"]' -p -j 100 -k -n`; dry-run job stats total `778` and include `sentdhiomr_sr_align`, `sentdhiomr_pass1`, `sentdhiomr_sr_markdup`, `sentdhiomr_stage1`, `sentdhiomr_stage2`, `sentdhiomr_stage3`, `sentdhiomr_pass2`, `sentdhiomr_transfer`, `sentdhiomr_transfer_merge`, `sentdhiomr_call_svs`, and concordance ROI jobs. |  | 5x hiomr dry-run passed. |
| HIOMR-5X-RUN-001 | 5x real test | If dry-run passes, launch 5x ILMN+ONT `hiomr`/`sentdhiomr` real test. | ATTEMPTING_BUGFIX | feature_implementation | Gate 2 | Hume | Session `hg003_hiomr_5x5x_1018` started `2026-05-23T08:21:20Z`; command `bin/day_run produce_snv_concordances produce_sentdhiomr_sv produce_sentdhiomr_snv_vcf --config 'dedupers=["dmd"]' -p -j 100 -k`; input stage `/fsx/staging/staged_sample_data/remote_stage_20260523T081915Z`, `RUNID=HIOGIAB5X5X`, `SAMPLEID=HG003`, `EXPERIMENTID=ILMN5x-ONT5x`, one ILMN R1 `/fsx/control_data/genomic_data/organism_reads/H_sapiens/giab/NovaSeqX_WHGS_TruSeqPF_HG002-007/downsampled/HG003_5x_R1.fastq.gz`, one ILMN R2 `/fsx/control_data/genomic_data/organism_reads/H_sapiens/giab/NovaSeqX_WHGS_TruSeqPF_HG002-007/downsampled/HG003_5x_R2.fastq.gz`, ONT CRAM `/fsx/control_data/genomic_data/organism_reads/H_sapiens/giab/agbt_2026/ont/HG003_5x.cleaned.cram`. Workflow status completed `2026-05-23T08:52:13Z` with `exit_code=1`; post-failure queue returned header only. Slurm stderr for `sentdhiomr_hybrid_select` jobs `400` and `610` shows `ModuleNotFoundError: No module named 'importlib_resources'` while resolving `hybrid_select.py`; per-rule logs contain only the start line and `selected.bed` outputs are missing for affected chromosomes. A stdlib-import clone patch was tested briefly, then rejected per user direction; session `hg003_hiomr_5x5x_1018_importlibfix` tmux was killed and Slurm job `187` was cancelled while still `CONFIGURING`. Correct fix path is env-level: `hg003_hiomr_5x5x_1018_envfix_dryrun` patched only `workflow/envs/sentieon_v0.3.yaml` to add `importlib_resources`, verified rule imports still use `importlib_resources`, and completed `exit_code=0` at `2026-05-23T09:46:13Z`. Real env-fix rerun `hg003_hiomr_5x5x_1018_envfix` started `2026-05-23T09:47:25Z`, currently `exit_code=null`; first queue check showed no Slurm jobs yet. | Refactored hybrid rule shells call `python -c "from importlib_resources import files; ..."` but `workflow/envs/sentieon_v0.3.yaml` omitted the `importlib_resources` dependency. |  |
| HIOMR-FULL-FSX-001 | Full-data gate | If 5x succeeds, check `/fsx` free space and decide whether full-data hiomr is allowed. | OPEN | contract_test | Gate 3 | Hume | Pending `hg003_hiomr_5x5x_1018_envfix` terminal result. |  |  |
| HIOMR-FULL-RUN-001 | Full real run | Launch full ILMN+ONT `hiomr`/`sentdhiomr` only if 5x succeeds and `/fsx` has sufficient space. | OPEN | feature_implementation | Gate 4 | Hume | Pending 5x success and FSx gate. |  |  |
| HIOMR-COMPARE-001 | Rule/output comparison | If 5x hiomr succeeds, compare legacy `hiom`/`sentdhiom` and refactored `hiomr`/`sentdhiomr` rule targets, Sentieon algorithm invocations, and required outputs. | IN_PROGRESS | contract_test | Gate 5 | orchestrator,Hume | Source comparison complete, runtime comparison pending env-fix 5x result. Legacy alias `produce_sentdhiom_snv_vcf` maps to `produce_sentdhiom_vcf`; refactored alias `produce_sentdhiomr_snv_vcf` maps to `produce_sentdhiomr_vcf`. Both invoke the same core Sentieon algorithms/commands for SNV/SV: `sentieon bwa mem`, `sentieon util sort`, `DNAscope`, `HybridStage1`, `HybridStage2`, `HybridStage3`, `DNAModelApply`, and `LongReadSV`. Analogous required outputs are legacy `.../snv/sentdhiom/{sample}.{alnr}.{ddup}.sentdhiom.snv.sort.vcf.gz(.tbi)` and `.../sv/sentdhiom/{sample}.{alnr}.{ddup}.sentdhiom.sv.vcf.gz(.tbi)` versus refactored `.../snv/sentdhiomr/{sample}.{alnr}.{ddup}.sentdhiomr.snv.sort.vcf.gz(.tbi)` and `.../sv/sentdhiomr/{sample}.{alnr}.{ddup}.sentdhiomr.sv.vcf.gz(.tbi)`. Rule-family count indicates `hiomr` is a strict superset at source level, adding `produce_sentdhiomr_cnv`, `produce_sentdhiomr_segdup`, `produce_sentdhiomr_mito`, `sentdhiomr_export_sr_cram`, and `sentdhiomr_merge_sr_bams`; launched command requests only SNV/SV/concordance. Speed evidence is mixed from source: same core algorithms/model/threads/resources, but `hiomr` has more scatter/parallel transfer/merge structure, including dry-run total `778` steps and `450` `sentdhiomr_transfer` jobs. |  | Pending runtime result before deprecation recommendation. |
| HIOMR-DEPRECATE-001 | Deprecation recommendation | If comparison supports equivalence or `hiomr` strict-superset behavior, recommend whether slower `hiom` should be deprecated from available hybrid workflows. | BLOCKED | active_product_contract | Gate 6 | orchestrator | Pending successful hiomr test and comparison evidence. | Product/catalog workflow availability change requires explicit follow-up approval and the 5x `hiomr` success trigger has not been satisfied. | Deprecation recommendation remains blocked. |
| HIOMR-FINAL-001 | Closeout | Reconcile worker evidence, sessions, status, and blockers. | OPEN | contract_test | Gate 5 | orchestrator | Pending worker report. |  |  |

## Conditional Hybrid Ancestor Deprecation Amendment - 2026-05-23T08:37:31Z

User direction: if `hiomr` succeeds, then for all hybrid pipelines with a `hiomr`-style refactored pattern, move the non-refactored `h[short+long][m]` ancestors of `h[short+long][m]r` into `workflow/rules/to_deprecate/`, flag them as imminently deprecated, remove them from `workflow/Snakefile` includes, and catalog the changes in a report.

Trigger gate: no source-file moves or include removals before a 5x `hiomr` run reaches `exit_code=0`. The first 5x run `hg003_hiomr_5x5x_1018` completed `exit_code=1` after `sentdhiomr_hybrid_select` failed to import `importlib_resources`; the deprecation trigger remains unsatisfied.

Read-only candidate inventory from the DayOA 1.0.18 run clone `/fsx/analysis_results/johnm/hg003_hiomr_5x5x_1018/daylily-omics-analysis`: current `workflow/Snakefile` includes both ancestor and refactored modular hybrid rule files. Matching refactored-pair candidates are:

- `workflow/rules/sent_hybrid_ilmn_ont_modular.smk` -> `workflow/rules/sent_hybrid_ilmn_ont_modular.refactored.smk`
- `workflow/rules/sent_hybrid_ilmn_pb_modular.smk` -> `workflow/rules/sent_hybrid_ilmn_pb_modular.refactored.smk`
- `workflow/rules/sent_hybrid_ug_ont_modular.smk` -> `workflow/rules/sent_hybrid_ug_ont_modular.refactored.smk`
- `workflow/rules/sent_hybrid_ug_pb_modular.smk` -> `workflow/rules/sent_hybrid_ug_pb_modular.refactored.smk`

Roche hybrid modular files were present as `sent_hybrid_roche_ont_modular.smk` and `sent_hybrid_roche_pb_modular.smk`, but no matching `.refactored.smk` pair was found in this clone, so they are excluded from the conditional `*r`-pattern deprecation set unless a later source checkout shows matching refactored files.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| HDEP-G0-001 | Baseline | Record conditional trigger and read-only inventory of ancestor/refactored hybrid rule pairs. | SUCCESS | plan_amendment | Gate 0 | orchestrator | Amendment section above; trigger requires `hg003_hiomr_5x5x_1018 exit_code=0` before source-file moves. |  | Deprecation workstream scoped but not yet allowed to mutate source. |
| HDEP-SOURCE-001 | Source checkout | Identify the authoritative editable DayOA source checkout for rule-file moves and report generation. | OPEN | contract_test | Gate 1 | orchestrator | Pending hiomr success and local source checkout selection. |  |  |
| HDEP-MOVE-001 | Rule move | Move non-refactored ancestor modular hybrid rule files with matching refactored `*r` successors into `workflow/rules/to_deprecate/` and add imminent-deprecation headers. | BLOCKED | active_product_contract | Gate 2 | orchestrator | Candidate files listed above. | Blocked until 5x hiomr success trigger is satisfied. |  |
| HDEP-SNAKEFILE-001 | Snakefile includes | Remove moved ancestor rule-file includes from `workflow/Snakefile` while preserving refactored includes. | BLOCKED | active_product_contract | Gate 2 | orchestrator | Current run clone includes ancestor and refactored modular hybrid files at `workflow/Snakefile` lines 319-328. | Blocked until 5x hiomr success trigger is satisfied. |  |
| HDEP-REPORT-001 | Deprecation report | Catalog moved files, removed includes, retained refactored replacements, excluded non-matching files, and validation evidence in a durable report. | BLOCKED | contract_test | Gate 3 | orchestrator | Report path to be selected in the authoritative DayOA source checkout once triggered. | Blocked until source changes are permitted. |  |
| HDEP-FINAL-001 | Closeout | Verify diffs/tests and report whether the conditional deprecation objective is complete. | OPEN | contract_test | Gate 4 | orchestrator | Pending trigger and implementation. |  |  |

## ONT Solo 191-Thread Tmp Preservation Debug Amendment - 2026-05-23T08:51:13Z

User direction: debug the ONT solo failure in a different agent; if a temp-preservation run has not already been done, rerun full-coverage HG003 ONT solo with `sentdont` restored to `partition=i192mem,i192bigmem`, requesting/using `191` threads, and temp preservation enabled. If it fails, inspect preserved temp files; because `/dev/shm` is compute-node-local and may be lost or overwritten after Slurm releases the node, preserve the failing temp evidence to FSx from inside the job before exit where possible.

Delegated worker: `James`, agent id `019e5407-b1d9-7da3-a522-e67d807a069d`.

Read-only baseline: no `/home/ubuntu/daylily-runs/*ont*preserve*`, `*sentdont*preserve*`, or `*191*` run dirs existed; no `/fsx/analysis_results/johnm/*ont*preserve*`, `*sentdont*preserve*`, or `*191*` analysis dirs existed. Prior rule inspection shows `sent_snv_ont.smk` sets `TMPDIR=/dev/shm/sentdont_tmp_$timestamp`, `SENTIEON_TMPDIR=$TMPDIR`, and currently has unconditional cleanup `trap 'rm -rf "$TMPDIR" 2>/dev/null || true' EXIT;`. FSx at baseline: `8.8T` total, `647G` used, `8.1T` available.

Preservation strategy: patch only the launched DayOA clone before `bin/day_run`; set `sentdont.threads=191`, `sentdont.use_threads=191`, `sentdont.partition="i192mem,i192bigmem"`, and patch the `sent_snv_ont` EXIT trap so the compute-node job writes `/dev/shm` `df`, inode, `du`, file inventory, and a `tar.gz` of `$TMPDIR` to `/fsx/analysis_results/johnm/<analysis_id>/ont_tmp_capture/<node>_<timestamp>_<pid>/` before returning the original exit code. This avoids relying solely on post-failure interactive SSH into a node that may be reused or terminated.

Corrected v2 failure update: session `hg003a_ont_snv_alignstats_1018_191_preservetmp_v2` reached `sent_snv_ont` and failed reproducibly on node `i192mem-dy-all-1`; `/dev/shm` was not full during the failure window (`378G` total, `0-51M` used, free inodes effectively all available, `733-740G` memory available). Slurm job `182` failed after `00:02:08`, Snakemake retried as job `185`, and the workflow completed `exit_code=1` at `2026-05-23T09:14:29Z`. Failure log shows `sentieon-cli dnascope-longread` called `sentieon driver --thread_count 191 ... /dev/shm/sentdont_tmp_20260523091219_15587/tmp4sjd77jh/out_diploid_tmp.vcf.gz`, then Sentieon failed with `Failed to create temporary directory /dev/shm/sentdont_tmp_20260523091219_15587/job15794: No such file or directory`. Preserved summaries exist at `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_191_preservetmp_v2/ont_tmp_capture/i192mem-dy-all-1_20260523T091158Z_11408/summary.txt` and `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_191_preservetmp_v2/ont_tmp_capture/i192mem-dy-all-1_20260523T091411Z_15587/summary.txt`; both show the top-level `$TMPDIR` was already absent at trap time, so no tarball was possible. Live node sampling also showed `sentieon-cli` and license server process arguments still pointed at `/dev/shm/sentdont_tmp_*` while `find /dev/shm -maxdepth 1 -name sentdont_tmp_*` returned no directory. Sentieon CLI package inspection found hidden `--retain_tmpdir` support in `sentieon_cli.pipeline.BasePipeline`; next diagnostic attempt will pass that flag to `sentieon-cli dnascope-longread`.

Cleanup hypothesis update: direct inspection of `workflow/rules/sent_snv_ont.smk` in the run clone found no `temp(...)` wrappers around the `sent_snv_ont` declared outputs; lines 48-56 are plain VCF/TBI paths. The original rule does contain an EXIT trap that deletes `$TMPDIR`, but in the v2 live sample the rule shell was still the parent of `sentieon-cli` while `$TMPDIR` was already absent, so the rule-level trap is not the primary premature deletion mechanism. The strongest suspect remains Sentieon CLI internal temp cleanup or a child-process mismatch around `SENTIEON_TMPDIR`; `sentieon_cli.util.tmp()` creates a subdirectory under `SENTIEON_TMPDIR`, and `sentieon_cli.pipeline.BasePipeline.main()` deletes that subdirectory unless `--retain_tmpdir` is set. A v3 diagnostic launch intended to add `--retain_tmpdir` was invalid: its Python patch failed to find the command needle, and because the wrapper shell lacked `set -e`, `bin/day_run` continued with only config changes. The invalid v3 session was killed and Slurm job `186` was canceled before `sent_snv_ont`; it is not valid failure evidence. Future diagnostic launch must set `set -euo pipefail` before patching so patch failure cannot silently continue.

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| ONTPRES-G0-001 | Baseline | Confirm no prior temp-preservation ONT solo rerun exists and record tmp preservation strategy. | SUCCESS | plan_amendment | Gate 0 | orchestrator,James | Amendment section above. |  | Preservation rerun is needed. |
| ONTPRES-DRY-001 | Dry-run | Launch full-coverage HG003 ONT solo dry-run with patched `sentdont` resources and tmp-preservation trap. | SUCCESS | contract_test | Gate 1 | orchestrator,James | Initial sessions `hg003a_ont_snv_alignstats_1018_191_preservetmp_dryrun` and `hg003a_ont_snv_alignstats_1018_191_preservetmp` exposed a launch-patch bug: the Python wrapper evaluated Snakemake `{log}` as a Python f-string placeholder (`NameError: name 'log' is not defined`), so config changed but trap preservation did not. Invalid real attempt was stopped before `sent_snv_ont`; tmux was killed and Slurm job `180` was canceled while still `pre_prep_ont_cram`. Corrected dry-run `hg003a_ont_snv_alignstats_1018_191_preservetmp_v2_dryrun` started `2026-05-23T09:01:03Z` and completed `2026-05-23T09:01:26Z` with `exit_code=0`; direct inspection of the launched clone confirms `sentdont.threads=191`, `sentdont.use_threads=191`, `sentdont.partition=i192mem,i192bigmem`, `sentdont.mem_mb=300000`, capture root `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_191_preservetmp_v2_dryrun/ont_tmp_capture`, escaped bash braces, and Snakemake `{log}` placeholders retained. |  | Corrected dry-run passed; the first launch attempt was invalid and did not reach the failing rule. |
| ONTPRES-RUN-001 | Real rerun | If dry-run passes, launch full-coverage HG003 ONT solo real rerun with patched resources and tmp-preservation trap. | ATTEMPTING_BUGFIX | feature_implementation | Gate 2 | orchestrator,James | Corrected session `hg003a_ont_snv_alignstats_1018_191_preservetmp_v2` launched `2026-05-23T09:04:56Z`; direct clone inspection confirmed `sentdont.threads=191`, `sentdont.use_threads=191`, `sentdont.partition=i192mem,i192bigmem`, `sentdont.mem_mb=300000`, and capture root `/fsx/analysis_results/johnm/hg003a_ont_snv_alignstats_1018_191_preservetmp_v2/ont_tmp_capture`; Slurm `sent_snv_ont` jobs `182` and retry `185` ran on `i192mem-dy-all-1` with `191` CPUs and `300000M`; workflow completed `exit_code=1` at `2026-05-23T09:14:29Z`. Sentieon CLI package inspection found hidden `--retain_tmpdir` in `sentieon_cli.pipeline.BasePipeline`; invalid real `hg003a_ont_snv_alignstats_1018_191_retain_tmp_v3` was canceled before `sent_snv_ont` after patch failure; corrected retain-temp v4 pending with hard-fail shell guard. | `sentieon-cli` or its child `sentieon driver` loses the `/dev/shm/sentdont_tmp_*` parent while the command is still running; not caused by `/dev/shm` capacity exhaustion. |  |
| ONTPRES-FAIL-001 | Failure evidence | If rerun fails, inspect preserved `/dev/shm` evidence from FSx capture and, if still available, compute node `/dev/shm` via `ssh <nodename>` from the headnode. | SUCCESS | contract_test | Gate 3 | orchestrator,James | v2 failure evidence captured in amendment above; FSx summaries at `.../ont_tmp_capture/i192mem-dy-all-1_20260523T091158Z_11408/summary.txt` and `.../ont_tmp_capture/i192mem-dy-all-1_20260523T091411Z_15587/summary.txt`; live `ssh i192mem-dy-all-1` samples from `2026-05-23T09:11:48Z` through `2026-05-23T09:16:50Z` showed `/dev/shm` `378G`, never above `51M` used, no `sentdont_tmp_*` dirs, no active Sentieon after terminal failure. |  | Failure evidence collected; no capacity exhaustion observed. |
| ONTPRES-FINAL-001 | Closeout | Report whether 191-thread both-partition preservation rerun succeeds; if it fails, summarize preserved tmp evidence and next fix target. | OPEN | contract_test | Gate 4 | orchestrator | Pending. |  |  |
