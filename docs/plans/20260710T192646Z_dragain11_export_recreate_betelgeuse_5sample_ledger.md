# DRAGEN Export, Recreate, And Betelgeuse Five-Sample Ledger

Created: `2026-07-10T19:26:46Z`

Controlling request: after the active HG003 native-DRAGEN concordance
controller completes, preserve and push DRAGEN-related source changes, verify
the private operational AMI remains account-private, export all required
`dragain11` FSx results to S3, delete `dragain11`, create a replacement private
mixed DRAGEN/CPU cluster, mount the Betelgeuse ILMN prefix, and run `HG002`,
`HG003`, `NA19235`, `NA20775`, and `NA23687` through native DRAGEN, short-read
variant calling, SNV concordance, SMN callers, ExpansionHunter, routine QC,
reporting, and final S3 export. The user corrected the earlier ONT instruction:
this cluster/run is ILMN-only.

## Gate 0

| Surface | Evidence |
|---|---|
| Current cluster | `dragain11`, `us-west-2`, `CREATE_COMPLETE`, compute fleet `RUNNING`, headnode `i-0684060a8c71c339a`, FSx `fs-09ff0f6f0412bc7d9`. |
| Active controller | Terminal success. `dragen_hg003_conc_dragain11_20260710` completed, native DRAGEN job `7` and RTG concordance completed, and the Slurm queue drained. |
| HG003 native outputs | Non-empty 72,469,974,208-byte BAM, 451,596,448-byte SNV VCF, 2,004,111,962-byte gVCF, indexes, native validation JSON, and `giab_concordance_mqc.tsv`; no partial files. Primary GIAB HC `All` F-score: `0.999079967406051`; SNP transition/transversion F-scores: `0.9992405988623179` / `0.9989431693637525`. |
| Prior native proof | HG002 native SNV completed under DayOA `10.0.83`; license/FPGA/memlock validation is terminal success. |
| Current mounts | Reference DRA and ILMN BCLConvert FASTQ DRA are available; no ONT DRA is attached to `dragain11`. |
| Private image | Child `ami-046ab49ce477823fe` is owned by account `108782052779`, `Public=false`, and has no launch permissions. Snapshot `snap-0c4395d45b0d99d73` has no create-volume permissions. |
| Cost center | `dragen-followup-20260710`, active, cap `$250`; current CUR snapshot reports `$0` through `2026-07-10T15:00:00Z` and is not real-time. |
| Sample set | `HG002`, `HG003`, `NA19235`, `NA20775`, `NA23687`. Truth expectations for SMN1/SMN2 are `2/2`, `2/2`, `4/0`, `3/1`, `1/2`. |
| ILMN source | `s3://lsmc-ssf-sequencing-data/basecalls/lsmc/ssf-hq/LH01106/2026/20260618_LH01106_0011_A23MFMCLT3/Analysis/1/Data/BCLConvert/fastq/`; all five samples have eight R1 and eight R2 lane FASTQs. |
| ONT scope | Excluded by the user's corrected instruction. No ONT path appears in the active manifests or DRAGEN graph. Previously created ONT DRAs were not detached or deleted without separate destructive approval. |
| Git boundary | Public AlmaLinux branches are clean. DayOA parse/concordance integration PR `#9` merged into `jem-dev` at `8855549`; native preflight/final-directory fix PR `#10` merged at `4c5933a`; native DRAGEN evidence routing PR `#11` merged at `690ab4f`. DYEC mixed-queue PR `#7`, replacement-cluster PR `#8`, and memlock bootstrap PR `#9` are already merged into `jem-dev`. |
| Destructive gate | User explicitly approved deletion of `dragain11` in `us-west-2` after successful exports. Both required exports are now independently verified, so live deletion is authorized after the mixed-queue PR is published and the final empty-queue/dry-run checks pass. |

## Control Ledger

| ID | Requirement | Status | Evidence / Next Gate |
|---|---|---|---|
| CUR-HG003-001 | Finish HG003 native SNV concordance and verify aggregate metrics. | SUCCESS | Controller completed, queue drained, `giab_concordance_mqc.tsv` exists, primary GIAB HC `All` F-score is `0.999079967406051`, and SNP transition/transversion F-scores are `0.9992405988623179` / `0.9989431693637525`. |
| GIT-DAYOA-001 | Commit and push all DRAGEN-related DayOA changes without unrelated dirty work. | SUCCESS | Native DRAGEN/memlock release `d831c5b` is included in `origin/jem-dev` commit `90adbd5`, annotated tag `10.0.84`. The gated native-BAM route for short-read SMN callers and ExpansionHunter merged through PR `#11` at `690ab4f`; no unrelated files, release tag, or DYEC pin were included. |
| GIT-DYEC-001 | Commit and push all DRAGEN-related DYEC code, config, tests, manifests, and ledgers without unrelated dirty work. | SUCCESS | Operational releases are pushed through `10.0.149`; mixed `dragen`/CPU partition support merged by PR `#7` at `fc1978ae`. Replacement-cluster config, ledger evidence, and the lock-initialized `day-clone` fix were committed as `62b3218e`, passed all three CodeQL checks, and merged normally by PR `#8` into `jem-dev` at `eedd4d8b`. Branches were not deleted. |
| AMI-PRIVATE-001 | Keep private child AMI and snapshot LSMC-account-only. | SUCCESS | Image and snapshot have no public or cross-account permissions. |
| EXPORT-OLD-001 | Export every required completed `dragain11` analysis root to a new empty S3 destination and verify receipts. | SUCCESS | HG002 task `task-044d48709a8970750` and HG003 task `task-0036549a5bab2a9f5` both `SUCCEEDED`; both temporary DRAs detached with lifecycle `DELETED`; both receipts record `delete_data_in_file_system: false`. S3 inventories: HG002 `1924` objects / `14239363777` bytes; HG003 `2481` objects / `78461853721` bytes. |
| DELETE-OLD-001 | Delete `dragain11` only after export verification and second approval. | SUCCESS | Final queue was empty; delete dry-run passed; exact approval was applied. DYEC removed heartbeat resources, ParallelCluster reached terminal deletion, and FSx `fs-09ff0f6f0412bc7d9` no longer exists. Exported/source S3 prefixes were not deleted. |
| MIXED-TEMPLATE-001 | Add a private EFA-disabled mixed template with `dragen`, `i192`, and `i192nvme` partitions. | SUCCESS | Source and packaged templates contain exact queues `dragen`, `i192`, and `i192nvme`; CPU bootstrap skips DRAGEN device validation while DRAGEN nodes remain fail-hard. Focused tests: `5 passed`; broader workflow/default/headnode/pricing tests: `162 passed`; Ruff, Bash syntax, YAML parse, and `git diff --check` passed. PR `#7` merged normally after all CodeQL checks passed. |
| CREATE-NEW-001 | Create replacement cluster in `us-west-2b` under the active `$250` cost center. | SUCCESS | `dragain12` reached `CREATE_COMPLETE` in 24m44s. Headnode `i-0222f73126d42ef9` (`10.0.1.84`) uses `r7i.2xlarge`; the mixed template has one-node maxima for `dragen`, `i192`, and `i192nvme`. Preflight passed 14 checks, ParallelCluster dry-run passed, and private AMI/secret/reference/budget gates passed. |
| SOURCE-BUNDLE-001 | Seed exact private DYEC and DayOA releases without placing personal GitHub credentials on the cluster. | SUCCESS | AES256-encrypted immutable S3 bundles were verified by exact byte size and SHA-256, then cloned on the headnode at DYEC `10.0.149` (`08f21590`) and DayOA `10.0.84` (`90adbd5`). The local-only Git URL rewrite points DayOA clone requests to the exact seeded repository. No personal SSH key or PAT was copied. |
| DRAGEN-SMOKE-001 | Prove native DRAGEN device and license operation on one replacement-cluster f2 node. | SUCCESS | Slurm job `1` ran alone on partition `dragen`; native DRAGEN `13.031.818.4.5.4` initialized and the supported `dragen_lic` query exited `0`. Genome entitlement is active, with `4.6 Tbases` remaining of `5.0 Tbases`, expiry `2026-11-01`. Credential contents were not printed. |
| DRAGEN-MEMLOCK-001 | Ensure replacement-cluster login and Slurm jobs can raise native DRAGEN memlock to unlimited. | SUCCESS | The first controller attempts stopped before `dragen --version`; exact probes showed finite memlock inherited from `/etc/security/limits.d/99-edico.conf`, not a license or FPGA failure. DYEC bootstrap now fails hard unless the vendor entry exists and rewrites it to `unlimited`; source/package Bash syntax, 37 focused tests, Ruff, and diff checks passed. Exact private boot script SHA-256 `bd4bd325a9cd434c56ef97e0e166144f265640d05eec57350d1da63c25876694` was published AES256-encrypted. Fresh node `i-072b61f87beaebf23` applied it during boot; Slurm probe job `7` reported unlimited soft/hard memlock and DRAGEN `13.031.818.4.5.4`. |
| MOUNT-ILMN-001 | Mount and verify the exact ILMN BCLConvert FASTQ prefix read-only. | SUCCESS | DRA `dra-0308b51ae189f35a1` is `AVAILABLE`; `dyec mounts verify` passed. Mounted counts/bytes exactly equal S3: HG002 `8/37003624993`, `8/37335094934`; HG003 `8/38635607903`, `8/38970470569`; NA19235 `8/31787165597`, `8/32061482369`; NA20775 `8/37597192947`, `8/37964051254`; NA23687 `8/36627909063`, `8/36845312328`. |
| MOUNT-ONT-001 | Mount and verify all six exact ONT Set3/Set4 flow-cell prefixes read-only. | SKIPPED | User corrected the scope: DRAGEN here is ILMN-only and ONT is not needed. All local wait controllers were canceled. Existing AWS associations were not detached or deleted because no separate destructive approval was given; they are excluded from manifests and execution. |
| CLONE-LOCK-001 | Allow `day-clone` to populate a newly lock-initialized analysis root without weakening existing-destination protection. | SUCCESS | `day-clone` now accepts only an existing root whose sole entry is non-symlink `.dayoa_agent` with a non-symlink `write.lock` directory; any analysis data still fails hard. Source/package copies match; 42 focused tests, Ruff, Python compile, and `git diff --check` passed. |
| MANIFEST-001 | Rebuild exact five-sample ILMN-only manifests. | SUCCESS | Run-specific builder requires exact eight-lane R1/R2 counts and byte totals, writes ONT/PacBio/Ultima fields as `na`, and emits no ONT path. It creates a fail-hard analysis-local truth view using HG002 GIAB v5.0q and HG003 GIAB v4.2.1 files from `/fsx/references`; the source helper and headnode copy match SHA-256 `1f95ee0c52c46dddbc3f74f9b19df3cef40edeff8fd75b357a99baa2076b4c9e`. |
| DAYOA-ACTIVATE-001 | Initialize the pinned DayOA checkout and explicit cluster/runtime/cost-center context. | SUCCESS | DayOA `10.0.84` cloned at `90adbd5` under the owned analysis lock. `source dyoainit` built DAYOA, and `dy-a slurm_rhel hg38` succeeded. `slurm_rhel` is the current DayOA RHEL-family profile name and no AlmaLinux alias exists. The profile submits `--comment "$DAY_PROJECT"`; the pane explicitly exports `DAY_PROJECT=dragen-followup-20260710`, independently of the legacy DayOA project allow-list. No sample jobs launched during activation. |
| REFERENCE-001 | Verify mounted ILMN DRAGEN and GIAB truth resources through `/fsx`. | SUCCESS | Native reference `/fsx/references/runtime_assets/tool_specific_resources/dragen/ilmn/hg38-alt_masked.graph.cnv.hla.methyl_cg.rna_v6` exposes non-empty hash table, graph, CNV, repeat, and reference assets. HG002 GIAB v5.0q and HG003 GIAB v4.2.1 VCF/TBI/BED sources are non-empty. The imported HG002 v4.2.1 `giabHCv5q` aliases point to obsolete `/fsx/data`, so the run uses explicit analysis-local links to the valid `/fsx/references` sources without modifying shared references. |
| DRYRUN-001 | Dry-run the native DRAGEN/concordance target set and inventory jobs and unsupported rules. | SUCCESS | Final `dy-r ... -n -j 1` returned `0` and planned 24 jobs: exactly five `dragen_all_callers`, two GIAB `rtg_vcfeval_roi`, and supporting local/staging rules. It planned zero `dragen_aln_sort_snv`, ONT, hybrid, or Sentieon compute jobs. The active concordance rule matches source SHA-256 `a5fe97f54a250575fd6e14ab804f3be6e4652ac52467984ca49af36bc07963cd`; `dragen_pangenome.concordance_source=all_callers`. |
| RUN-DRAGEN-001 | Run native DRAGEN all-callers including SNV/gVCF/CNV/SV/targeted/SMN/HLA/repeat/MRJD. | SUCCESS | All five jobs exited `0`, published non-empty all-caller validation markers, and left no partial output. Job/task costs and wall times: HG002 `$1.009605` / `01:25:38`; HG003 `$1.047606` / `01:28:52`; NA19235 `$0.955035` / `01:21:00`; NA20775 `$1.020961` / `01:26:35`; NA23687 `$0.973769` / `01:22:35`. Total recorded f2 task cost is `$5.006976`, excluding headnode, FSx, and other infrastructure. Native SMN1/SMN2 calls exactly match expectations: HG002 `2/2`, HG003 `2/2`, NA19235 `4/0`, NA20775 `3/1`, NA23687 `1/2`. All three concurrent jobs independently connected to the license service, acquired FPGA board `-Z0`, and completed. Active source SHA-256 is `06266cc4ed09a0a1197c650cf4146764c3551b93d2cb873f0adff2012190ab0a`. |
| DRAGEN-PARALLEL-001 | Raise the DRAGEN queue capacity and run the three remaining samples concurrently without interrupting completed or active work. | SUCCESS | The serial `-j 1` controller was suspended while NA20775 job `16` finished, then terminated only after its validation marker appeared and the queue drained; its analysis lock was released. ParallelCluster's authoritative config was updated only at `Scheduling.SlurmQueues[dragen].ComputeResources[f26xlarge].MaxCount`, from `1` to `3`; stack update reached `UPDATE_COMPLETE` at `2026-07-11T00:43:55Z`, and Slurm exposes `dragen-dy-f26xlarge-[1-3]`. A fresh `dy-r ... -n -j 3` returned `0` and planned exactly three `dragen_all_callers` plus two GIAB RTG jobs. The live `dy-r ... -j 3` controller then placed HG002, HG003, and NA19235 on nodes 3, 2, and 1 respectively. Simultaneous successful license connections disprove the one-analysis-only license hypothesis for this three-job workload. |
| RUN-HYBRID-001 | Run hybrid Sentieon SNV/SV/CNV/mito/SMN1 segdup outputs. | SKIPPED | Explicitly removed from this cluster/run when the user corrected the ONT instruction. |
| RUN-SMN-001 | Run short-read SMNCopyNumberCaller plus supported orthogonal SMN callers and aggregate evidence. | IN_PROGRESS | Native DRAGEN SMN is complete for all five samples. The first `dy-r` dry-run for `smn12`, `smaca`, and `sma_finder` stopped fail-hard because DayOA did not recognize the validated native all-caller `drbwa/na` BAM/BAI as downstream evidence. The explicit gated route is now merged into DayOA `jem-dev` at `690ab4f`; it still must be deployed and dry-run on the active analysis before live caller jobs. Unsupported Parascopy remains excluded unless validated resources exist. |
| RUN-QC-001 | Run ExpansionHunter, concordance, routine QC/reporting, and evidence manifest. | IN_PROGRESS | Native SNV concordance is complete. HG002 primary GIAB HC `All` F-score is `0.9930599247270807`; HG003 is `0.999079967406051`. ExpansionHunter shared the same native-BAM routing blocker now addressed by DayOA `690ab4f`; active-checkout deployment, dry-run, live execution, MultiQC, and evidence-manifest generation remain pending. |
| EXPORT-NEW-001 | Export completed replacement-cluster analysis to S3 and verify receipt/inventory. | PENDING | No cleanup until export task `SUCCEEDED`, DRA detached, and S3 inventory is non-empty. |
| FINAL-001 | Terminalize every row and report exact completed outputs, tags, costs, exports, and remaining blockers. | PENDING | Objective is not complete while any required run/export row is non-terminal. |

## Safety And Scope Contracts

- Never print credential contents or credential-bearing URLs.
- Never invoke raw Snakemake; use `dy-r` in persistent interactive `ubuntu`
  tmux sessions with explicit DayOA tags and analysis-root locks.
- Do not stage or commit unrelated dirty files from shared worktrees.
- Do not infer or silently substitute missing sample, mount, truth, reference,
  caller-resource, partition, or export paths.
- Do not delete FSx, S3 objects, mounts, AMIs, snapshots, or clusters without
  the required destructive-action approval.
- The `$250` cost-center cap is a hard launch boundary. CUR usage lag is recorded
  explicitly and is not treated as real-time zero cost.
