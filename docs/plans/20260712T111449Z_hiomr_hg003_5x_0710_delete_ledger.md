# `hiomr-hg003-5x-0710` Destructive Deletion Ledger

Controlling request: delete AWS ParallelCluster `hiomr-hg003-5x-0710` in
`us-west-2` after exact destructive-scope review and explicit approval.

Ledger path: `docs/plans/20260712T111449Z_hiomr_hg003_5x_0710_delete_ledger.md`

## Approval Record

1. Initial delete request: user requested `delete cluster hiomr-hg003-5x-0710`.
2. Destructive warning: agent stated that deletion requires a second explicit
   approval and began enumerating stack-owned resources.
3. Second request: user repeated `delete cluster hiomr-hg003-5x-0710`.
4. Material data-loss discovery after the second request: stack-owned FSx
   `fs-00de92c17ff025ed1` is a 12,000 GiB `SCRATCH_2` filesystem with
   `DeletionPolicy: Delete`, no backups, and no analysis-output auto-export.
5. Final acknowledgement received at `2026-07-12T11:24:35Z`:
   `Confirm delete hiomr-hg003-5x-0710 including fs-00de92c17ff025ed1 and all unexported /fsx data.`

## Exact Destructive Scope

- Cluster/stack: `hiomr-hg003-5x-0710`, ParallelCluster 3.15.0,
  CloudFormation `CREATE_COMPLETE`.
- Headnode: `i-0c81509251a1f0adb`, `r7i.2xlarge`, running in `us-west-2d`.
- Headnode EIP: `44.240.207.113`; deletion releases the stack-owned address.
- FSx: `fs-00de92c17ff025ed1`, 12,000 GiB Lustre `SCRATCH_2`, mounted at `/fsx`.
- FSx template/config policy: `DeletionPolicy: Delete` and
  `UpdateReplacePolicy: Delete`.
- Backups: none.
- DRAs: six available associations for Set4 FC1/FC2/FC3, the LH01106 Illumina
  run, staged external sequencing data, and the DayOA references bucket.
- DRA effect: associations and imported FSx metadata/cache are deleted; source
  S3 objects remain. No DRA has an analysis-output `AutoExportPolicy`.
- Other stack-owned resources include the private Route 53 hosted zone,
  ParallelCluster and Slurm DynamoDB tables, IAM roles/policies, log groups,
  Lambda cleanup resources, launch templates, security groups, and EBS-backed
  headnode storage.
- Idle proof at approximately 2026-07-12 11:10 UTC: no `dy-r`, `day_run`, or
  Snakemake process; empty Slurm `squeue`; four detached tmux shell/lock sessions
  with no non-shell pane command.
- ParallelCluster deletion was submitted after final approval and reached
  terminal success at `2026-07-12T11:42:33Z`.

## Control Ledger

| ID | Requirement | Status | Category | Approval Gate | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|
| INV-001 | Enumerate cluster, stack, headnode, EIP, FSx, DRA, backup, and deletion-policy scope | SUCCESS | legitimate_safety_handling | Gate 0 | Exact destructive scope above |  | Resource manifest complete. |
| IDLE-001 | Verify no controller or Slurm job is active | SUCCESS | legitimate_safety_handling | Gate 0 | Live headnode process and `squeue` checks |  | Cluster is idle; stale shell sessions do not execute workflow work. |
| ACK-001 | Obtain explicit acknowledgement that cluster deletion destroys the 12 TiB Scratch2 FSx and all unexported `/fsx` data | SUCCESS | active_product_contract | Destructive data-loss gate | Exact confirmation received at `2026-07-12T11:24:35Z` |  | User explicitly approved deletion of the named cluster, FSx filesystem, and all unexported `/fsx` data. |
| DEL-001 | Run `pcluster delete-cluster` for exactly `hiomr-hg003-5x-0710` and wait for terminal deletion | SUCCESS | feature_implementation | Destructive data-loss gate | `pcluster delete-cluster` returned `DELETE_IN_PROGRESS`; CloudFormation waiter exited `0`; terminal checks completed at `2026-07-12T11:42:33Z` |  | Cluster absent from ParallelCluster; stack absent; headnode terminated; FSx absent; EIP released; DRA lookup empty; hosted zone absent. |

## Approved Confirmation

`Confirm delete hiomr-hg003-5x-0710 including fs-00de92c17ff025ed1 and all unexported /fsx data.`

## Terminal Verification

- ParallelCluster inventory no longer includes `hiomr-hg003-5x-0710`.
- CloudFormation reports that stack `hiomr-hg003-5x-0710` does not exist.
- Headnode `i-0c81509251a1f0adb` is `terminated`.
- FSx reports `FileSystemNotFound` for `fs-00de92c17ff025ed1`.
- EC2 reports `InvalidAddress.NotFound` for former EIP `44.240.207.113`.
- DRA lookup for the deleted filesystem returns an empty association list.
- Route 53 reports `NoSuchHostedZone` for `Z03312023UFUHDF37O0SY`.
- `ifx-reworkB` and `partrevert` remain `CREATE_COMPLETE`; no command in this
  deletion targeted either cluster or its Slurm jobs.
- CloudFormation intentionally retained log group
  `/aws/parallelcluster/hiomr-hg003-5x-0710-202607110723` with
  `DELETE_SKIPPED`; it is not an active cluster resource and was not separately
  deleted.
