# DayOA 11.0.8 and DYEC 10.3.14 Pin Ledger

- Created: 2026-07-15T22:28:27Z
- Branch: `sentieon-single`
- DayOA release: `11.0.8` at `d13527885e2360c1a9c6bed9095ca8ebfefb3348`
- DYEC release target: `10.3.14`
- Live rollout target: `ifx-p2-1000-120-0715`

## Scope

1. Make `day-clone` resolve DayOA `11.0.8` by default.
2. Make both HIOMRS catalog commands launch exact tag `11.0.8`.
3. Preserve `validated_version: 11.0.7` until a terminal live `11.0.8` kitchen-sink run proves the new tag; do not falsely promote validation metadata.
4. Keep source and packaged catalogs byte-identical.
5. Self-pin the released DYEC payload to annotated tag `10.3.14`.
6. Configure the newest cluster after release without modifying its active DayOA `11.0.7` controller.

## Ledger

| ID | Item | State | Evidence |
|---|---|---|---|
| DPIN-001 | Verify DayOA `11.0.8` remote tag | SUCCESS | Remote peeled tag resolves to `d13527885e2360c1a9c6bed9095ca8ebfefb3348`. |
| DPIN-002 | Update source and packaged DayOA defaults/catalog commands | SUCCESS | Default and launch tag are `11.0.8`; validation metadata remains `11.0.7`; source and packaged catalogs are byte-identical. |
| DPIN-003 | Update source and packaged DYEC self-pin | SUCCESS | Source and packaged self-pins both target exact release `10.3.14`. |
| DPIN-004 | Run DYEC regression tests | SUCCESS | Activated `DAY-EC` environment: `1542 passed, 11 skipped, 1 warning in 61.38s`; targeted catalog tests: `44 passed`. |
| DPIN-005 | Commit, tag, push, and verify DYEC | SUCCESS | Release commit `dcf5904b432518641dda47f0baba09dc11fc576a`; annotated `10.3.14` tag object and peeled commit verified remotely; `sentieon-single` matched at release time. |
| DPIN-006 | Configure and verify newest cluster default | SUCCESS | Headnode `i-09b566e847c16233b` is detached cleanly at exact DYEC tag `10.3.14`; installed version is `10.3.14`; `day-clone --list` reports DayOA `11.0.8`; `day-clone --check-auth` validates the exact default ref. |

## Terminal disposition

All rows are terminal and the default-update objective is complete. The active DayOA `11.0.7` controller was not restarted or modified. At final inspection it remained at 69/174 with Slurm job 32 running the HIOMRS core group; the current rule log was advancing through transfer-shard concatenation.
