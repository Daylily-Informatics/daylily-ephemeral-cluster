# DYEC TST R5 Headnode and DayOA Smoke Diagnostic Ledger

Created: `2026-07-16T16:18:37Z`

## Control Ledger

Controlling request: diagnose the DayOA `mmdc` WebSocket timeout on `dyec-tst-r5`; verify Slurm accounting and login-shell DAY-EC activation; verify an explicitly version-pinned `day-clone -d test`; then run the supported DayOA initialization, requested build check, local activation, and `dy-r help` smoke sequence.

Ledger path: `docs/plans/20260716T161837Z_dyec_tst_r5_headnode_dayoa_smoke_diagnostic_ledger.md`

Gate 0 baseline:

- DYEC repo: `main` at `7dbb9cb5b1f4e02b04fbbeff6cb9aa0c172710a7`, exact tag and activated package `10.3.26`; existing unrelated/unmodified untracked prior-run ledger `docs/plans/20260716T153510Z_dyec_tst_r5_remaining10_hiomrs_kitchensink_ledger.md`.
- DayOA repo: clean `main` at `ef279ccafa70dd6244c6feac39340cc34f4a80f3`, exact and current maximum remote tag `11.0.15`.
- Live target: AWS profile `lsmc`, region `us-west-2`, cluster `dyec-tst-r5`, headnode instance previously verified as `i-09e68735609973936`.
- Workflow contract: SSM only; `ubuntu`; persistent one-pane `bash -il` tmux; separate `source dyoainit`, `dy-a`, and `dy-r` commands; never raw Snakemake.
- Analysis-root contract: record visits before touching `/fsx/analysis_results/**`; acquire the exact-root write lock for clone/build writes; do not take over another owner.
- Explicit ref: test clone will use `day-clone -t 11.0.15 -d test`; an unpinned clone is forbidden by the current DayOA operating contract.
- Safety boundary: the user explicitly approved stopping/restoring the `dyec-tst-r5` compute fleet and the disclosed ongoing-cost PrivateLink design. Scope is limited to a private TCP/3306 bridge from the cluster VPC to the existing accounting service, dry-run-validated ParallelCluster attachment, restoration, `sacct` verification, and the pinned clone test. The existing database host and `ifx-p2-1000-120-0715` must remain untouched.

| ID | Area/Repo | Requirement/Surface | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| HEAD-001 | DYEC/headnode | Verify whether Slurm accounting is enabled and usable | SUCCESS | contract_test | Gate 0 | orchestrator | `AccountingStorageType=(null)`, `JobAcctGatherType=(null)`, and `sacct` prints `Slurm accounting storage is disabled`. |  | Accounting is conclusively disabled on the live cluster. |
| HEAD-002 | DYEC/headnode | Verify a fresh `ubuntu` bash login shell automatically activates `DAY-EC` | SUCCESS | contract_test | Gate 0 | orchestrator | Fresh SSM session: `id -un=ubuntu`, login-shell check rc `0`, `CONDA_DEFAULT_ENV=DAY-EC`, bootstrap marker `1`. |  | Automatic login-shell activation works. |
| DAY-001 | DayOA/headnode | Reproduce and identify the root cause of the `mmdc` WebSocket timeout | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Original pane proves a single immediate post-download Puppeteer launch timed out at 30 seconds. Current warm render rc `0` in 0.62s; isolated 118.7 MB cold-cache install 3.50s and first render rc `0` in 0.60s; no missing libraries, OOM, or kernel fault. | The browser launch suffered a non-reproducible transient WS-handshake stall. The durable installer defect is that the one-shot failure leaves the created `DAYOA` env in place, deletes its smoke log, and later `dyoainit` skips validation because the env exists. | Persistent Chrome/Node/library incompatibility and `/dev/shm` were ruled out; exact transient process detail is unrecoverable because the failed smoke directory was deleted. |
| DAY-002 | DayOA/headnode | Verify an explicit `day-clone -t 11.0.15 -d test` checkout | SUCCESS | active_product_contract | Gate 1 | orchestrator | `/fsx/analysis_results/dyec-tst-r5/test/daylily-omics-analysis`, detached exact tag `11.0.15`, commit `ef279ccafa70dd6244c6feac39340cc34f4a80f3`. |  | Explicit-tag clone succeeded. |
| DAY-003 | DayOA/headnode | Run `source dyoainit` successfully in the test checkout | SUCCESS | contract_test | Gate 1 | orchestrator | Persistent one-pane tmux `dayoa_test_smoke_11015_20260716`; `dyoainit_rc=0`; direct mandatory-equivalent `mmdc` render also rc `0`. |  | Initialization now returns success, while the partial-env validation gap remains documented in DAY-001. |
| DAY-004 | DayOA/headnode | Determine whether requested `dy-b BUILD` exists and run it if supported and not already complete | NO_LONGER_NEEDED | contract_test | Gate 1 | orchestrator | Live `dy-b` is an alias to DYEC `bin/init_dayec`, not a DayOA builder. `DAY-EC` already existed, and the later successful headnode configure reinstalled/upgraded it to exact `10.3.26`. |  | The conditional build was already complete; rerunning `dy-b BUILD` would only repeat the DYEC editable install and its script ignores `BUILD`. |
| DAY-005 | DayOA/headnode | Run `dy-a local hg38` after successful initialization/build | SUCCESS | contract_test | Gate 1 | orchestrator | `dy_a_rc=0`, local profile activated, genome build `hg38`; live environment then set `TMPDIR`, `TMP`, `TEMP`, and `SENTIEON_TMPDIR` to `/dev/shm/`. |  | Local activation succeeded; temp-path risk is captured by AUDIT-001. |
| DAY-006 | DayOA/headnode | Run `dy-r help` through the supported wrapper | BLOCKED | contract_test | Gate 1 | orchestrator | Wrapper executed and returned rc `1`: `config/units.tsv` is absent. No raw Snakemake was invoked; wrapper released the analysis lock; queue and controller process lists remained empty. | A clean clone has no `config/units.tsv`, and the failure contract forbids silently selecting/copying an inferred fixture. | Resume only after an exact units manifest or explicit fixture-staging instruction is supplied. |
| AUDIT-001 | DayOA/source | Audit every `/dev/shm` occurrence under `config/**` and `workflow/**` | SUCCESS | contract_test | Gate 0 | orchestrator | `daylily-omics-analysis/docs/plans/20260716T161837Z_dev_shm_audit.md`: 188 occurrences across 50 files, with complete file/line inventory and execution-context classification. |  | Read-only audit complete; no runtime behavior changed. |
| HEAD-003 | DYEC/headnode | Rerun the supported headnode configure step from current DYEC `10.3.26` | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Legacy/public invocation failed SSM command `f439c41e-1662-473b-8c50-5ade145a7f78`, rc `128`, because existing SSH `origin` did not use the pinned host-key file. Rerun with the headnode's exact DYEC and DayOA deploy-key secret references succeeded. Live headnode is clean `daylily-managed...origin/main`, commit `7dbb9cb5...`, package/config/tag `10.3.26`. | The no-deploy-key configure path normalized the requested URL but did not replace an existing SSH origin, so `git fetch origin` failed host-key verification. | Strict deploy-key-backed configure completed without weakening SSH verification. |
| HEAD-004 | DYEC/Slurm accounting | Inspect and then enable the supported Slurm accounting attachment | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | PrivateLink stack `dayec-sacct-pl-vpc-06b01782f2abece1c` is `UPDATE_COMPLETE`; ParallelCluster `dyec-tst-r5` is `UPDATE_COMPLETE`; fleet is restored `RUNNING`; original headnode `i-09e68735609973936` was preserved. Live checks: `slurmdbd` and `slurmctld` active, `AccountingStorageType=accounting_storage/slurmdbd`, `JobAcctGatherType=jobacct_gather/cgroup`, registered cluster `dyec-tst-r5|10.0.0.113|6820`, and `sacct_rc=0`. | Same-CIDR VPCs prevented direct attachment. The bridge then exposed a second defect: NLB TCP health checks exhausted MariaDB's default `max_connect_errors=100` and temporarily blocked the NLB source. | Flushed the MariaDB host cache, set and persisted `max_connect_errors=4294967295`, and added that setting to the managed accounting template. Accounting is enabled and verified. |
| DAY-007 | DayOA/headnode | After configure/accounting, verify `day-clone` with a new explicit destination and DayOA tag | SUCCESS | contract_test | Gate 2 | orchestrator | Persistent one-pane login-shell tmux `dayoa_test_after_sacct_11015_20260716`; cloned `/fsx/analysis_results/dyec-tst-r5/test-after-sacct-20260716/daylily-omics-analysis` with `day-clone -t 11.0.15 -d test-after-sacct-20260716`; exact commit `ef279ccafa70dd6244c6feac39340cc34f4a80f3`, exact tag `11.0.15`, clean worktree. Exact-root write lock was acquired and released. | `analysis visit` and `lock acquire` reject a nonexistent analysis root even though `day-clone` explicitly supports a root containing only `.dayoa_agent/write.lock`; the exact empty root had to be initialized before visit/lock. | Pinned post-accounting clone succeeded; the bootstrap contract gap is recorded without adding fallback discovery. |
| HEAD-005 | DYEC/create | Diagnose why create retained the cluster and returned apparent success with accounting disabled | SUCCESS | contract_test | Gate 0 | orchestrator | Original receipt records requested mode `on`, outcome `WARNING`, `error_stage=service_preparation`, and no fleet mutation. Current reproduction resolves that bounded stage to `service_incompatible` from the VPC mismatch. Chronicle evidence confirms the terminal displayed a Slurm accounting warning and then the cluster-complete/cost panel. Source at `daylily_ec/workflow/create_cluster.py` returns `EXIT_SUCCESS` after a failed accounting stage unless `--fail-on-sacct-error` is supplied. | Accounting is deliberately post-create and fail-soft by default so the successfully created base cluster is retained. The public receipt redacts the underlying preparation exception, and the success panel plus zero exit status can mask the warning to automation/operators. | Root cause confirmed: this was not a failed ParallelCluster create; it was a same-VPC accounting-preparation failure downgraded to a warning by the default CLI contract. |
| PLAN-001 | Execution scope | Apply the user's approval without exceeding the described existing-stack attachment scope | SUCCESS | plan_amendment | Gate 2 | orchestrator | Approval was received after the exact stop/update/replacement impact statement. Read-only preparation discovered the materially different new-stack requirement before the compute fleet was stopped. |  | The approved existing-stack action was not attempted because it is invalid; no live state was mutated and the new ongoing-cost action is separately gated. |
| PLINK-001 | DYEC/AWS | Implement a durable, account-restricted TCP/3306 PrivateLink accounting bridge | SUCCESS | feature_implementation | Gate 2 | orchestrator | Added the bridge CloudFormation asset, packaged-resource validation, redacted resolver, idempotent create/update ensure path, explicit CLI, attach integration, exact endpoint-SG egress, and NLB-safe MariaDB host-error configuration. `pytest tests/test_slurm_accounting.py tests/test_slurm_accounting_privatelink.py tests/test_attach_slurm_accounting.py tests/test_cli_registry_v2.py -q` -> `225 passed`; both CloudFormation templates validate; `git diff --check` passes. | Live verification found and fixed two pre-attachment gaps: same-stack subnet overlap prevented idempotent updates, and an endpoint SG with no explicit egress resource retained EC2's default all-egress rule. | Source and focused contract tests pass; the database is not replaced or exposed publicly, and all bridge data paths are explicitly TCP/3306. |
| PLINK-002 | AWS live | Create and health-check the bridge without changing the existing database host | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Disabled termination protection and deleted only the empty failed record after explicit approval, then recreated the corrected stack. Final stack `UPDATE_COMPLETE`; endpoint `vpce-05ad8a0f6e6b0f666` available; account-root-only endpoint-service permission; internal NLB active; target `10.0.1.237:3306` healthy; client SG `sg-089707c8f0bf9267f`; all bridge SG rules are TCP/3306 only. | Initial unquoted YAML `off` was coerced to Boolean `false`; the subsequent live rule audit caught and corrected default endpoint-SG all-egress. | Corrected bridge is healthy, private, termination-protected, and retained independently of cluster deletion. |
| PLINK-003 | DYEC/Slurm | Attach the bridge and restore the initially running compute fleet | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Pre-stop queue was empty with no workflow controller processes. Fleet reached `STOPPED` at `2026-07-16T18:43:32Z`; dry-run config added only client SG `sg-089707c8f0bf9267f` and the endpoint database URI; update submitted and reached `UPDATE_COMPLETE` at `18:50:56Z`; fleet restored `RUNNING` at `18:54:30Z`. Existing accounting stack remains termination-protected, its instance remains running, and `ifx-p2-1000-120-0715` remains `UPDATE_COMPLETE`/`RUNNING`. | During the update, MariaDB blocked NLB source `10.0.1.138` after health-check handshake errors; a host-cache flush allowed SlurmDBD to connect and the persistent maximum error threshold prevents recurrence. | Cluster update, accounting bootstrap, fleet restoration, and preservation checks all succeeded. |
| DAY-008 | DayOA/headnode | Prove Ursa's accounting transport boundary | SUCCESS | contract_test | Gate 0 | orchestrator | Current Ursa source runs `/opt/slurm/bin/sacct` on each cluster headnode through DYEC/AWS SSM; Ursa does not connect to MariaDB directly. |  | PrivateLink is required only from `dyec-tst-r5` headnode to the database, not from the Ursa webservice VPC. |

## Final Report

All rows terminal: `yes`

Objective complete: `no` for the original broader diagnostic request because `DAY-006` still lacks an exact `config/units.tsv`; `yes` for the approved PrivateLink/accounting recovery and pinned post-accounting clone amendment.

Status counts:

- `SUCCESS`: 16
- `NO_LONGER_NEEDED`: 1
- `BLOCKED`: 1
- `IN_PROGRESS`: 0
- `ATTEMPTING_BUGFIX`: 0

Approved failed-stack recovery is complete:

- Deleted object: only the empty `ROLLBACK_COMPLETE` record for `dayec-sacct-pl-vpc-06b01782f2abece1c`, after termination protection was disabled under explicit approval.
- Recreated bridge: same stack name, now termination-protected and `UPDATE_COMPLETE`, with a healthy internal TCP/3306 path and account-restricted endpoint service.
- Cluster: `dyec-tst-r5` update `UPDATE_COMPLETE`, compute fleet `RUNNING`, original headnode preserved, `sacct` rc `0`.
- Existing service: `dayec-slurm-accounting-us-west-2c` and instance `i-088957ddbc7fcf3d1` remain healthy; `ifx-p2-1000-120-0715` remains `UPDATE_COMPLETE`/`RUNNING` and retains direct database sessions.
- Clone proof: exact DayOA `11.0.15` at commit `ef279ccafa70dd6244c6feac39340cc34f4a80f3`, clean worktree under `test-after-sacct-20260716`.
