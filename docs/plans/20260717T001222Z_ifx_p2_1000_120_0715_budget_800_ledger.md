# ifx-p2-1000-120-0715 budget increase to $800

Date: 2026-07-17 UTC

## Authorization and baseline

- Target cluster and cost center: `ifx-p2-1000-120-0715`.
- Requested change: AWS cluster budget and DYEC monthly cost-center cap from `$600` to `$800`.
- First approval: user requested `increase this budget to $800`.
- Second approval: user replied `confirm increase to $800`.
- Effect disclosed before second approval: authorize up to `$200` additional cluster spend.
- AWS account/profile: `108782052779` / `lsmc`.
- Pre-change AWS Budget: `$600.0`, actual spend `$537.941`, health `HEALTHY`.
- Pre-change DYEC cost center: active, monthly cap `$600`, allowed user `ubuntu`.

## Execution ledger

| ID | Requirement | Status | Evidence | Terminal note |
|---|---|---|---|---|
| BUDGET-001 | Update AWS Budget `ifx-p2-1000-120-0715` from `$600` to `$800` | SUCCESS | AWS `UpdateBudget` returned HTTP `200`; post-change `describe-budget` shows `$800.0`, actual `$537.941`, health `HEALTHY`, and the original cluster tag filter | Existing 75% actual-spend notification remains present. |
| COSTCENTER-001 | Update DYEC cost-center monthly cap from `$600` to `$800` | SUCCESS | `dyec cost-centers edit ... --monthly-cap-usd 800`; post-change readback shows active cap `$800`, allowed user `ubuntu`, updated `2026-07-17T00:13:02Z` | No status, user, or notes field changed. |
| VERIFY-001 | Read back both enforcement surfaces at exactly `$800` | SUCCESS | Headnode `sbatch --test-only` reported AWS `total=800.0 USD`, used `$537.941`, `67.24%`, and cost-center `cap=800` | Test-only Slurm id `1909` was absent from both `squeue` and `sacct`; no job was submitted. |

## Final report

All rows terminal: yes

Objective complete: yes

Final effective headroom at the verified AWS spend snapshot: `$262.059`.
