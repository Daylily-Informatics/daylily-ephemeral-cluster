# `majors-cluster` HIOMRS Kitchen-Sink Execution Ledger

Controlling request: from a new initially empty local filesystem location, clone the released DYEC and DayOA forks, create a default Intel cluster for `johnm@lsmc.com` with every public Intel maximum-count family set to 8, prove `sacct` is operational, launch separate DayOA `12.0.2` HIOMRS kitchen-sink catalog workflows for HG003 1x and HG003 5x, and monitor both analyses through completion.

Ledger path: `docs/plans/20260718T075931Z_majors_cluster_hiomrs_kitchensink_ledger.md`

## Scope And Safety Contract

- Local isolation root: `/Users/jmajor/projects/lsmc-hiomrs-kitchensink-20260718T075106Z`; it was verified empty before either clone was created.
- DYEC clone: exact annotated tag `11.0.4`, peeled commit `93e0042cad38d4bc5623b6b5ea52c68e4d35e48a`, fork-only HTTPS origin `https://github.com/lsmc-bio/daylily-ephemeral-cluster.git`.
- DayOA clone: exact annotated tag `12.0.2`, peeled commit `46d51469b7962a6bec7b4ff5c3765ddd6303a2fe`, fork-only HTTPS origin `https://github.com/lsmc-bio/daylily-omics-analysis.git`.
- DYEC must be invoked locally only after `source ./activate`; no direct SSH or raw Snakemake is allowed. Headnode workflow operations must use the supported DYEC/SSM and persistent interactive `ubuntu` tmux path.
- Named AWS profile: `lsmc`; account `108782052779`; region/AZ default `us-west-2d`.
- Proposed cluster contract: `majors-cluster`, Intel, `r7i.2xlarge` headnode, 4,800-GiB `PERSISTENT_2` FSx at 250 MB/s/TiB, budget enforcement enabled, new $200 cluster budget/cost center for `ubuntu` owned by `johnm@lsmc.com`, existing $600 `daylily-global` budget unchanged, and all public Intel maximum-count values set to 8.
- Only one existing non-deleted `us-west-2` cluster was observed before create, below the default regional cap of 5.
- `sacct` is a hard acceptance gate before workflow launch. Read-only diagnosis and supported DYEC headnode configuration are in scope; any direct Slurm service/config intervention must be stated precisely before execution.
- Workflow ref must be explicitly `12.0.2`; catalog command must be `hybrid_ilmn_ont_hiomrs_kitchensink`; analysis IDs are `majors-hg003-1x-hiomrs-1202-20260718T0911Z` and `majors-hg003-5x-hiomrs-1202-20260718T0911Z`; both analyses must be observed through terminal completion.

## Gate 0 Baseline

- `source ./activate` created/activated `DAY-EC`; `which dyec` resolved to `/Users/jmajor/miniconda3/envs/DAY-EC/bin/dyec`, import source resolved to this isolated checkout, and `dyec version` reported `11.0.4`.
- Live cluster inventory contained only `ifx-p2-1000-120-0715` in `UPDATE_COMPLETE`.
- `majors-cluster` had no existing ParallelCluster record, AWS Budget, or cost-center registry row.
- Existing global AWS Budget `daylily-global` is $600 with $0 current calculated spend; DYEC `ensure_global_budget` does not alter an existing budget.
- The released catalog pins `hybrid_ilmn_ont_hiomrs_kitchensink` to DayOA `12.0.2`, uses input contract `sample_manifest_v12`, profile `default_reads_slim`, `hg38`, and the full HIOMRS kitchen-sink target set.
- User amendment at `2026-07-18T09:02Z`: freeze all cost-analysis work and restrict the active lane to the new DYEC cluster test with separate HG003 1x and 5x HIOMRS kitchen-sink runs.
- User approval after the accounting diagnosis: fix DYEC to reuse compatible existing PrivateLink more gracefully, then stop the proven-empty `majors-cluster` compute fleet, dry-run and apply the accounting attachment through the existing bridge, wait for the ParallelCluster update, reconfigure a replacement headnode if required, restore the fleet, and prove `sacct` as `ubuntu`; creating a database or PrivateLink service is expressly prohibited.
- Strict inputs are under `docs/plans/20260718T075931Z_majors_cluster_hg003_inputs/`. HG003 specimen/sample fields come from the source-owned DayOA 12 GIAB fixture; 1x and 5x library fields and exact source paths come from released DYEC staging examples. Conditional EUIDs remain blank exactly as the source owns them, and blank `ANALYSIS_UNIT_UID` is preserved for DayOA's tested construction from exact library fields. No identifier or EUID is invented.
- The generated triples are staged as six exact objects under `s3://lsmc-ssf-sequencing-data/staged_external_data/majors-cluster/majors-hg003-{1x,5x}-hiomrs-1202-20260718T0911Z/`; each run prefix contains `specimens.tsv` (151 bytes), `samples.tsv` (496 bytes), and schema-valid `libraries.tsv` (1,152 bytes).

## Control Ledger

| ID | Area | Requirement | Status | Category | Gate | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|
| ISO-001 | Local clones | Prove the final parent began empty and both fork-only clones resolve to the exact released annotated tags. | SUCCESS | inventory | Gate 0 | Paths, HTTPS origins, tag object types, and peeled commits recorded above. |  | Final working clones are isolated from all earlier dirty checkouts. |
| CFG-001 | DYEC runtime | Activate the new DYEC checkout and prove the CLI/package/source are exact `11.0.4`. | SUCCESS | config_or_startup_contract | Gate 0 | `source ./activate`; package and `dyec version` both report `11.0.4`; imported source is this clone. |  | No older DYEC executable is in use. |
| CFG-002 | Provisioning contract | Resolve literal defaults, live inventory, budget behavior, Intel counts, and the exact profile/account. | SUCCESS | config_or_startup_contract | Gate 0 | Contract recorded above; user selected profile `lsmc`; regional projected count is 2 of cap 5. |  | No budget-cap increase or regional-cap override is requested. |
| AMEND-001 | Scope | Freeze cost analysis and amend the live test to separate HG003 1x and 5x HIOMRS kitchen-sink analyses. | SUCCESS | plan_amendment | Gate 0 | User direction recorded above; remaining cost work is frozen at `/Users/jmajor/projects/lsmc/docs/remaining_cost_analysis_work.md`. |  | This ledger now owns only the DYEC cluster and two workflow tests. |
| DATA-001 | Workflow inputs | Select and validate strict DayOA 12 HG003 1x and 5x hybrid manifests and exact reference/control/staging roots without invented identities or paths. | SUCCESS | active_product_contract | Gate 2 | Both `dyec samples stage ... --manifest-contract dayoa12 --config-only` prechecks passed: 1 row, 1 sample, 22 source objects, and 1 concordance directory each. Generated exact `specimens.tsv`, `samples.tsv`, and `libraries.tsv` triples are under the input root. Source SHA-256 values: 1x `995e18fad06ffee923990de0f758f471e9f63e2ac7838c9f83d3445cf7541914`; 5x `06c253784cca9e4727f349aef713b8e0c320fd89dfa3b335b8fe8729bf8d4ede`. |  | Six source objects were independently confirmed in `lsmc-dayoa-references-usw2`; DayOA 12 preserves blank conditional EUIDs and constructs blank analysis-unit identities from exact library fields. |
| AWS-001 | Preflight | Run the complete DYEC preflight with the exact proposed values and no persistent mutation. | SUCCESS | contract_test | Gate 5 | `dyec preflight --region-az us-west-2d --profile lsmc --config docs/plans/20260718T075931Z_majors_cluster_request.yaml --non-interactive` passed all 12 IAM, catalog, quota, network-capacity, and S3 role checks. |  | Proposed infrastructure contract is currently admissible. |
| AWS-002 | Cost control | Create/verify the $200 `majors-cluster` budget and cost-center row without changing the existing $600 global cap. | SUCCESS | config_or_startup_contract | Gate 5 | Live create reported `Budgets: global=daylily-global, project=majors-cluster`; direct `aws budgets describe-budget` receipts report `daylily-global=600.0 USD` and `majors-cluster=200.0 USD`. The durable request now records both literal caps. |  | `ensure_global_budget` is create-only when the global budget already exists, so the live create did not mutate the $600 cap. No budget-cap increase was requested or performed. |
| AWS-003 | Cluster create | Create `majors-cluster` and wait for ParallelCluster plus DYEC post-create stages to reach a usable terminal state. | SUCCESS | feature_implementation | Gate 5 | Live create passed all 14 create-time preflight checks. Dedicated P2 FSx `fs-0c3980010a92252b1` became `AVAILABLE` after 435 seconds and reference DRA `dra-06a58d32ab234e73d` became `AVAILABLE` after 540 seconds. The rendered cluster passed `pcluster` dry-run; `majors-cluster` reached `CREATE_COMPLETE` in 10m29s, headnode configuration succeeded, heartbeat configuration succeeded, and state was written to `/Users/jmajor/.config/daylily/state_majors-cluster_20260718091339.json`. |  | Cluster/headnode creation is complete; the separate accounting gate remains open. |
| DYEC-001 | PrivateLink reuse | Make create-time accounting reuse the exact existing healthy managed PrivateLink bridge for the cluster VPC when direct regional-service attachment is incompatible, without creating or broadly selecting infrastructure. | SUCCESS | feature_implementation | Gate 5 | Branch `codex/slurm-accounting-privatelink-reuse`; deterministic consumer-VPC resolver, automatic direct-then-bridge preparation, docs, and regression tests patched. Live preparation without an explicit bridge selected `dayec-sacct-pl-vpc-06b01782f2abece1c`, reported `service_created=False`, and rendered `/Users/jmajor/.config/daylily/majors-cluster_slurm_accounting_update_20260718101853.yaml`. Ruff check and format checks pass; `git diff --check` passes; focused suite passed `51 passed in 0.55s`; the complete suite passed `2268 passed, 11 skipped` in 66.93 seconds. | Create-time preparation accepted only an explicit PrivateLink stack even though managed bridge names are deterministic by consumer VPC. | Reuse path checks only the exact deterministic existing bridge, validates consumer/provider identities and live target health, and never creates or broadly selects infrastructure. |
| DYEC-002 | DayOA 12 manifest schema | Generate strict DayOA 12.0.2 `libraries.tsv` columns that the pinned schema accepts, without silently discarding populated values. | SUCCESS | feature_implementation | Gate 5 | The first live `dy-r -n` attempts failed before DAG construction because generated blank `ULTIMA_SUBSAMPLE_PCT` and `ONT_SUBSAMPLE_PCT` columns violate the pinned schema's `additionalProperties: false`. DYEC now omits those blank columns from DayOA 12 libraries output and rejects either field when populated. Ruff checks, formatting, `git diff --check`, and the combined manifest/PrivateLink focused suite pass (`30 passed in 0.70s`); the complete suite passed `2268 passed, 11 skipped` in 66.93 seconds. Regenerated 1x/5x triples passed full precheck (`1` row, `1` sample, `22` source objects, `1` concordance directory each). | The legacy `UNITS_HEADER` supplied two columns that downstream DayOA code can default but the released DayOA 12.0.2 libraries schema does not accept. | New 1,152-byte libraries manifests were staged at the exact run prefixes; no DayOA checkout was modified and nonblank unsupported values fail hard. |
| DAYOA-001 | Sex-complement provenance | Make the DayOA 12 reported-versus-inferred sex report consume `BIOLOGICAL_SEX` from its schema-owned `specimens.tsv` through the exact `samples.tsv:SPECIMEN_ID` foreign key. | SUCCESS | feature_implementation | Gate 5 | The 1x live run exposed `SexComplementReportError: samples manifest is missing BIOLOGICAL_SEX`. The pinned strict schemas prove `BIOLOGICAL_SEX` is required in `specimens.tsv` and forbidden by `additionalProperties: false` in `samples.tsv`. Local isolated DayOA branch `codex/dayoa12-sex-complement-specimen-provenance` joins the two normalized manifests and validates missing/blank/duplicate keys. Guarded patches are installed in both exact-tag checkouts. The report-facing 5x `input_samples_mqc.tsv` contains `BIOLOGICAL_SEX=male`, while source snapshots remain byte-preserving. The complete local suite passes `958 passed in 31.90s`; focused remote shell tests pass `33 passed`. Both wrappers returned 0. | The comparison script retained the pre-normalization assumption that reported sex lived directly in `samples.tsv`, contradicting the released DayOA 12 manifest schemas. | The corrected provenance contract is live-proven at HG003 1x and 5x and remains fail-hard for missing, blank, duplicate, or unmapped specimen identities. |
| DAYOA-002 | MultiQC provenance | Preserve valid single-sample, retry, identity, grain, scope, modality, caller, and required custom-section provenance through strict final MultiQC staging. | SUCCESS | feature_implementation | Gate 5 | Live retries exposed independent strict-contract defects. Fixes include optional benchmark attempt/status/job/datetime columns; exact Slurm allocation selection from the authoritative `slurm_jobid`; preservation of every benchmark attempt row instead of `tail -n 1`; correct header-only single-sample Somalier schema; global `workflow_artifact`; explicit SeqFu `sr`/`native_file`; pipe-joined source grains; exact conflict diagnostics; generic/SNV/SV caller reconciliation; report-facing specimen-sex enrichment; and the basename-only `sp.library_summary` search pattern required by pinned MultiQC. The two-attempt collector regression passes, the complete local suite passes `958 passed`, Ruff/format/diff checks pass, and the guarded final 5x rebuild returned 0. The 1x/5x logs each record `library_summary: Found 1 samples` twice and both MultiQC JSON files contain raw data key `multiqc_library_summary`. | Several final-report producers and the staging/configuration layers had independently drifted from the stricter DayOA 12/MultiQC selector provenance contract. | Final receipts: 1x HTML `8,445,209` bytes and evidence JSON `552,443` bytes; 5x HTML `8,466,228` bytes and evidence JSON `552,903` bytes. Both evidence manifests parse as JSON. |
| SLURM-001 | Accounting | Prove `sacct` is present, configured, and returns a successful bounded query before workflow launch. | SUCCESS | contract_test | Gate 5 | After a required-format empty `squeue` proof, the compute fleet moved `RUNNING -> STOPPED`. The patched no-explicit-bridge dry-run selected existing bridge `dayec-sacct-pl-vpc-06b01782f2abece1c` and rendered `/Users/jmajor/.config/daylily/majors-cluster_slurm_accounting_update_20260718102339.yaml`; the live attach repeated its built-in dry-run and submitted `/Users/jmajor/.config/daylily/majors-cluster_slurm_accounting_update_20260718103029.yaml`. `dyec cluster wait` reached `UPDATE_COMPLETE`. The current headnode `i-0b8817aa1ded68964` has launch time `2026-07-18T09:37:12Z`, before the `10:30Z` update, so it was not replaced and no reconfigure was required. The compute fleet returned to `RUNNING` at `2026-07-18T10:37:53.751Z`. In an interactive Session Manager login shell, `id -un` returned `ubuntu`, `command -v sacct` returned `/opt/slurm/bin/sacct`, and a bounded `timeout 60 sacct -X --starttime now-1hour` query returned rc=0. | The original create-time attach attempted the incompatible direct regional service instead of the existing bridge. | Resolved by deterministic reuse of the exact healthy existing consumer-VPC bridge. No database or PrivateLink service was created. The empty `sacct` result set is expected before the first submitted job; the database query itself succeeded. |
| WF-001 | Staging/dry-run | Stage both strict DayOA 12 triples, create both explicit `12.0.2` analysis checkouts, acquire each analysis-root lock, and pass catalog dry-runs through `dy-r`. | SUCCESS | contract_test | Gate 5 | Separate one-window/one-pane interactive `ubuntu` tmux sessions were created with the exact analysis IDs. `day-clone -t 12.0.2` resolved both checkouts to `46d51469b7962a6bec7b4ff5c3765ddd6303a2fe`; stable agent identity variables, write visits, and owned write locks were recorded at `/fsx/analysis_results/majors-cluster/<analysis-id>`. Exact staged triples were installed, `source dyoainit` exposed Snakemake `7.25.0b113`, and `dy-a slurm hg38` succeeded. After the source-contract repair in DYEC-002, both full catalog `dy-r ... -n` runs reported `This was a dry-run` and `RETURN CODE: 0`. | Initial generated libraries headers exceeded the strict pinned schema. | Corrected at the DYEC producer, regenerated, restaged, and reproven without modifying DayOA or using raw Snakemake. |
| WF-002 | Live workflow | Launch the HG003 1x and 5x `hybrid_ilmn_ont_hiomrs_kitchensink` analyses in separate persistent named `ubuntu` tmux sessions using the supported wrapper path. | SUCCESS | feature_implementation | Gate 5 | Both full live `dy-r` commands ran in verified one-window/one-pane interactive `ubuntu` sessions with exact tag `12.0.2`, exact dry-run-proven targets/configuration, and Snakemake `7.25.0b113`. The supported cost-center path added active `majors-cluster` in existing tables with cap `$200`, user `ubuntu`, owner `johnm@lsmc.com`, and spend `$0`; no database or table was created. Slurm automatically requeued job `10` after `NODE_FAIL`; job `70` completed in `02:24:28`; the failed SiteMix allocation `58` was cleanly replaced by completed job `164`. The 1x wrapper returned 0 at `14:46:17Z`; the 5x final guarded rebuild completed 5/5 and returned 0 at `15:33:17Z`. |  | Both persistent tmux sessions remain structurally valid, both roots are unlocked, and the final required-format `squeue` contains only its header. |
| MON-001 | Completion | Monitor both controllers, `squeue`, `sacct`, retry/attempt evidence, final outputs, MultiQC, and evidence manifests through terminal successful completion. | SUCCESS | contract_test | Gate 5 | As `ubuntu`, final `sacct -D` preserves job `10` as `NODE_FAIL` `00:05:32` then `COMPLETED` `00:15:11`; job `58` as `FAILED` `00:15:13` exit `0:15`; retry `164` as `COMPLETED` `00:13:39`; core `70` as `COMPLETED` `02:24:28`; 1x final report `161` as `COMPLETED` `00:00:41`; and guarded 5x final report `250` as `COMPLETED` `00:00:39`. The final 5x benchmark report contains 120 accepted success rows, all with attempt `1`, `snakemake_jobid`, and `slurm_jobid`; grouped row identity is exact `9 -> 9`. Both evidence JSON files parse and both analysis locks report `unlocked`. | Slurm requeue and signal termination happened below a surviving Snakemake benchmark process: job `10` was observed by Snakemake as one successful attempt, and killed job `58` could not emit a process-level failed benchmark row. | Duplicate/failed `sacct` records are the authoritative allocation-level retry evidence. The corrected collector is proven by a synthetic two-attempt benchmark regression and no longer discards multi-row attempt data when Snakemake can emit it. |
| EVD-001 | Closeout | Record exact cluster/run/analysis/session identifiers, output receipts, status counts, and every non-success row. | SUCCESS | durable_evidence | Gate 5 | Cluster `majors-cluster`; headnode `i-0b8817aa1ded68964`; FSx `fs-0c3980010a92252b1`; DRA `dra-06a58d32ab234e73d`; bridge `dayec-sacct-pl-vpc-06b01782f2abece1c`; sessions/analysis IDs `majors-hg003-{1x,5x}-hiomrs-1202-20260718T0911Z`. Current output byte receipts and terminal Slurm rows are recorded in DAYOA-002 and MON-001. Local DYEC suite: `2268 passed, 11 skipped`; local DayOA suite: `958 passed`; both dirty trees pass `git diff --check`. |  | No database, table, or PrivateLink service was created. No source commit, push, tag, release, or PR was performed for these newly live-proven DYEC/DayOA changes. |

## Current Status

- SUCCESS: 17
- IN_PROGRESS: 0
- OPEN: 0
- BLOCKED: 0
- FAIL: 0

All rows terminal: **yes**

Objective complete: **yes**

Non-success terminal rows: **none**

Warnings retained in successful rows:

- Slurm job `10` was requeued below Snakemake, so its benchmark row represents the one workflow attempt Snakemake observed; both physical allocations remain in duplicate `sacct` records.
- Signal-killed SiteMix allocation `58` could not finish a process-level failed benchmark row; failed allocation `58` and successful retry `164` remain explicit in `sacct`.
- The live-proven DYEC and DayOA fixes remain dirty and unpublished by design; this execution did not authorize a new commit, tag, release, push, or PR.
