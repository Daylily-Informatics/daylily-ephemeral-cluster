# Majors HG003 1x HIOMRS DayOA 13.0.10 Ledger

Created: `2026-07-19T13:03:38Z`

## Objective

Start a third, separate HIOMRS kitchen-sink analysis on `majors-cluster` using
the explicitly requested released DayOA tag `13.0.10`, with exactly one HG003
analysis unit backed by the mounted ILMN 1x and ONT 1x inputs at full input
fractions. Preserve both existing analyses and Slurm job `251` unchanged.

## Gate 0 Baseline

- AWS profile `lsmc`, region `us-west-2`, cluster `majors-cluster`, remote user `ubuntu`.
- Local DYEC repo `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster` is on `main`, 41 commits behind `origin/main`, with five pre-existing untracked ledger files. This ledger is an additional scoped untracked artifact; no existing changes will be modified.
- `day-clone -t 13.0.10 --check-auth` succeeded with `Repository authentication validated: daylily-omics-analysis @ 13.0.10`.
- Existing four-unit root `/fsx/analysis_results/majors-cluster/hg003-1x-hiomrs-1308cand-20260719T115732Z` and single-unit root `/fsx/analysis_results/majors-cluster/hg003-1x-full-hiomrs-1308cand-20260719T123250Z` remain live under their own controllers and locks.
- Slurm job `251` is not authorized for cancellation in this request and remains untouched.
- Intended new analysis id: `hg003-1x-full-hiomrs-13010-20260719T130338Z`.
- Execution contract: `day-clone -t 13.0.10 -d <analysis_id>`, exact-tag verification, exact-root write visit and lock, separate named tmux, `source dyoainit`, `dy-a slurm hg38`, fail-fast `dy-r` dry-run, then identical live `dy-r` command. Never invoke raw Snakemake.

## Control Ledger

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| INV-001 | Baseline | Preserve existing workflows and job 251; validate exact requested tag authentication | SUCCESS | config_or_startup_contract | Gate 0 | orchestrator | Two existing roots identified; `day-clone -t 13.0.10 --check-auth` succeeded |  | Exact tag is resolvable; unrelated live work remains out of scope. |
| ROOT-001 | Fresh root | Create a new analysis root through `day-clone -t 13.0.10` and verify exact tag | SUCCESS | feature_implementation | Gate 1 | orchestrator | `/fsx/analysis_results/majors-cluster/hg003-1x-full-hiomrs-13010-20260719T130338Z/daylily-omics-analysis`; detached HEAD `27b9f24f9d7ce10322efa9125c7443e56b4380ac`; exact tag `13.0.10`; clean before manifests |  | Fresh exact-tag checkout created through the required command; no outputs reused. |
| LOCK-001 | Agent lock | Record write visit and acquire the exact new-root lock | SUCCESS | legitimate_safety_handling | Gate 1 | orchestrator | Write visit recorded; status after launch: `locked by codex-root-majors-hg003-1x-13010-20260719` |  | Live controller retains its exact-root lock. |
| INPUT-001 | Six manifests | Install and validate only one full-depth HG003 1x SR/LR analysis unit | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | `dayoa manifests validate --manifest-dir config/` -> `VALIDATE_RC:0`; rows `1/1/1/2/1/2`; all invariants true; blank SR/ONT subsample fields; analysis-unit SHA256 `90da7902...`; joins SHA256 `2e277bfd...` |  | Exact A1-only manifest contract validated. |
| DRY-001 | DAG | Run the exact kitchen-sink target/config set fail-fast through `dy-r -n` | SUCCESS | contract_test | Gate 4 | orchestrator | Dedicated tmux `majors-hg003-1x-full-hiomrs-13010-20260719`; `source dyoainit`; `dy-a slurm hg38` -> `DYA_RC:0`; dry DAG 178 jobs, max 128 threads; wrapper `RETURN CODE: 0`; `DRY_RUN_RC:0` |  | No `-k`; exact target/config set reserved for live execution. |
| LIVE-001 | Execution | Launch the identical command live and capture controller/Slurm evidence | SUCCESS | feature_implementation | Gate 5 | orchestrator | Controller PID `1107065`; exact live command active; initial Slurm jobs `428`-`432` |  | Identical target/config set launched without `-n`. |
| MON-001 | Monitoring | Confirm lock, controller, accounting, and initial progress without claiming completion | SUCCESS | contract_test | Gate 5 | orchestrator | Tmux `majors-hg003-1x-full-hiomrs-13010-20260719`; lock held; `sacct` confirms jobs `428`-`432` all `RUNNING` with exact new-root `WorkDir` |  | Start objective verified; analysis remains in progress and is not claimed complete. |

## Final Report

All rows terminal: `yes` (`7 SUCCESS`)

Objective complete: `yes` for starting and verifying the requested test. The analysis itself remains in progress.

## Status Snapshots

- `2026-07-19T13:18Z`: controller PID `1107065` active for 2m31s in tmux
  `majors-hg003-1x-full-hiomrs-13010-20260719`; exact-root lock remains owned by
  `codex-root-majors-hg003-1x-13010-20260719`. `sacct` reports jobs `428`-`432`
  `RUNNING` with the exact new-root `WorkDir`; `squeue` reports those same jobs
  `CONFIGURING`. No workflow failure or controller exit is present. Analysis is
  active, not complete.
