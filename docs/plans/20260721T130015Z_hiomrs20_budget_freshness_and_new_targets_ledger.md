# HIOMRS20 Budget Freshness And New Targets Ledger

Created: 2026-07-21T13:00:15Z

## Objective

Use explicit user approval to change DYEC cost-center freshness from 36h to 72h
globally, skip the exhausted cluster AWS Budget check only for this approved
job attempt, then run the requested 20-library HIOMRS add-on targets first as a
dry-run with `--rerun-triggers mtime -j 300 -p -T 1 -k -n`. If the plan only
runs the requested new work and does not recreate large existing outputs, rerun
the same target set live without `-n`.

## Gate 0 Baseline

| Item | Evidence |
|---|---|
| DYEC repo | `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster` |
| DayOA repo | `/Users/jmajor/projects/lsmc/daylily-omics-analysis` |
| DYEC branch/status | `codex/hiomrs-pgx-10.3.34...origin/codex/hiomrs-pgx-10.3.34`; pre-existing untracked ledgers/artifacts under `docs/plans/` |
| DayOA branch/status | `codex/hiomrs-pgx-closeout-20260721...origin/codex/hiomrs-pgx-closeout-20260721`; pre-existing untracked Inflection artifacts and SeqOne packaging plan |
| Target cluster/profile/region | `ifx-p2-1000-120-0715`, `lsmc`, `us-west-2` |
| Target analysis root | `/fsx/analysis_results/ifx-p2-1000-120-0715/bjuiceprevalanalysis-complete/daylily-omics-analysis` |
| Current AWS Budget | `ifx-p2-1000-120-0715`: limit `$2700.00`, actual `$2705.361`, exhausted; user explicitly said no cap change |
| Current cost-center | `ifx-p2-1000-120-0715`: cap `$1550`, stored July usage `$917.68816167270000000005355`, latest processed hour `2026-07-19T15:00:00Z` |
| Approval | User approved changing cost-center freshness to 72h globally and skipping the exhausted budget check for this job; no approval to raise budget cap |
| Execution contract | No raw Snakemake. Use headnode `ubuntu` tmux, `source dyoainit`, `dy-a slurm hg38`, then `dy-r ...`; use read visits/locks per analysis-root rules. |

## Tracking Rows

| ID | Area | Requirement | Status | Category | Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| BF-001 | DYEC policy | Change global cost-center freshness threshold from 36h to 72h in source, packaged payload, validator, and tests. | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | Changed `config/day_cluster/sbatch`, `daylily_ec/resources/payload/config/day_cluster/sbatch`, and `daylily_ec/aws/validation.py`; `source ./activate && python -m pytest -q tests/test_sbatch_wrapper.py tests/test_aws_validation_cost_controls.py` -> 37 passed. |  | Source, packaged payload, readiness constant, and tests now use 72h. |
| BF-002 | Headnode enforcement | Apply the 72h sbatch wrapper to target headnode and verify the wrapper reports `max_hours=72.00` on this stale snapshot. | SUCCESS | config_or_startup_contract | Gate 5 | orchestrator | SSM upload `d82101b6-c3df-4fe2-a638-8c39c3504597`; install `5c3105fb-c49e-42e6-842f-475601fb1538`; `/opt/slurm/bin/sbatch` SHA changed `7dccdeb8...` -> `1581240a...`; line 13 `max_usage_age_hours="72"`. |  | Target headnode wrapper now uses 72h freshness threshold. |
| BF-003 | Approved job exception | Skip only the exhausted AWS Budget check for the approved job attempt without raising the AWS Budget cap. | OPEN | config_or_startup_contract | Gate 5 | orchestrator |  |  |  |
| RUN-001 | Target discovery | Resolve the exact requested DayOA targets for Sniffles2, short-read CNV mirrored from HIOMRS, ROH/AUP, samstats/idxstats, and MultiQC across all 20 libraries. | SUCCESS | feature_implementation | Gate 5 | orchestrator | Corrected target surface is the prior full HIOMRS kitchensink command plus `produce_samtools_metrics`, `produce_sentdhiomr_cnv`, and concrete Inflection delivery manifest target `results/day/hg38/deliveries/inflection/ifx-del-20260721T132208Z/INFLECTION_delivery_set/delivery_set_manifest.json`; delivery batch id `ifx-del-20260721T132208Z`. |  | Target set now follows user's "new targets added on to the full hiomrs kitchensink command" correction. |
| RUN-002 | Dry-run | Run the requested target set with `--rerun-triggers mtime -j 300 -p -T 1 -k -n` and classify whether it limits work to the requested new targets. | BLOCKED | contract_test | Gate 5 | orchestrator | Headnode log `/fsx/analysis_results/ifx-p2-1000-120-0715/bjuiceprevalanalysis-complete/daylily-omics-analysis/logs/hiomrs20_full_ks_plus_new_ifx-del-20260721T132208Z_dryrun.log`; dry-run returned 0 and submitted no jobs; summary planned 622 jobs total, including 20 `sentdhiomr_sr_align`, 20 `sentdhiomr_sr_markdup`, 20 `sentdhiomr_merge_sr_bams`, 20 `sentdhiomr_call_cnvs`, plus missing HIOMRS Sniffles2/CNV/ROH/Inflection/MultiQC/package outputs. | The corrected dry-run is broader than the user's conditional "only new stuff and does not try to recreate lots of existing files" gate. | Live execution needs explicit user approval or a narrower target contract. |
| RUN-003 | Live run | If dry-run scope is acceptable, run the same target set without `-n` and monitor supported status surfaces. | HOLD | feature_implementation | Gate 5 | orchestrator | Not started. Budget skip not applied. Write lock remains available for the current decision point. | Dry-run scope gate is not satisfied automatically. | Awaiting explicit proceed or narrower target instruction. |
| RUN-004 | SegDup delta scope | Review produced SegDup calls and request only the v0.9.0 difference from the existing 20-library output set. | SUCCESS | contract_test | Gate 5 | orchestrator | Existing headnode inventory: 260 VCF rows, 20 samples, 13 VCF-producing genes each: `CFH,CYP11B1,CYP2B6,CYP2D6,GBA1,HBA,NCF1,PMS2,RCCX1,RHD,SBDS,SMN1,STRC`. Repo v0.9.0 allowed/pinned catalog adds `CYP2A6,OTOA,LPA` as VCF-producing delta genes; `IKBKG` is configured but non-VCF. Revised dry-run log `logs/hiomrs20_segdup_v090_delta_ifx-del-20260721T132208Z_dryrun2.log` returned 0 and planned exactly 60 `hiomrs_segdup_gene` jobs, 8 threads each, all due missing output files. | Earlier full target dry-run requested all 17 genes via `produce_hiomrs_segdup`; exact shard targets are required to avoid rerunning already produced genes. | Delta request is clean for per-gene shard generation only; collector/package integration remains a separate scope decision because collector publishes terminal merged outputs. |
| RUN-005 | SegDup duration estimate | Estimate SegDup delta runtime from existing benchmark TSVs. | SUCCESS | evidence | Gate 5 | orchestrator | Parsed 300 existing SegDup benchmark rows: per gene/sample median about 6.0 minutes overall; slowest observed gene/sample 17.9 minutes. Gene medians included `CFH` 14.4m, `RHD` 11.0m, `GBA1` 10.9m, `PMS2` 8.2m, `CYP2B6` 6.9m, `SMN1` 6.1m, `CYP2D6` 3.4m, `STRC` 3.6m. No direct historical rows for new delta genes `CYP2A6,OTOA,LPA` in this run. |  | For 60 jobs at 8 threads/job and `-j 300`, expected compute wall is roughly one to two scheduler waves; practical estimate 30-75 minutes including Slurm and env overhead, assuming capacity is available. |
| RUN-006 | Full v0.9.0 SegDup live | With user approval that rerunning genes is acceptable, run `produce_hiomrs_segdup` using the complete 17-gene v0.9.0 list. | RUNNING | feature_implementation | Gate 5 | orchestrator | Active `rule_config.yaml` list confirmed: `CFH,CYP11B1,CYP2A6,CYP2B6,CYP2D6,GBA1,HBA,IKBKG,NCF1,PMS2,RCCX1,RHD,SBDS,SMN1,STRC,OTOA,LPA`. Applied approved job exception in `/opt/parallelcluster/shared/cluster-config.yaml`; backup `/opt/parallelcluster/shared/cluster-config.yaml.pre_hiomrs20_segdup_budget_skip_20260721T133445Z`; verified tag `aws-parallelcluster-enforce-budget=skip`. Live command in tmux `hiomrs20_newtargets_20260721`; log `logs/hiomrs20_segdup_v090_full_ifx-del-20260721T132208Z_live.log`. First submissions observed: Slurm jobs `6802`, `6803` for `hiomrs_segdup_gene`; sbatch output confirms AWS Budget check skipped and cost-center usage still OK (`917.688... / 1550`, latest processed hour `2026-07-19T15:00:00Z`, age `46.63h`). |  | Running; restore budget tag after workflow no longer needs submissions. |
| RUN-007 | SegDup LPA contract correction | Correct DayOA's SegDup runtime/rule contract after observed `segdup-caller 0.9.0` LPA YAML emitted no `Variants` output. | SUCCESS | bugfix | Gate 5 | orchestrator | Local DayOA patch changes `workflow/scripts/hiomrs_specialty.py` `SEGDUP_NO_VARIANT_GENES` to include `LPA`, changes `workflow/rules/hiomrs_specialty.smk` variant-gene output list to exclude both `IKBKG` and `LPA`, and updates `tests/test_hiomrs_specialty_rules.py`; `PYTHONPATH=. pytest -q tests/test_hiomrs_specialty_rules.py tests/test_slurm_profile.py` -> 42 passed. | `segdup-caller 0.9.0` treats LPA as a copy-number/no-small-variant result, while DayOA previously required an LPA VCF. | Local patch ready for headnode deployment after active old controller exits or is explicitly canceled. |
| RUN-008 | Corrected non-delivery dry-run | Per user correction, dry-run the non-delivery target set only: SegDup, CNV, Sniffles2 SV, ROH/UPD, samtools metrics/idxstats, MultiQC, final MultiQC HTML, and DayOA evidence manifest. | BLOCKED | contract_test | Gate 5 | orchestrator | Target list excludes Inflection delivery. Includes `produce_hiomrs_segdup`, `produce_sentdhiomr_cnv`, `produce_hiomrs_cnv_vcf`, `produce_hiomrs_sniffles2_sv_vcf`, `produce_hiomrs_roh_upd`, `produce_samtools_metrics`, `produce_multiqc_all`, `results/day/hg38/reports/DAY_final_multiqc.html`, and `results/day/hg38/reports/dayoa_evidence_manifest.json`. At `2026-07-21T14:00:38Z`, old live SegDup controller PID `4019126` still active with 242 running SegDup jobs and repeated LPA failures. | Starting a second `dy-r -n` in the same active analysis root would collide with the live controller/lock; canceling live Slurm work requires explicit user approval. | Await old controller exit or explicit cancel approval before deploying the patch and sending the corrected `-n` command. |
