# Take15 HG003 + NA23687 HIOMR2 `13.0.88` execution ledger

**Created:** 2026-07-30T08:51:57Z  
**Controlling request:** release the raw-5x-FASTQ comparison correction and rerun
the Take15 full-coverage HIOMR2 command under RnD.

## Frozen execution contract

- Fresh analysis root: `/fsx/analysis_results/preval-hiomr2/take15-hg003-13088`.
- DayOA source: annotated `13.0.88`, tag object
  `b4737b5bfdd3896f6a3468faeaadd5fdf8f0aaf7`, peeled commit
  `d2e798711297b74071b3068f59ef13e76a6caffd`.
- Runtime: `hg38`; full Bjuice Illumina and ONT FASTQs in `[0,25)`, with no
  SR or ONT subsampling.
- Samples: HG003 (male) and NA23687 (female). The NA23687 female mapping is
  the user-selected runtime overlay, supported by the
  [Coriell NA23687 record](https://catalog.coriell.org/0/Sections/Search/Sample_Detail.aspx?Product=DNA&Ref=NA23687); source metadata is not rewritten.
- Five configuration-owned scopes are `1-3`, `4-8`, `9-15`, `16-20`, and
  `21-25`, covering chr1-22, X, Y, and MT.
- Targets: `produce_sentdhiomr2_kitchensink`, `produce_snv_concordances`,
  and `produce_sentdhiomr2_inflection_analytical_package`; delivery batch
  `take15`.
- Attribution: `DAY_PROJECT=RnD` and `DAYLILY_COST_CENTER=RnD`. Bjuice is
  input provenance only.

## Supersession boundary

`take15-hg003-13087` and `/home/ubuntu/t15s_13087` are superseded evidence.
The old root was never created and no workflow/controller was launched there;
the partially staged home payload is deliberately left untouched. This run
uses only the fresh `13088` root and fresh `t15s_13088` stage.

## Gate 0 evidence

- On the interactive headnode as `ubuntu`, `take15-hg003-13088` and its
  write lock were absent; no matching process or Slurm job was present.
- Both required run mounts are AVAILABLE: `20260618_LH01106_0011_A23MFMCLT3`
  and `pca100-2026`.
- RnD is active, allows `ubuntu`, and has an unchanged USD 500 monthly cap.
  Its July usage snapshot is USD 0 (latest processed 2026-07-19T10:00Z).
- `13.0.88` is a pushed annotated tag; source test evidence is `38` focused
  comparison/alignment tests and `116` HIOMR2 tests passing.

## Fresh manifest receipt

Generated with `dyec catalog config-bjuice-preval` for only HG003 and
NA23687, analysis label `TAKE15`, using the full Bjuice inputs and blank SR/ONT
subsample fields. `dyec identities validate` accepted the exact six-manifest
topology: two specimens, two samples, four libraries, eight sequencing inputs,
two analysis units, and eight analysis-unit links. Foreign-key, ordering,
no-identity-rewrite, no-network, and provider-neutral invariants are true.

| File | SHA-256 |
|---|---|
| specimens.tsv | `d3ffea1551f338aaf635f73f01127db0616eadc3b78a626b636f231a56bd6a60` |
| samples.tsv | `ae662279f16e04cc093b2fbc515c9b0e79003a765903d1d31a62f773937e8f95` |
| libraries.tsv | `381ba5aec7a758bd15c480cc6ddf143e2e01efaefc722f02e51b6384688c6fbd` |
| sequencing_inputs.tsv | `c1d5d2d533735b3d92f5f5a69ad7c64930df6b67cbd4a83a65d4eb7d0cfc8667` |
| analysis_units.tsv | `ac945e104b62f4e381c9460b9dbd4942416c88f1d135d20d297db7a764016b09` |
| analysis_unit_inputs.tsv | `28406e29d9efd1093ce34ac61f6816b4cc63b9f63c50bb36d520f22addb0f213` |
| generator receipt | `689b79237878e172cadd1628796a857e30bba4d327ada0e2a1843466d36d1f44` |
| validation receipt | `bf1e918ebeb55585db4e0ec0f1489bbf4e8c6a3916ae9596cd15fd12aab34a62` |
| Take15 overlay | `c06ab2df7fa7f26c33620ea13d0d45a92717e85c5c4e911e7c9332021577f238` |

## Control ledger

| ID | Requirement | Status | Evidence / terminal note |
|---|---|---|---|
| P88-001 | Release the raw 5x slim-data FASTQ comparison correction. | SUCCESS | `13.0.88` at `d2e79871`; source fixture and CLI runner use raw 5x ILMN/ONT FASTQs. |
| P88-002 | Verify current mount availability, root/lock/controller absence, and RnD authorization. | SUCCESS | Gate 0 evidence above. |
| P88-003 | Generate and validate a new two-sample six-manifest set. | SUCCESS | `20260730T085157Z_take15_hg003_na23687_13088_manifests`; all required invariants are true. |
| P88-004 | Create fresh owned root, record write visit, acquire lock, and clone exact `13.0.88`. | SUCCESS | Empty-root bootstrap, write visit and write lock; `day-clone` reported `13.0.88` at `d2e798711297b74071b3068f59ef13e76a6caffd`. |
| P88-005 | Stage only declared manifests, receipts, and overlay through supported SSM text helpers, then guarded-copy into clone. | SUCCESS | Fresh `/home/ubuntu/t15s_13088`; all ten copied payload hashes matched this ledger's receipt. |
| P88-006 | Run the exact `dy-r` dry run. | SUCCESS | `RETURN CODE: 0`; 204-job graph with the requested hg38/RnD/take15 inputs and targets. |
| P88-007 | Accept the rendered graph. | FAILED | Missing HG003 `rtg_vcfeval_roi` comparison job despite supplied truth data. |
| P88-008 | Submit the accepted identical command without `-n`. | NOT RUN | Gate P88-007 failed; no workflow job or controller was submitted. |
| P88-009 | Retain lock through terminal controller evidence. | SUCCESS | Dry-run controller returned `0`; no matching Slurm job or controller remained, then the owned lock was released. |

## Dry-run disposition

The locked `13.0.88` root was cloned at the exact peeled commit, and the
guarded copy reproduced every staged manifest, receipt, overlay, and initial
ledger hash exactly. `source dyoainit` and `dy-a slurm hg38` completed in the
fresh one-window/one-pane Ubuntu tmux session.

The required `dy-r ... -n` command returned `0`. Its graph had two active
analysis units, ten `sentdhiomr2_scoped_shard_gvcf` jobs, two analytical
package manifests under `take15`, no legacy `sentdhiomr` rule, and only
60- or 240-minute declared allocations.

It did **not** pass acceptance. Although HG003's supplied GIAB directory was
present with ROI subdirectories and the diagnostic graph reported
`CONCORDANCE_SAMPLES=['HG003-4xvrg7erk7knfs']`, it instantiated only the
aggregate `produce_snv_concordances` rule and no `rtg_vcfeval_roi` job. The
cause is the HIOMR2 hard-VCF tuple omission recorded in the source release
ledger for `13.0.89`. NA23687 correctly remained without a truth path; no
comparison input was fabricated.

## Retry boundary

The rejected `13088` root is not a candidate for a live rerun. The next retry
must use a fresh root from annotated `13.0.89`, whose targeted hard-VCF
concordance regression suite passed, and must repeat the same accepted
dry-run gate before any submission.

## Deliberate non-actions

- No budget-cap increase, budget bypass, scheduler action, export, deletion,
  or infrastructure mutation is authorized.
- The raw-5x FASTQ comparison fixture is a source test contract; this Take15
  execution remains full-coverage Bjuice input and is not a slim-data run.
