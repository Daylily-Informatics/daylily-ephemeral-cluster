# HG002 5x/5x closeout, export, and Take101 full-coverage execution ledger

- Created: `2026-08-02T22:12:50Z`
- Human requestor: John Major
- Cluster: `preval-hiomr2` (`lsmc`, `us-west-2`)
- Completed-run analysis root: `/fsx/analysis_results/preval-hiomr2/13-4-0`
- Planned Take101 analysis root: `/fsx/analysis_results/preval-hiomr2/take101`
- Released DayOA baseline: annotated tag `13.4.1`, commit `9d8d1a52ae84bb38c1e9fca8c037ee4d2b4c18ec`
- Prior run ledger: `docs/plans/20260802T101851Z_hg002_5x5x_hiomr2_1340_kitchensink_ledger.md`

## Objective

Close the terminal HG002 5x Illumina plus 5x ONT HIOMR2 kitchen-sink, mega, and Inflection analytical-package run with an evidence-backed technical report, runbook, verified complete FSx-to-S3 export, and Slack handoff. Only after that handoff, create Take101 at exact DayOA `13.4.1` using proven full-coverage HG002 and one proven SMN12-positive-control Illumina plus ONT `[0,25)` inputs; pass the exact `-j 333 -p -k -T 1 -n` preflight, launch the identical live command without `-n`, notify Mike Kennemer and John Major, and leave exactly one 20-minute same-thread monitor.

## Gate 0 baseline

- The prior ledger records terminal `RC=0`, an empty Slurm queue, no remaining analysis write lock, final MultiQC job `3369` completed `0:0`, and final MultiQC SHA-256 `a5ba27d9d2207b30b759389fd23b51fc9c269781f1e486abef9240cb94e5af05`.
- The prior ledger records Inflection analytical packaging job `3364` completed `0:0` with schema `dayoa.hiomr2_inflection_analytical_package/1.3`, 37 artifacts, and `nicu_research_included=true`.
- The prior ledger records branch `codex/13.4.1-hg002-hiomr2-repairs`, pushed release commit `9d8d1a52...`, and pushed annotated non-v tag `13.4.1`.
- The prior heartbeat automation was deleted. No recurring automation may be created until the Take101 live controller exists; then exactly one 20-minute heartbeat is allowed.
- Existing unrelated tracked and untracked work in this DYEC checkout is excluded. Only this ledger and the exact `20260802T221250Z_hg002_5x5x_closeout_take101_artifacts/` report bundle may be staged unless a later proven DYEC change is explicitly required.

## Execution ledger

| ID | Scope | Gate | State | Evidence / exit criteria | Notes |
|---|---|---:|---|---|---|
| CLOSE-001 | Reverify completed run | 0 | SUCCESS | Current headnode, tmux, controller, Slurm, lock, tag, MultiQC, package, benchmark, and receipt state independently match the prior closure record | Read-only except required visit logs; FSx capacity also passed with 6.2 TB free and balanced 45-46% OST use |
| CLOSE-002 | Metric inventory | 1 | SUCCESS | Complete inventory of `ROI==giabHC` SNV hard-VCF F-scores, Sentieon and SMN12 copy-number evidence, every Truvari output/receipt, and explicit gaps | Durable audit TSVs preserve completion artifacts, SMN12 evidence, and 13 Truvari/alias/NICU rows without treating raw diagnostic summaries as published metrics |
| CLOSE-003 | Runbook | 1 | SUCCESS | Checked technical run instructions written into the completed analysis root | Exact eight-target `dy-r` command, config, selectors, tmux/init sequence, lock contract, completion checks, and stale-banner caveat recorded in `HG002_5x5x_HIOMR2_13.4.1_RUNBOOK.md` |
| CLOSE-004 | Technical report | 1 | SUCCESS | Canonical report artifact and verified self-contained HTML written into the analysis root; exact evidence paths and limitations included | Browser verification passed at 1440 and 390 px with 14 blocks, one chart, four tables, and keyboard source interaction; all six report-bundle hashes match local-to-FSx |
| CLOSE-005 | FSx to S3 export | 2 | SUCCESS | Supported export completes and FSx/S3 object, byte, and required-artifact reconciliation proves the requested root is complete | DRA `dra-0f7e6dd80654daad7`; task `task-0d9e23c42a8c0113c` `SUCCEEDED` `7,271/7,271`, zero failed; detached/deleted; S3 `7,273` objects and `108,432,011,155` bytes exactly match the file-plus-directory-marker source model and FSx logical bytes; required hashes match |
| CLOSE-006 | Completion Slack | 2 | SUCCESS | Mike Kennemer and John Major receive one message with S3 URI, report, MultiQC, DayOA `13.4.1`, 5x/5x scope, caveats, and full-coverage-next note | Sent to proven Michael Kennemer + John Major DM `D0AQK8RB3D5`: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785711593140789` |
| T101-001 | Input inventory | 3 | SUCCESS | Exact reviewed Bjuice source hashes match Take21; the three SMN controls are NA19235 (4/0 concordant), NA20775 (3/1 primary, Sentieon discordant), and NA23687 (1/2 SMA carrier); NA23687 selected explicitly. Fresh provider-neutral Take101 manifests validate with 2 samples, 4 libraries, 8 sequencing inputs, 2 units, 8 links, all 8 ILMN lane pairs and 3 x 146 ONT paths per sample | Runtime `use_fq_data_starting_hrs=0 use_fq_data_up_to_hrs=25` supplies half-open elapsed-hour window `[0,25)` and historically retains 150/438 ONT FASTQs per sample; manifest subsample fields remain blank |
| T101-002 | Fresh Take101 boundary | 3 | SUCCESS | Fresh root, visit/lock, one-pane persistent Ubuntu Bash-login tmux, `day-clone -t 13.4.1 -d take101`, exact tag proof, and explicit manifests/config | Root `/fsx/analysis_results/preval-hiomr2/take101`; tmux `dayoa_take101_fullcov_hg002_na23687_20260802`; exact annotated tag `13.4.1` at `9d8d1a52...`; all payload hashes verified before install |
| T101-003 | Dry run | 4 | SUCCESS | Exact eight-target `dy-r` closure passes with `-j 333 -p -k -T 1 --rerun-triggers mtime -n` | Candidate dry run passed `RC=0` with `466` jobs, two samples, two NICU Truvari jobs, both samples retaining `150/438` ONT FASTQs, no Slurm submission, and normal lock release |
| T101-004 | Live run | 4 | SUCCESS | Identical command with only `-n` removed completed the requested graph: the original controller reached `441/466`, the exact repair dry run passed, and the terminal repair-live controller completed the remaining `25/25` with durable `RC=0` | Repair controller PID `3420088` exited normally; every external job `3762`-`3775` completed `0:0`, the queue is empty, and the write lock is absent. Final MultiQC and both 37-artifact Inflection packages were independently verified. The seven live-proven repair files were committed as `53b43186f0b840a756c9151df1ff1b830033c139`, the branch was pushed, and annotated non-v tag `13.4.2` was created and pushed without moving a tag |
| T101-005 | Live-start Slack | 4 | SUCCESS | Mike and John receive one message that two full-coverage production-snapshot samples are underway with estimated 5-6 hour completion | Sent after real submission: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785721601092879` |
| T101-006 | Single monitor | 4 | SUCCESS | Exactly one same-thread heartbeat monitored at 20-minute cadence; every third heartbeat updated Mike/John; instructions distinguished lost-node retry from proven code failure | Automation `take101-fullcov-hiomr2-20-minute-monitor` remained singular through terminal acceptance and was deleted after live `RC=0`, release, package verification, terminal Slack, and local attention notification |

## Monitoring and repair boundary

- Monitor controller, Slurm, direct rule logs, receipts, and outputs read-only.
- Leave lost-node recovery to `-T 1`; do not cancel or requeue such jobs.
- Only after evidence proves a non-node-loss code defect and identifies the workflow-respawned replacement job may the explicitly authorized bounded kill be considered. Record a kill visit, hold the analysis-root kill/write lock, use `dyec analysis guard`, and touch only that exact replacement job.
- Diagnose source defects only after the controlling workflow has reached the relevant terminal failure boundary. Test any fix with focused tests and the exact mtime dry run before another live retry.
- Commit and push a repair branch and create the next annotated non-v semver tag only after live proof; never move an existing tag and never stage analysis inputs or logs.

## Closeout evidence recorded 2026-08-02T22:49:11Z

- Headnode: `i-0b70541bdad454c76` / `ip-10-0-0-138`; cluster `preval-hiomr2`; analysis root `/fsx/analysis_results/preval-hiomr2/13-4-0`.
- Reverification: controller absent, Slurm queue empty, tmux `dayoa_hg002_5x5x_1340_20260802` alive with one pane, write lock absent after report installation, annotated DayOA tag `13.4.1` peels to `9d8d1a52ae84bb38c1e9fca8c037ee4d2b4c18ec`.
- Capacity: FSx reports approximately `6.2 TB` available (`46%` used); data OSTs are balanced at `45-46%`. No historical-root export or deletion is required.
- Final MultiQC: `daylily-omics-analysis/results/day/hg38/reports/DAY_final_multiqc.html`, `10,341,154` bytes, SHA-256 `a5ba27d9d2207b30b759389fd23b51fc9c269781f1e486abef9240cb94e5af05`.
- Final MultiQC data: `14,296,219` bytes, SHA-256 `80c29469a71a2826a103f84cced2227500080e59589d5e314969d1a77a29a818`.
- Final evidence manifest: `323,836` bytes, SHA-256 `2e49a88de2b4d854c3d10634cbf9bb1ed5ce2913cdcc7237536c59f126a32b3d`.
- Final regenerated benchmark TSV: `81,455` bytes, SHA-256 `65176c21073cea7150ef27a210ac9779f6e3df375f0e3553a65993282d99939b`; this supersedes the earlier pre-final snapshot recorded in the prior ledger.
- Inflection package manifest: schema `dayoa.hiomr2_inflection_analytical_package/1.3`, `37` artifacts, NICU included, analytical/research use only and not customer-release eligible.
- Portable report verification: `{ok:true}`, `14` blocks, `1` chart, `4` tables, keyboard source dialog passed, viewports `1440` and `390`.
- FSx report-bundle hashes:
  - runbook `e93ea9659e4e4f31d6567d06cce2552ad2fa0f6893b639dcd57c38f5a8b3f69e`
  - report HTML `5139f4f669a42b234fb251e2c229afd354b0308753d080a72233cba181ff5209`
  - artifact JSON `621b2520d4e50ad7d077d80658341ee33177ce0b00b3860f509a21ce87a16ad3`
  - completion receipt `9470de1c3d800e22fd8ec31eae6737f2441f5bf8eb2817afd82993f57a515b66`
  - SMN12 audit `e1f16d06797024f27705ee5f2245cc1d8679fda6d9bcdadfb123c04b8963a1ef`
  - Truvari audit `c1c135672c01b89ef107f3b6622d79d8def59fdd183f71787beb4956559cce65`
- Export receipt: `docs/plans/20260802T221250Z_hg002_5x5x_closeout_take101_export/fsx_export.yaml`, schema `4`, status `success`, task `task-0d9e23c42a8c0113c`, DRA `dra-0f7e6dd80654daad7`, `delete_data_in_file_system=false`, detach lifecycle `DELETED`.
- S3 reconciliation: `s3://lsmc-ssf-sequencing-data/derived/preval-hiomr2/13-4-0/` contains `7,273` objects and `108,432,011,155` bytes. The count equals `6,298` regular files plus `975` non-root directory markers; bytes exactly equal the FSx logical-file baseline. S3-streamed SHA-256 values match FSx for the report HTML, report artifact, runbook, final MultiQC HTML/data, evidence manifest, final benchmark summary, and package manifest.
- Completion Slack: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785711593140789` sent only after export reconciliation to Michael Kennemer (`U0AQXA08V6Z`) and John Major (`U08TN63K73M`).

## Take101 input receipt recorded 2026-08-02T23:04:00Z

- Reviewed Bjuice source hashes match the prior accepted Take21 contract: `source_manifest_resolved.json` `f03b5f39...`, `run_evidence_v2.json` `363c2186...`, library/run matrix `3da41aca...`, sample snapshot `4ce4f1ee...`, and unit snapshot `c0af9f31...`.
- The three reviewed SMN copy-number controls are NA19235 (expected and observed SMN1/SMN2 `4/0`, two-caller concordant), NA20775 (expected `3/1`, primary SMNCopyNumberCaller `3/1`, Sentieon `3/2`), and NA23687 (expected and raw observed `1/2`, `isCarrier=true`, `PASS:Majority`). NA23687 is selected because it is the positive SMA-carrier boundary and has an already accepted full-coverage two-sample manifest lineage.
- Fresh artifacts: `docs/plans/20260802T221250Z_take101_hg002_na23687_fullcov_manifests/`. `dyec identities validate` passed foreign keys, ordered inputs, provider neutrality, and no identifier creation or rewriting. The two analysis-unit and delivery EUID fields remain blank, so this research run is not customer-release eligible.
- Exact manifest hashes: specimens `da22e23...`, samples `b673dba1...`, libraries `4e835d2f...`, sequencing inputs `b128e357...`, analysis units `a709bca2...`, input links `2ffd5038...`; generator receipt `8aa479f2...`, validation receipt `d3f823d7...`, overlay `f8143397...`.
- HG002 and NA23687 each have all eight Illumina R1/R2 lane pairs, blank SR/ONT subsample fields, and three reviewed ONT runs with 146 FASTQ paths each (438 paths total). The DayOA runtime bounds, not manifest subsampling, implement `[0,25)` with `use_fq_data_starting_hrs=0` and `use_fq_data_up_to_hrs=25`; the previously accepted equivalent graph retained 150/438 paths per sample.

## Take101 preflight and repair progress recorded 2026-08-03T01:37:36Z

- The fresh analysis root is `/fsx/analysis_results/preval-hiomr2/take101`. The persistent tmux `dayoa_take101_fullcov_hg002_na23687_20260802` has exactly one interactive Ubuntu Bash-login pane. `day-clone -t 13.4.1 -d take101 --executing-entity preval-hiomr2` succeeded, and the clone was proven to be exact annotated tag `13.4.1`, commit `9d8d1a52ae84bb38c1e9fca8c037ee4d2b4c18ec`.
- `source dyoainit` and the separate `dy-a slurm hg38` activation succeeded. The initialized tmux explicitly restores `DAY_PROJECT=RnD`, `DAYLILY_COST_CENTER=RnD`, and `SEQONE_DELIVERY_BATCH_ID=take101` before any `dy-r` invocation.
- Dry preflight 1 stopped before formal Snakemake execution because the minimal overlay had not enabled the required Truvari contract. The corrected explicit overlay SHA-256 is `2b204bae7f87e1057a421dfaa79f7729a514c0306804978513b42efb7caf23f5` and includes the proven GIAB SV v5.0q truth and NICU/package settings.
- Dry preflight 2 also stopped before formal execution, after again proving that both HG002 and NA23687 retain exactly `150/438` ONT FASTQs for elapsed hours `[0,25)`. Its terminal graph error was `sentdhiomr2_nicu_truvari`: the public all-active-sample NICU aggregate required a mandatory HG002-only GIAB SV benchmark artifact for NA23687. No controller or Slurm job was created by either failed preflight.
- Exact source inspection proved an internal graph/schema mismatch, not an input defect: `_hiomr2_nicu_manifest_paths()` expands all active samples, while `_hiomr2_nicu_artifact_paths()` mandates the two Truvari roles for every sample and `_hiomr2_nicu_truth_sample()` previously rejected every non-HG002 analysis unit during DAG construction.
- The write lock was acquired normally and the exact-tag clone was placed on branch `codex/13.4.2-take101-multisample-nicu-warning`. The bounded patch SHA-256 is `1b65d145f7b5fd6572a08b05ad9c4bdc0eafe94dff2593ecb80242f026bd1e51`; it changes four tracked files. HG002 still runs real GIAB SV v5.0q Truvari. Non-HG002 samples now publish an explicit `NOT_APPLICABLE` warning receipt with the query and truth VCF hashes, empty metrics, no attempted command, and no fabricated precision/recall/F1, while the rest of NICU, MultiQC, and Inflection packaging remains in scope.
- Focused tests pass `58 passed in 1.02s` in the exact headnode `DAY-EC` test runtime. Functional Ruff checks (`E9,F63,F7,F82`), the new CLI help path, and `git diff --check` pass. The first test invocation used the workflow runtime, which intentionally lacks pytest; that invocation is preserved separately and is not a code-test failure.
- The exact candidate dry/live harness SHA-256 is `b844dc48d956d7be7ca74b390fbad69135df5bcc4982760d133a3179a0792a4e`. It preserves the original eight targets and every option; `dry` adds only `-n`, and `live` removes only `-n`.
- The exact candidate dry run completed `RC=0` at `2026-08-03T01:41:18Z`. Its 466-job DAG contains both requested full-coverage samples and two `sentdhiomr2_nicu_truvari` jobs; the rendered non-HG002 shell path includes `truvari-not-applicable` with expected analysis unit `HG002-ck2ky6p4wk6exh`. The command line contains the exact eight targets, `[0,25)` selectors, `-j 333 -p -k -T 1 --rerun-triggers mtime -n`. It submitted no Slurm job and released the write lock normally.
- The write lock was reacquired normally and the identical live command with only `-n` removed was accepted at `2026-08-03T01:45Z`. Controller PID `3132158` is running from log `take101_hg002_na23687_fullcov_13.4.2_candidate_live_20260802.log`; initial external jobs `3370`-`3375` include both HG002 and NA23687 and were accepted under Slurm comment/cost center `RnD`.
- The required live-start Slack update was sent to the proven Michael Kennemer + John Major DM only after actual job submission: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785721601092879`.
- Exactly one active same-thread heartbeat exists: `take101-fullcov-hiomr2-20-minute-monitor`, 20-minute cadence, with every third check routed to the same Slack DM. The only other automation file is a paused 45-minute heartbeat for a different completed Take4 task.

## Take101 monitoring heartbeats

- Durable heartbeat number: `18`

| Heartbeat | Observed UTC | Controller / tmux | Workflow and Slurm state | Direct evidence | Capacity | Slack cadence |
|---:|---|---|---|---|---|---|
| 1 | `2026-08-03T02:11:46Z` through `02:12:29Z` | PID `3132158` alive after `1,616` seconds; exactly one matching controller; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `39/466` steps (`8%`) with `44` external submissions; `sacct` has `29 COMPLETED`, `15 RUNNING`, and no failed/cancelled/timeout/OOM/node-fail/preempted rows; both samples have advanced into QC and SV work | Active rule log files were inspected; no controller error marker. HG002 Sniffles2 Truvari emitted the explicitly accepted warning receipt `status=DIAGNOSTIC_FAILED`, `exit_code=0`, because projected `18,269` versus compared `18,262` records differed by seven; the workflow continued under the human-approved warn policy | FSx `5.3T/12T` used, `5.9T` available (`48%`); OSTs balanced `48-49%` | Not due; progress Slack is sent on heartbeat 3 |
| 2 | `2026-08-03T02:29:47Z` through `02:39:49Z` | PID `3132158` alive and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Final snapshot: main log `75/466` steps (`16%`); queue has `60 COMPLETED`, `46 RUNNING`, `10 CONFIGURING`, and `10 PENDING`. Four deterministic failures are the initial and `-T 1` attempts for both relatedness contexts: jobs `3419`/`3421` (`sentdhiomr2lr/na`) and `3477`/`3479` (`sentdhiomr2sr/smd`), all exit `1`. Ten HG002 segdup job IDs separately recorded `NODE_FAIL`; `scontrol` proved the same IDs retained `Requeue=1` and moved to pending/running recovery, with no agent intervention | Direct rule logs and an isolated headnode reproduction prove the relatedness defect: Somalier `0.3.2` exits `0` and logs that it wrote all outputs, but creates only `cohort.html` (`24,263` bytes), `cohort.pairs.tsv` (`281`), and `cohort.samples.tsv` (`588`) while omitting declared `cohort.groups.tsv` when no cancer groups exist; DayOA's fail-closed spool publisher then rejects the missing file. Repair is deferred until the active controller reaches its normal terminal boundary and releases the lock | FSx remains `5.3T/12T` used, `5.9T` available (`48%`); OSTs balanced `48-49%` | Urgent serious-problem update sent and corrected in Mike/John DM: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785724428783629`; required local `ATTENTION JOHN` alert and correction issued |
| 3 | `2026-08-03T02:49:44Z` | PID `3132158` alive after `3,894` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `122/466` steps (`26%`) with `146` external submissions; `sacct` has `105 COMPLETED`, `37 RUNNING`, and only the four previously proven relatedness failures. Of the ten segdup job IDs that recorded node loss, jobs `3488`, `3490`, and `3497` completed and the other seven were running; no recovery job remained pending, and no agent intervention occurred | Active direct rule logs were inspected with no new controller error marker. No new receipt was written in the preceding 25 minutes; the previously accepted HG002 Sniffles2 Truvari warning receipt remains unchanged | FSx `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Required third-heartbeat progress update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785725400644499` |
| 4 | `2026-08-03T03:09:27Z` through `03:10:02Z` | PID `3132158` alive after `5,077` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `161/466` steps (`35%`) with `158` external submissions; `sacct` has `141 COMPLETED`, `13 RUNNING`, and only the four previously proven relatedness failures. All ten segdup job IDs that recorded node loss are now `COMPLETED 0:0`; no recovery job is pending, and no agent intervention occurred | Active direct rule logs were inspected with no new controller error marker. HG002 TIDDIT Truvari wrote a new terminal receipt with `status=SUCCESS` and `exit_code=0` | FSx remains `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Not due; next cadence progress Slack is heartbeat 6 |
| 5 | `2026-08-03T03:29:05Z` | PID `3132158` alive after `6,255` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `169/466` steps (`36%`) with `163` external submissions; `sacct` has `148 COMPLETED`, `11 RUNNING`, and only the four previously proven relatedness failures. The recovered segdup set remains fully completed | Active direct logs for all 11 running rules were inspected with no new controller error marker. The most recent receipt remains HG002 TIDDIT Truvari `status=SUCCESS`, `exit_code=0` | FSx remains `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Not due; next cadence progress Slack is heartbeat 6 |
| 6 | `2026-08-03T03:49:17Z` | PID `3132158` alive after `7,467` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `176/466` steps (`38%`) with `166` external submissions; `sacct` has `154 COMPLETED`, `8 RUNNING`, and only the four previously proven relatedness failures. No node-loss recovery regressed | Active direct logs for all eight running rules were inspected with no new controller error marker. No new receipt was written in the preceding 25 minutes | FSx remains `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Required sixth-heartbeat progress update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785728970804309` |
| 7 | `2026-08-03T04:09:04Z` | PID `3132158` alive after `8,654` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `178/466` steps (`38%`) with `167` external submissions; `sacct` has `156 COMPLETED`, `7 RUNNING`, and only the four previously proven relatedness failures. The longest active rule is HG002 Sentieon/Sniffles at `2:03:32` of its `4:00:00` limit | Active direct logs for all seven running rules were inspected with no new controller error marker. No new receipt was written in the preceding 25 minutes | FSx remains `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Not due; next cadence progress Slack is heartbeat 9 |
| 8 | `2026-08-03T04:29:05Z` through `04:29:38Z` | PID `3132158` alive after `9,855` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | State is materially unchanged: main log `178/466` steps (`38%`) with `167` external submissions; `sacct` has `156 COMPLETED`, `7 RUNNING`, and only the four previously proven relatedness failures. The longest active rule is HG002 Sentieon/Sniffles at `2:23:33` of its `4:00:00` limit | Active direct logs for all seven running rules were inspected with no new controller error marker. No new completion or receipt was recorded during this interval | FSx remains `5.5T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Not due; next cadence progress Slack is heartbeat 9 |
| 9 | `2026-08-03T04:49:14Z` | PID `3132158` alive after `11,064` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log `222/466` steps (`48%`) with `236` external submissions; `sacct` has `198 COMPLETED`, `34 RUNNING`, and only the four previously proven relatedness failures. NA23687 hybrid CLI completed `0:0`, its terminal validation and command manifest completed, and chromosome-VEP fanout is active. The longest rule is HG002 Sentieon/Sniffles at `2:43:42` of its `4:00:00` limit | Active direct logs were inspected with no new controller error marker. The new NA23687 command manifest is `38,431` bytes | FSx reports approximately `5.6T/12T` used, `5.7T` available (`50%`); OSTs balanced `49-51%` | Required ninth-heartbeat progress update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785732574411009` |
| 10 | `2026-08-03T05:09:08Z` through `05:12:06Z` | PID `3132158` alive after `12,258` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Accounting snapshot: `299 COMPLETED`, `38 RUNNING`, and `6 FAILED`; main log then advanced from `341/466` (`73%`) to `352/466` (`76%`). Jobs `3633`/`3635` are the initial and `-T 1` retry attempts for the third relatedness context, `sentdhiomr2rsr/na`; its direct log proves the identical Somalier `0.3.2` missing-`cohort.groups.tsv` defect already diagnosed in the other two contexts. No new failure class or agent intervention occurred | New HG002 terminal receipts: hard-SNV Truvari and CNVscope Truvari are `SUCCESS`; LongReadSV Truvari is warning-only `DIAGNOSTIC_FAILED` with exit `0` because `19,849` projected versus `19,841` compared records differed by eight; CNVscope copy-state is warning-only `DIAGNOSTIC_FAILED` because truth projection was empty. Job `3394` HG002 Sentieon/Sniffles crossed three hours and is flagged `needs investigation`; a bounded point-in-time Glances sample at `03:05:46/04:00:00` showed `71.4%` user CPU, load `134.8/129.5/126.0`, `6.3%` memory use, and the supplemental node snapshot showed no swap, demonstrating active compute rather than a local or stalled job | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`); long-job node root is `63%` used and FSx is mounted normally | Not due; next cadence progress Slack is heartbeat 12 |
| 11 | `2026-08-03T05:30:00Z` through `05:31:01Z` | PID `3132158` alive after `13,510` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log reached `403/466` steps (`86%`) with `359` external submission markers; `sacct` has `352 COMPLETED`, `3 RUNNING`, and the same `6 FAILED` relatedness attempts only. At the accounting snapshot the active external rules were HG002 Sentieon/Sniffles, NA23687 Manta, and the newly started HG002 NICU caller-normalization stage after HG002 Manta completed; the active set is expected to change as downstream jobs become ready | All active direct stdout/stderr paths were inspected with no new error signature, and the recent log scan found no new error, traceback, OOM, kill, or no-space marker. The most recent receipts remain hard-SNV Truvari and CNVscope Truvari `SUCCESS`. Job `3394` remains `needs investigation` but compute-active: repeated bounded Glances samples around `03:24/04:00` showed `64.5-65.6%` user CPU, load approximately `127`, only `1.2%` memory use, no swap, and normal FSx mount; this remains point-in-time evidence, not a health guarantee | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`); long-job node root is `63%` used | Not due; required progress Slack is next heartbeat 12 |
| 12 | `2026-08-03T05:49:35Z` through `05:50:31Z` | PID `3132158` alive after `14,685` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log reached `413/466` steps (`89%`) with `369` external submission markers; `sacct` has `360 COMPLETED`, `3 RUNNING`, and the same `6 FAILED` relatedness attempts only. Active external work is HG002 Sentieon/Sniffles, NA23687 Manta, and HG002 Inflection analytical packaging; no new failure class or agent intervention occurred | Active direct stdout/stderr paths contained no new error signature. HG002 NICU Truvari wrote the approved warning-only `DIAGNOSTIC_FAILED` receipt with `exit_code=0` because `30,395` projected versus `30,388` compared records differed by seven. Job `3394` was still compute-active at `03:44:04/04:00:00`; bounded Glances samples showed `64.7-65.1%` user CPU, load approximately `128`, and `1.2-1.3%` memory use. Its proximity to the time limit remains under read-only observation | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`) | Required twelfth-heartbeat update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785736231241099` |
| 13 | `2026-08-03T06:09:19Z` through `06:21:33Z` | PID `3132158` alive after `15,869` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log reached `416/466` steps (`89%`) with `373` external submission markers; `sacct` has `363 COMPLETED`, `8 FAILED`, `1 TIMEOUT`, and `1 RUNNING`. The six existing failures remain the three Somalier relatedness initial/retry pairs. New jobs `3740`/`3741` are the initial/retry failures for NA23687 Manta caller normalization. HG002 Sentieon/Sniffles job `3394` timed out at `04:00:20/04:00:00`; `-T 1` started exact replacement job `3742`, which was running at `00:03:18`, with no agent or scheduler intervention | Direct log proves `VCF REF does not match the reference at chr7:154575459: vcf=N reference=R`. A pipe-only audit of all `155,191` source Manta records found exactly two affected records, both the same insertion at `chr7:154575458` with source `REF=GN`, canonical `REF=GR`, and sole mismatch `N→R`. This bounds the post-exit repair to an explicit, provenance-recorded Manta ambiguous-reference-anchor contract rather than a fallback. No code or analysis-root file was changed; repair remains deferred until normal controller exit and lock release | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`) | New serious-problem local `ATTENTION JOHN` alert issued; urgent Mike/John Slack sent: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785738093504519` |
| 14 | `2026-08-03T06:29:34Z` through `06:30:58Z` | PID `3132158` alive after `17,084` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | State counts are unchanged: main log `416/466` (`89%`), `373` external submission markers, and `sacct` has `363 COMPLETED`, `8 FAILED`, `1 TIMEOUT`, and `1 RUNNING`. Exact retry `3742` was running at `00:24:55/04:00:00`; no new failure, receipt, or controller error marker appeared | Direct rule log proves forward progress through read parsing and coverage traversal from chromosome 1 through chromosome 19. `sstat` reports `18:08:41` average CPU accumulated, approximately `151.98 GB` average disk read, and `301.5 MB` average disk write; the live Sniffles process was state `R` with approximately `6.25 GB` RSS and open BAM descriptors. Although a short Glances sample caught a low-CPU phase, the cumulative and rule-log evidence prove the job is not stalled. The bounded kill condition is therefore not met | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`) | Not due; required cadence Slack is heartbeat 15 unless an urgent new problem appears sooner |
| 15 | `2026-08-03T06:49:12Z` through `06:50:12Z` | PID `3132158` alive after `18,262` seconds and singular; one-window/one-pane tmux healthy; live RC absent; active write lock present as expected | Main log reached `420/466` (`90%`) with `379` external submission markers; `sacct` has `367 COMPLETED`, `8 FAILED`, `1` historical `TIMEOUT`, and `3 RUNNING`. Retry `3742` completed and opened downstream jobs `3745` Jasmine integrated, `3747` Jasmine alignment compare, and `3748` Sniffles1/Iris Truvari. No new terminal failure class appeared | Retry rule log reached `Finalizing` and published its partial output before completion. Sniffles1/Iris Sentieon Truvari wrote a new `SUCCESS`, exit-`0` terminal receipt. The completed Sentieon-refine log contains non-terminal per-event IRIS/racon and out-of-range `samtools faidx` exceptions; these are retained as a report caveat but did not create a Slurm/controller failure, and the downstream Truvari receipt succeeded | FSx remains approximately `5.6T/12T` used, `5.7T` available (`50%`) | Required fifteenth-heartbeat update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785739812074669` |
| 16 | `2026-08-03T07:09:18Z` through `07:40:17Z` | PID `3132158` was initially alive and singular, then exited naturally; durable receipt is `TAKE101_LIVE_RC=1` and its lock released normally. The same one-window/one-pane tmux remains healthy and now owns singular repair-live controller PID `3420088`; live RC is pending and the write lock is present as expected | Original log advanced from `431/466` to terminal `441/466` (`95%`) under `-k`. Terminal inventory confirmed only the six Somalier initial/retry failures and two NA23687 Manta-normalization initial/retry failures; Sentieon/Sniffles `3394` timed out but exact retry `3742` completed `0:0`. The exact repair mtime dry run then passed `RC=0` with `25` closure jobs. Identical live retry accepted external jobs `3762`-`3765`; all four were in normal node `CONFIGURING` state at the final snapshot, with no new failure or intervention | Patch SHA-256 `86a883f892384b47cb2dc882af02efcae8152c505ee28f070f2edd4052e88a23` applied cleanly. Focused tests pass `49` tests plus functional Ruff and diff checks. Actual Somalier validation reproduced the missing native groups output and materialized the declared `16`-byte header-only TSV only after the other three outputs passed. Actual NA23687 Manta validation preserved and reference-validated all `155,191` source records, repaired exactly two `GN` to canonical `GR` anchors, recorded both source IDs under receipt schema `1.2`, and produced prepared VCF SHA-256 `78f6a541...` | FSx remains approximately `5.6T/12T` used with `5.7T` available (`50%`) | Not due; next cadence progress Slack is heartbeat 18 unless an urgent new problem appears sooner. The existing automation was updated in place for PID `3420088`; no second scheduled monitor was created |
| 17 | `2026-08-03T07:43:06Z` through `07:46:02Z` | Repair-live PID `3420088` remains alive and singular; the one-window/one-pane tmux is healthy; live RC is pending and the write lock is present as expected | Closure advanced to `12/25` (`48%`). Somalier jobs `3762`-`3764` completed `0:0` in `14` seconds each, Manta normalization `3765` completed `0:0` in `31` seconds, provenance `3766` completed `0:0`, Jasmine `3767` completed `0:0`, and SURVIVOR `3769` completed `0:0`. OctopuSV `3768` is the only active job at `00:02:32/04:00:00`; no error marker or scheduler intervention occurred | All three live Somalier logs contain the explicit no-group materialization marker and all three published group outputs are `16` bytes. The live Manta receipt is schema `1.2`, `1,303` bytes, preserves all `155,191` records, and records exactly the same two repaired source IDs and prepared-VCF SHA-256 as the fixture proof. No new direct-log defect appeared | FSx remains `5.6T/12T` used with `5.7T` available (`50%`) | Not due; routine Mike/John Slack remains scheduled for heartbeat 18 |
| 18 | `2026-08-03T08:02:16Z` through `08:17:07Z` | Repair-live PID `3420088` exited normally; durable receipt is `TAKE101_LIVE_RC=0`; the one-window/one-pane tmux remains healthy; controller is absent and the write lock was released | Main log reports `25 of 25 steps (100%) done` and `RETURN CODE: 0`. Jobs `3762`-`3775` are all `COMPLETED 0:0`; OctopuSV `3768` completed in `10:06`, both NICU closure stages completed, both package stages completed, final MultiQC job `3775` completed, and Slurm is empty | Final MultiQC HTML is `12,196,815` bytes, SHA-256 `a61c9dca9e3a1d81b6c60bf656204e5461f94489752deaefede5c97d8666f55c`; its parsed JSON contains both analysis-unit IDs. HG002 and NA23687 package manifests each declare exactly `37` artifacts; all `74` files exist with zero declared-byte mismatches. The seven tracked repair files were committed/pushed as `53b43186...`; annotated tag `13.4.2` peels to that commit and was pushed. No untracked analysis inputs or logs were staged | FSx remains approximately `5.6T/12T` used with `5.7T` available (`50%`) | Terminal update sent to Mike/John: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785745076159719`; local `ATTENTION JOHN` notification succeeded; the heartbeat automation was deleted |

## Take101 terminal closure recorded 2026-08-03T08:17:07Z

- Final workflow state: requested eight-target Take101 graph satisfied, terminal repair controller `RC=0`, queue empty, controller absent, and analysis-root write lock absent.
- Final MultiQC: `results/day/hg38/reports/DAY_final_multiqc.html`, `12,196,815` bytes, SHA-256 `a61c9dca9e3a1d81b6c60bf656204e5461f94489752deaefede5c97d8666f55c`; data JSON `18,523,045` bytes, SHA-256 `e95634427983612827a99bdf78999fc8abf5c3ece0f7a02e21968b318f0233ce`.
- Inflection analytical packages: schema `dayoa.hiomr2_inflection_analytical_package/1.3`, `37` artifacts each for `HG002-ck2ky6p4wk6exh` and `NA23687-zsvw0fa30jhz3n`, NICU research included, analytical mode, and not customer-release eligible. Declared package bytes are `22,450,744,512` and `22,106,910,338`; all `74` artifact paths exist with zero size mismatches.
- Live-proven release: branch `codex/13.4.2-take101-multisample-nicu-warning`, commit `53b43186f0b840a756c9151df1ff1b830033c139`, exactly seven tracked files, `369` insertions and `34` deletions. The branch is pushed. Annotated non-v tag `13.4.2` is pushed and peels to the same commit; remote tag object is `796098d9ba27311e614e749154bc5e448c872c61`.
- Terminal Slack: `https://lsmchq.slack.com/archives/D0AQK8RB3D5/p1785745076159719`. The required local `say` notification succeeded. Automation `take101-fullcov-hiomr2-20-minute-monitor` was deleted after terminal acceptance.

## Terminal acceptance

- All ledger rows must be terminal.
- The 5x/5x closeout is not complete until verified S3 reconciliation and the first Slack handoff succeed.
- The Take101 launch objective is not complete until a real live controller is accepted, the second Slack message is sent, and exactly one 20-minute monitor is confirmed active.
- Terminal result: all ledger rows are terminal, the 5x/5x closeout/export/handoff is complete, and Take101 completed `RC=0` with final MultiQC, both Inflection analytical packages, pushed DayOA `13.4.2`, terminal Slack, and the completed monitor deleted.
