# Take101 full-coverage HIOMR2 report bundle

This bundle is the durable, reproducible technical report for the two-sample Take101 run at DayOA `13.4.2`. The canonical source is `TAKE101_HG002_NA23687_FULLCOV_HIOMR2_13.4.2_VALIDATION_REPORT.artifact.json`; the self-contained reader is the matching `.html` file.

Reviewed TSV receipts preserve coverage and fragment metrics, scoped-VCF treatment counts, HG002 GIAB hard-VCF F-scores, orthogonal SMN1/SMN2 evidence, every terminal Truvari/applicability result, live-proven repairs, package closure, primary artifact hashes, release provenance, and important stable S3 URIs. `build_take101_report.py` regenerates the canonical artifact from those receipts. The portable delivery receipt records validator, package, browser, responsive, and source-dialog verification.

The seven-day MultiQC share link is intentionally not embedded here because it is an ephemeral credential-bearing URL. It is generated and verified after the S3 object exists and retained in the separate post-export share receipt.
