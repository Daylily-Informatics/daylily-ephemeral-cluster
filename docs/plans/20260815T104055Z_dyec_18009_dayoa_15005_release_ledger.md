# DYEC 18.0.9 / DayOA 15.0.5 release amendment ledger

Controlling request: promote the isolated DYEC local-context implementation, pin every active
DayOA catalog command to the released DayOA `15.0.5`, publish both `lsmc-bio` releases, and
complete a final twelve-hour retained-work audit.

Related implementation ledger:
`docs/plans/20260815T081943Z_dyec_context_dayoa_1503_ledger.md`.

## Gate 0 — inventory freeze

- DYEC implementation worktree: `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster-dyec-context-18008`,
  branch `codex/dyec-context-dayoa-1503-18008`, based on annotated DYEC `18.0.8`
  (`67717bbaf4f994d3ce39bf4369af568ca12a645b`).
- This worktree contains only the previously identified, uncommitted local-context/catalog/docs/test
  implementation; the detached primary DYEC checkout and its unrelated untracked `TrusSV/` and
  `tmp/dayoa-ont-headnode-proof/` directories are excluded.
- DayOA input: annotated, pushed `15.0.5` ->
  `c4341516d1a228056b2331bb8524828d629dae27`; its GitHub release is published before the DYEC
  promotion. DayOA source and the dirty primary DayOA worktree are immutable/excluded here.
- Source and packaged catalog sweep before retargeting: 30 active top-level DayOA commands and
  29 `dyec_builds.current` commands, all targeting `15.0.3`; one current alias resolves through
  those commands. The canonical JSON SHA-256 of every historical `dyec_builds.*` block other than
  `current` was `1b92a412eb6a2630e5db31d5fd03c26a1e61868d6abc930bfca9fafd9e4728f2` in both mirrors.
- Scope excludes AWS, DayOA workflow execution, cluster operations, and release publication beyond
  the authorized GitHub tags/releases.

## Execution rows

| ID | Area | Requirement | Status | Category | Gate | Evidence / terminal note |
| --- | --- | --- | --- | --- | --- | --- |
| REL-001 | DayOA | Verify and publish the existing annotated `15.0.5` release. | SUCCESS | config_or_startup_contract | Gate 0 | `15.0.5^{}` resolves to `c4341516d1a228056b2331bb8524828d629dae27`; GitHub release was created at `https://github.com/lsmc-bio/daylily-omics-analysis/releases/tag/15.0.5`. |
| CAT-001 | DYEC catalogs | Retarget every active top-level and `dyec_builds.current` DayOA command, direct/default reference, and alias resolution to `15.0.5` in both mirrors. | SUCCESS | config_or_startup_contract | Gate 2 | Parser assertion: source/package byte-identical; `default_ref`, all 30 top-level commands, current tag list, and all 29 current commands resolve only to `15.0.5`. |
| CAT-002 | Catalog provenance | Preserve historical `dyec_builds.*` blocks and retain derived `validation_pending` semantics rather than relabeling validation proof. | SUCCESS | contract_test | Gate 2 | Historical checksum remained `1b92a412eb6a2630e5db31d5fd03c26a1e61868d6abc930bfca9fafd9e4728f2`; `18.0.8` still records `15.0.3`. All current commands correctly report pending validation because their retained proof predates `15.0.5`. |
| DOC-001 | DYEC docs | Update active DayOA target statements and reference the pending-validation meaning. | SUCCESS | feature_implementation | Gate 2 | Active target sweep passed for `README.md`, `docs/cli_reference.md`, `docs/pip_install.md`, and `docs/quickest_start.md`; each now says `15.0.5`. |
| TST-001 | DYEC tests | Update/add catalog pin assertions for `15.0.5`, current aliases, historical snapshots, local context, and public diagnostics. | SUCCESS | contract_test | Gate 5 | Focused suite: `514 passed`; final full suite: `2588 passed, 11 skipped`. Catalog tests prove explicit/current resolution, alias inheritance, pending state, preserved history, and the new DayOA pin. |
| TST-002 | DYEC regression test | Replace the stale literal `exclusive=""` assertion with the actual no-exclusive-rule behavior. | SUCCESS | contract_test | Gate 5 | `tests/test_packaged_defaults.py` now asserts absence of both `--exclusive` and the obsolete shell variable; the focused two-module run passed `56`. |
| TST-003 | DYEC regression test | Make the tracked `complete_genomics_solo_slim_mounted_reference_v1` fixture explicit in the staging inventory and assert its expected strict rejection until headnode-visible source paths are supplied. | SUCCESS | contract_test | Gate 5 | Initial runnable-fixture attempt correctly failed on non-headnode-visible `s3://` sources. The inventory now preserves the fixture and a regression test proves the precise strict rejection; no test was removed or skipped. |
| QA-001 | Local validation | Run lint, compile, YAML/catalog, documentation, and whitespace checks. | SUCCESS | contract_test | Gate 5 | `py_compile`, changed-code undefined-name lint, added-line length check, YAML parse, catalog invariants, documentation sweep, and `git diff --check` passed. Repository-wide Ruff remains pre-existing style debt (`2389` findings across untouched code), so no global autoformat was applied. |
| REL-002 | DYEC | Commit the isolated owned changes, merge the reviewed branch, create/push annotated `18.0.9`, and publish its GitHub release. | IN_PROGRESS | feature_implementation | Gate 5 | User explicitly authorized commit, push, tag, and `lsmc-bio` release. No tag may be moved or overwritten. |
| AUD-001 | Repositories | After publication, audit both repositories for unmerged, unstaged, stashed, or unreachable work modified in the preceding twelve hours; preserve all candidates and report exact findings. | IN_PROGRESS | plan_amendment | Gate 5 | Read-only audit only; no prune, restore, reset, or cleanup is authorized. |

## Acceptance record to complete before release

- Source/package catalog byte identity, YAML parse, and uniform active/current `15.0.5` target checks.
- Historical-block checksum remains
  `1b92a412eb6a2630e5db31d5fd03c26a1e61868d6abc930bfca9fafd9e4728f2`.
- Focused local-context/catalog tests, applicable full DYEC test suite, lint, and `git diff --check`.
- No DayOA/Snakemake/AWS workflow operation is run by this release amendment.
