# DYEC 18.0.12 / DayOA 15.0.6 release ledger

Status: complete

Created (UTC): 2026-08-16T00:35:53Z

## Objective

Advance every active DYEC command-catalog execution pin to the immutable DayOA
`15.0.6` release, preserve historical catalog snapshots and the existing DYEC
`18.0.11` tagged work, then publish and merge the next immutable DYEC patch
release.

## Gate 0: baseline and scope

- DayOA `15.0.6` is an annotated tag (`fb86ba72f9578f2776e4244b2570e50c7f37983e`)
  whose peeled release commit is `243763b3d293b1049129b82e3717b1225a1fd21c`.
  It is an ancestor of DayOA `origin/main` at
  `3555a6470b05d8dc14cc22d8e1730256ee628dad`.
- DYEC `origin/main` is `6e91ecf313e14145193d0d69bb97989ea2156f07`, the
  annotated `18.0.10` release commit.
- Existing DYEC `18.0.11` is an annotated tag whose peeled commit is
  `33ebc69856714a0c31e4c7ae79a50563d0b05276`; it remains immutable. Its
  existing branch is merged into this isolated candidate at
  `d474da2cbbae262cc2addfc566b1ddeb5545ccc7`, preserving the tagged commit as
  an ancestor of the candidate.
- `18.0.12` does not exist locally or on `origin`; it is the next candidate tag.
- The mutable active catalog surface is exactly:
  `repositories.daylily-omics-analysis.default_ref`,
  `dyec_builds.current.dayoa_git_tags`, and 29 current command `git_tag`
  entries. Historical DYEC build snapshots are immutable and excluded.
- Source catalog and packaged installer catalog are byte-identical at baseline.
- `validated_version` fields record prior workflow validation provenance and are
  not changed by this execution-pin release.
- No AWS, Slurm, headnode, or DayOA workflow action is in scope.
- Work is isolated in
  `/Users/jmajor/.codex/worktrees/dyec-release-18012-dayoa-15006`; the user's
  primary DYEC checkout and its unrelated untracked files are untouched.

## Execution rows

| ID | Action | Evidence | Status |
| --- | --- | --- | --- |
| BASE-01 | Verify DayOA release tag and DayOA main ancestry. | Tag type, peeled commit, and `merge-base --is-ancestor` recorded above. | complete |
| BASE-02 | Preserve DYEC 18.0.11 while starting from current main. | `18.0.11` tagged commit is an ancestor of candidate merge `d474da2c`. | complete |
| PIN-01 | Update source current catalog execution pins to DayOA 15.0.6. | Default ref plus 30 repository command entries, current tag list, and 29 current command entries are all `15.0.6`. | complete |
| PIN-02 | Mirror the identical update in packaged installer catalog. | Source and packaged catalogs are byte-identical after the update. | complete |
| TEST-01 | Validate active pin coverage, source/package parity, CLI/doc contract, and release version. | 54 catalog-contract, 60 CLI/output, 3 non-version operator-doc, and 15 exact-tag version/doc tests passed. Fatal syntax lint and diff checks passed. | complete |
| REL-01 | Commit candidate, create/push annotated `18.0.12`, and publish the GitHub release. | Candidate `5363958de23c4748f47e7e2226a1eec04aeb7103`; annotated tag `18.0.12`; published release URL recorded below. | complete |
| REL-02 | Merge the DYEC release PR normally to main. | PR #115 merged normally at `022f2da76cbc69abab4ef1a990506994b4826b80`; GitHub reported zero configured head-commit checks. | complete |
| VERIFY-01 | Verify both DayOA and DYEC releases and their mainline ancestry. | Both annotated release tags are ancestors of their respective current `origin/main` refs. | complete |

## Final verification

- DYEC `18.0.12` is an annotated tag that peels to
  `5363958de23c4748f47e7e2226a1eec04aeb7103`. The release is published at
  https://github.com/lsmc-bio/daylily-ephemeral-cluster/releases/tag/18.0.12.
  That commit is an ancestor of DYEC `origin/main` at
  `022f2da76cbc69abab4ef1a990506994b4826b80` after normal PR #115 merge.
- DayOA `15.0.6` is an annotated tag that peels to
  `243763b3d293b1049129b82e3717b1225a1fd21c`. The release is published at
  https://github.com/lsmc-bio/daylily-omics-analysis/releases/tag/15.0.6.
  That commit is an ancestor of DayOA `origin/main` at
  `3555a6470b05d8dc14cc22d8e1730256ee628dad`.
- The user's primary DYEC checkout was subsequently switched to the local
  `codex/active-dyec-18.0.12` branch at the exact release commit. `dyec version`
  reports `18.0.12`; its pre-existing `TrusSV/` and
  `tmp/dayoa-ont-headnode-proof/` untracked paths remain untouched.
- No AWS, Slurm, headnode, or DayOA workflow operation was performed.
