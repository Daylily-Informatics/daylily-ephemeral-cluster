# pcand-18015 current-catalog three-platform sequencing QC ledger

Created: 2026-08-16T08:09:46Z  
Controlling request: Attach read-only FSx DRAs for Bjuice prevalence Illumina and ONT source data plus a recent Ultima run on `pcand-18015`; run the current catalog's no-BCLConvert Illumina, ONT, and Ultima RunQC paths on `hg38`; after each terminal controller `rc=0`, export that analysis root to S3 and record the exported prefix in catalog evidence.

## Gate 0: inventory freeze

- Checkout: `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster`, detached at annotated DYEC tag `18.0.16` (`81d865e7`), with only the preserved task artifacts listed below untracked.
- Pre-existing, unowned changes: `TrusSV/`, `docs/plans/20260816T080536Z_pcan18013_solo_slim_kitchensink_ledger.md`, and `tmp/dayoa-ont-headnode-proof/`; this work does not modify them.
- Active CLI: DYEC `18.0.16`, catalog `dyec_builds.current`, with all three RunQC rows pinned to DayOA `15.0.9`, `hg38`, and `run_context`. The immutable release checkout supplies this exact `current` view.
- Initial cluster evidence at 2026-08-16T08:08:53Z: `pcan-18013` headnode `i-0ab6afb983a662ebc` was running as Ubuntu, but the ParallelCluster stack was `UPDATE_ROLLBACK_COMPLETE` / `UPDATE_FAILED` and compute fleet was `STOPPED`.
- Amendment at 2026-08-16T08:24Z: user redirected this task to `pcand-18015`. Current DYEC cluster inventory reports `pcand-18015` as `UPDATE_COMPLETE` with public IP `16.145.234.134`; its managed-mount inventory is empty. The three task-owned DRAs already creating on `pcan-18013` remain untouched; deleting or migrating them would be a separate destructive action requiring separate approval.
- Exact input scope: (1) ILMN `s3://lsmc-ssf-sequencing-data/basecalls/lsmc/ssf-hq/LH01106/2026/20260618_LH01106_0011_A23MFMCLT3/`; (2) canonical Bjuice ONT parent `s3://lsmc-ssf-sequencing-data/basecalls/lsmc/ssf-hq/pca100/2026/`, with `20260615_ONT_Set4-FC1` as the selected RunQC child; (3) recent Ultima `s3://lsmc-ssf-sequencing-data/basecalls/lsmc/ssf-hq/RUN604834/2026/604834-20260717_2309/`.
- No BCLConvert, basecalling, Slurm intervention, writeback mount, FSx cleanup, or DRA deletion is in scope.

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| AMD-001 | Plan amendment | Replace `pcan-18013` with user-selected `pcand-18015`; preserve prior live associations without deletion. | SUCCESS | plan_amendment | Gate 0 | Codex | User direction in this thread; current CLI lists `pcand-18015=UPDATE_COMPLETE`, no mounts. |  | Original target is superseded; its associations are not altered. |
| MNT-001 | Input mount | Original-target ILMN DRA on `pcan-18013`. | NO_LONGER_NEEDED | plan_amendment | Gate 1 | Codex | `dra-0654d57a45c4087da`, created `2026-08-16T08:10:42Z`, last observed `CREATING`; read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. The initial CLI attempt with explicit `--auto-export none` failed locally before AWS request. | User changed target cluster. | Preserved in place; not deleted or reused. |
| MNT-002 | Input mount | Original-target ONT-parent DRA on `pcan-18013`. | NO_LONGER_NEEDED | plan_amendment | Gate 1 | Codex | `dra-03742feb4fc43cf82`, created `2026-08-16T08:10:55Z`, last observed `CREATING`; read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. | User changed target cluster. | Preserved in place; not deleted or reused. |
| MNT-003 | Input mount | Original-target Ultima DRA on `pcan-18013`. | NO_LONGER_NEEDED | plan_amendment | Gate 1 | Codex | `dra-02b17359d092bb615`, created `2026-08-16T08:10:54Z`, last observed `CREATING`; read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. | User changed target cluster. | Preserved in place; not deleted or reused. |
| MNT-004 | Input mount | Create and await the read-only ILMN Bjuice DRA on `pcand-18015`. | SUCCESS | feature_implementation | Gate 1 | Codex | `dra-084eb2762ff778f5b`, created `2026-08-16T13:09:13Z`, became `AVAILABLE`; FSx `fs-0dd162cc74c29ddc9`, read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. Required run metadata and InterOp files are readable. |  | Retained read-only for analysis; no deletion. |
| MNT-005 | Input mount | Create and await the read-only canonical Bjuice ONT-parent DRA on `pcand-18015`. | SUCCESS | feature_implementation | Gate 1 | Codex | `dra-02df9745cdda264e6`, created `2026-08-16T13:09:14Z`, became `AVAILABLE` at `2026-08-16T14:09:50Z`; FSx `fs-0dd162cc74c29ddc9`, read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. Selected child proof found one 5,048,955,428-byte sequencing-summary file, 944 `*.fastq.gz` files, and 47 barcode/unclassified group directories. |  | Retained read-only for analysis; no deletion. |
| MNT-006 | Input mount | Create and await the read-only recent Ultima DRA on `pcand-18015`. | SUCCESS | feature_implementation | Gate 1 | Codex | `dra-04baed6035bb4842e`, created `2026-08-16T13:09:13Z`, became `AVAILABLE`; FSx `fs-0dd162cc74c29ddc9`, read-only, batch import true, auto import `NEW,CHANGED`, auto export `[]`. Read-only proof found `604834_LibraryInfo.xml`, `604834_SequencingInfo.json`, `UploadCompleted.json`, both merged trimmer CSVs, and 1,886 JSON files. |  | Retained read-only for analysis; no deletion. |
| QC-001 | Catalog | Render, dry-run, and live-launch current `illumina_run_qc` only after its DRA and headnode path verify. | SUCCESS | feature_implementation | Gate 2 | Codex | ILMN DRA became `AVAILABLE`; read-only proof found readable `RunInfo.xml` (79,277 bytes), `RunParameters.xml` (3,562), `SampleSheet.csv` (3,197), and 23 top-level InterOp files. First DayOA `15.0.8` dry ID `pcand18015_ilmn_runqc_1508_dry_20260816T1315Z` failed before dispatch (`rc=1`, zero Slurm submissions) because the shared DAYOA Conda environment was corrupt: package metadata claimed Node/ICU payloads that were absent. The 158 MB corrupt environment was preserved at `/home/ubuntu/miniconda3/envs/DAYOA.corrupt-20260816T1337Z`; a clean 1.7 GB environment was rebuilt from the immutable `15.0.8` contract and passed Node `v22.23.1` / Mermaid `11.15.0` readiness. Fresh `15.0.8` dry ID `pcand18015_ilmn_runqc_1508_dry2_20260816T1340Z` then failed closed before dispatch (`rc=25`, zero submissions) on nonignored established runtime inputs. DayOA `15.0.9` fixed that release contract; dry ID `pcand18015_ilmn_runqc_1509_dry_20260816T1351Z` succeeded with attributable `rc=0` and zero submissions. First live ID `pcand18015_ilmn_runqc_1509_live_20260816T1353Z` failed before submission (`rc=1`) because no `--cost-center` was supplied and sbatch validated nonexistent `pcand-18015`; read-only registry proof found the exact provisioned active row `pcand-18015-ccenter`, cap $800, allowed user `ubuntu`. Fresh live ID `pcand18015_ilmn_runqc_1509_live2_20260816T1404Z` explicitly selected that unchanged row, submitted five jobs, and reached terminal `SUCCEEDED` with attributable `rc=0`; no job or scheduler intervention occurred. | Initial runtime corruption; DayOA `15.0.8` ignore omission; omitted explicit provisioned cost-center identity. | Exported and verified without FSx deletion. |
| QC-002 | Catalog | Render, dry-run, and live-launch current `ont_run_qc` only after its DRA and selected child path verify. | SUCCESS | feature_implementation | Gate 2 | Codex | Current execution contract is DYEC `18.0.16` / DayOA `15.0.9`, target `produce_ont_run_qc_and_demux_multiqc`. DRA is `AVAILABLE`; selected child path proof found one 5,048,955,428-byte sequencing-summary file, 944 `*.fastq.gz` files, and 47 barcode/unclassified group directories. Fresh dry-run `pcand18015_ont_runqc_1509_dry_20260816T1420Z` reached terminal `SUCCEEDED` with attributable `rc=0` and zero Slurm submissions. Fresh live controller `pcand18015_ont_runqc_1509_live_20260816T1422Z` used explicit cost center `pcand-18015-ccenter`, completed all 10 workflow steps, and reached terminal `SUCCEEDED` with attributable `rc=0`. Read-only accounting during the heavy demux rule showed continued CPU and I/O progress; no job or scheduler intervention occurred. |  | Exported and verified without FSx deletion. |
| QC-003 | Catalog | Render, dry-run, and live-launch current `ultima_run_qc` only after its DRA and headnode path verify. | SUCCESS | feature_implementation | Gate 2 | Codex | Current execution contract is DYEC `18.0.16` / DayOA `15.0.9`, target `produce_ultima_run_qc`; the `AVAILABLE` mount and required source files have read-only proof. Fresh dry-run `pcand18015_ultima_runqc_1509_dry_20260816T1456Z` reached terminal `SUCCEEDED` with attributable `rc=0` and zero Slurm submissions. Fresh live controller `pcand18015_ultima_runqc_1509_live_20260816T1513Z` used explicit cost center `pcand-18015-ccenter`, completed four submitted jobs, and reached terminal `SUCCEEDED` with attributable `rc=0`; no job or scheduler intervention occurred. |  | Exported and verified without FSx deletion. |
| EXP-001 | Result export | For each controller with attributable terminal `rc=0`, record export visit, complete no-delete DRA export, and verify its receipt/S3 objects. | SUCCESS | feature_implementation | Gate 3 | Codex | Illumina: task `task-0bcbd1dad1a9a53a2`, detached DRA `dra-032a2da68b14f742d`, 4,039,466-byte MultiQC. ONT: task `task-061399116a85f689b`, detached DRA `dra-0186810753e344290`, 2,845,229-byte native and 3,406,609-byte demux MultiQC. Ultima: task `task-0ab47c24639346b61`, detached DRA `dra-049aa949551c8d1be`, 3,645,480-byte native MultiQC. All receipts record `status=success`, `phase=complete`, `task_lifecycle=SUCCEEDED`, `detached=true`, and `delete_data_in_file_system=false`; exact S3 objects verified. |  | All three analysis roots and input mounts retained; no deletion. |
| CAT-001 | Catalog evidence | Update command catalog evidence with each verified exported S3 prefix. | SUCCESS | feature_implementation | Gate 4 | Codex | Current `illumina_run_qc`, `ont_run_qc`, and `ultima_run_qc` rows now have `validated_version: 15.0.9`, exact `validation_evidence_s3_uri_prefix` values, and successful `pcand-18015` validation rows. Source and packaged catalogs are kept byte-identical; historical numeric snapshots are unchanged. |  | Included in the DYEC 18.0.17 release lane. |

## Execution notes

- Every mount is created through `dyec mounts create` with read-only policy, batch metadata import, `NEW,CHANGED` auto-import, no auto-export, and a 5,400-second readiness timeout.
- Catalog launch owns the persistent Ubuntu interactive-bash/tmux DayOA controller, analysis visit, and write-lock lifecycle. Distinct dry-run and live analysis IDs are required.
- Results are retained on FSx. FSx cleanup requires a separate destructive-action approval and is not requested here.

## Completion

All ledger rows are terminal. The amended `pcand-18015` objective is complete: all three current catalog commands passed exact-version dry-runs and live runs, all three results were exported and verified without deleting FSx data, and the three exact result prefixes are recorded in the current DYEC catalog evidence. Ursa was not touched.
