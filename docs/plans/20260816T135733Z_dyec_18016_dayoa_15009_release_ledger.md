# DYEC 18.0.16 / DayOA 15.0.9 release ledger

Created: 2026-08-16T13:57:33Z  
Objective: pin every active/current DYEC command-catalog DayOA reference to the exact `15.0.9` release that accepts the established DYEC run-context runtime inputs without weakening pinned-source integrity.

## Gate 0

- Base: annotated DYEC tag `18.0.15` (`d337cd13ced84daee2caee65a201aa2ab5806fa5`).
- DayOA `15.0.9` is an annotated tag on merge commit `53071f0680cbe194e1f926fe801ecfb14683feaa` and has a GitHub Release.
- DayOA `15.0.9` changes only `.gitignore` plus its release ledger: `config/runs.tsv` and `config/run_dir_links/` are explicit ignored runtime inputs.
- Exact `15.0.9` catalog dry proof on `pcand-18015`: `pcand18015_ilmn_runqc_1509_dry_20260816T1351Z` reached attributable terminal `rc=0` with zero Slurm submissions.
- No compatibility shim, fallback, alternate staging path, or pinned-checkout patch is added.

| ID | Requirement | Status | Evidence |
|---|---|---|---|
| PIN-001 | Update every active/current command-catalog DayOA tag from `15.0.8` to `15.0.9`. | SUCCESS | Source and packaged catalogs were mechanically updated together. |
| PIN-002 | Update active documentation and contract expectations to DYEC `18.0.16` / DayOA `15.0.9`. | SUCCESS | The same 24 release-contract files changed by the `18.0.15` pin bump were advanced one patch. |
| CAT-001 | Preserve source and packaged catalog identity. | SUCCESS | Byte comparison required before commit. |
| LIVE-001 | Prove a current-catalog run on `pcand-18015`. | IN_PROGRESS | Exact-tag dry proof passed; live Illumina RunQC is currently running and will not be manipulated. |
| REL-001 | Commit, merge, annotated-tag, publish GitHub Release, and build artifacts for `18.0.16`. | OPEN | Pending. |

## Validation boundary

- Per operator instruction, no local test suite is rerun for this mechanical pin release.
- Release evidence is the exact-tag catalog dry proof and the live three-platform RunQC task tracked in `docs/plans/20260816T080946Z_pcan18013_three_platform_seq_qc_ledger.md`.
