# pre-rel-18025 sequencing-QC rerun: DYEC 18.0.26 / DayOA 15.0.15

Created: 2026-08-17T14:27:01Z

## Objective and boundary

Run fresh ILMN, ONT, and Ultima sequencing-QC controllers on
`pre-rel-18025` using the activated DYEC `18.0.26` executable and the maximum
strict numeric DayOA tag that its current catalog pins: `15.0.15`. Each
platform gets one new analysis root; its catalog dry controller and its live
continuation must share that analysis root, checkout, run context, and runtime
configuration. The live controller may remove only `-n` from the verified dry
`dy-r` command.

Scope is limited to AWS profile `lsmc`, `us-west-2`, cluster/project/executing
entity `pre-rel-18025`, active cost center `pre-rel-18025-ccenter`, and remote
user `ubuntu`. All run mounts remain read-only. No DRA mutation, export,
writeback, raw Snakemake, Slurm intervention, budget change, analysis-root
replacement, or cleanup is authorized. The user has authorized source edits in
the three existing controller clones only when the exact root, pinned commit,
intended change, and dry controller command are recorded first; no such edit is
assumed or made merely to avoid diagnosing the failure.

## Gate 0 baseline

- Previous controlling ledger:
  `docs/plans/20260817T115933Z_pre_rel_18025_bjuice_preval_seq_qc_mounts_ledger.md`.
  Its three DayOA `15.0.14` live controllers are now attributable terminal
  successes (`rc=0`) with no active controller or Slurm job, so this is an
  explicit fresh rerun rather than a duplicate of a live controller.
- `source ./activate; dyec version` reports `Daylily Ephemeral Cluster 18.0.26`.
- `git ls-remote --tags` for `git@github.com:lsmc-bio/daylily-omics-analysis.git`
  shows strict numeric tags through maximum `15.0.15`. The annotated tag object
  is `78579c094be4f584ceb68900d6fb7da1ba9c3de9`; its peeled commit is
  `6ac26d834b5492bd4dfced7fe67f22a02c09c0eb`.
- Current catalog shows all three commands at `git_tag=15.0.15`:
  `illumina_run_qc -> produce_illumina_run_qc`,
  `ont_run_qc -> produce_ont_run_qc_and_demux_multiqc`, and
  `ultima_run_qc -> produce_ultima_run_qc`.
- The optional immutable catalog selector `--dyec-version 18.0.26` is not
  published (`No command eligibility set for DYEC build: 18.0.26`). It selects
  a catalog snapshot rather than the installed executable. Per the user's
  request, launches use the verified `18.0.26` executable's default current
  catalog view, with `--git-tag 15.0.15` explicit; no unavailable snapshot is
  claimed or substituted.
- Fresh re-verification at 2026-08-17T14:27Z: all three existing DRAs are
  `AVAILABLE`, `verified=true`, `usable=true`, read-only, metadata-import
  enabled, auto-import `NEW,CHANGED`, and have no auto-export. Verification
  command IDs: ILMN `ba73ca4b-5e39-402d-bcd3-f8c309775f82`; ONT
  `9b2cfad1-70e6-4549-9142-f6483e5b1b0f`; Ultima
  `3236a505-6df8-4310-84d4-54a2e66e745e`.
- Cost center `pre-rel-18025-ccenter` is active, permits `ubuntu`, and remains
  capped at USD 1100. This task does not alter it.
- Exact existing run contexts, content-hashed before reuse:
  - ILMN `docs/plans/20260817T115933Z_pre_rel_18025_bjuice_preval_seq_qc_mounts_artifacts/illumina_runs.tsv`
    — `52734f10d29db95f53c7284bb1c47dda162dd6040a45b7c6a9cb62ef8dfc0f5f`.
  - ONT `docs/plans/20260817T115933Z_pre_rel_18025_bjuice_preval_seq_qc_mounts_artifacts/ont_runs.tsv`
    — `6ef0b7ee2603f71fd375bc4b1862fe78a31af40cf5210ee9d63914af17107e3e`.
  - Ultima `docs/plans/20260817T115933Z_pre_rel_18025_bjuice_preval_seq_qc_mounts_artifacts/ultima_runs.tsv`
    — `94b9c2019fbaa035accd6a6bfb817aedd7484001a6e43e908aa37c05d6b5e39b`.
- User-owned local changes remain untouched: this repo has untracked `TrusSV/`
  and `tmp/dayoa-ont-headnode-proof/`; sibling DayOA checkout is on
  `codex/solo-kitchensink-sex-contract` with its listed modified rules/tests and
  untracked `tmp/`.

| ID | Area | Requirement | Status | Category | Approval gate | Owner | Evidence / terminal note |
|---|---|---|---|---|---|---|---|
| G0-001 | Scope | Freeze target, input contexts, existing-controller state, and safety boundary. | SUCCESS | legitimate_safety_handling | Gate 0 | Codex | Baseline above. |
| PIN-001 | Version pin | Use DYEC 18.0.26 and explicit maximum current-catalog DayOA tag 15.0.15. | SUCCESS | config_or_startup_contract | Gate 0 | Codex | Version/tag evidence above; named immutable catalog snapshot absent and not substituted. |
| MNT-001 | Inputs | Reverify the three existing read-only DRAs before new controllers. | SUCCESS | legitimate_safety_handling | Gate 0 | Codex | All three `verified=true`, `usable=true`; IDs above. |
| HN-001 | Headnode | Confirm the `ubuntu` DayOA controller contract exposed by the new local DYEC before launch. | SUCCESS | config_or_startup_contract | Gate 1 | Codex | Read-only `headnode run` command `7e281baf-57f8-471b-9f9e-fc0a30a8ac1f` returned remote DYEC 18.0.26, `/home/ubuntu/.local/bin/day-clone`, `dyec analysis` availability, and tmux 3.2a. |
| DRY-ILMN | Catalog dry | Render and launch fresh ILMN dry controller at tag 15.0.15; require attributable `rc=0` and zero submitted Slurm jobs. | SUCCESS | feature_implementation | Gate 1 | Codex | Same-root `retry1` status is attributable terminal `rc=0`, with zero submitted/finished Slurm jobs. Its plan log is recorded below. |
| LIVE-ILMN | Catalog continuation | Continue the verified ILMN dry root, removing only `-n`. | SUCCESS | feature_implementation | Gate 1 | Codex | `-live` receipt created at 2026-08-17T14:51Z; it submitted external job `20`. No BCL Convert. Workflow execution remains separately active. |
| DRY-ONT | Catalog dry | Render and launch fresh ONT dry controller at tag 15.0.15; require attributable `rc=0` and zero submitted Slurm jobs. | SUCCESS | feature_implementation | Gate 1 | Codex | Same-root `retry1` status is attributable terminal `rc=0`, with zero submitted/finished Slurm jobs. Its plan log is recorded below. |
| LIVE-ONT | Catalog continuation | Continue the verified ONT dry root, removing only `-n`. | SUCCESS | feature_implementation | Gate 1 | Codex | `-live` receipt created at 2026-08-17T14:53Z; it submitted external jobs `21` and `22`. No basecalling. Workflow execution remains separately active. |
| DRY-ULT | Catalog dry | Render and launch fresh Ultima dry controller at tag 15.0.15; require attributable `rc=0` and zero submitted Slurm jobs. | SUCCESS | feature_implementation | Gate 1 | Codex | Same-root `retry1` status is attributable terminal `rc=0`, with zero submitted/finished Slurm jobs. Its plan log is recorded below. |
| LIVE-ULT | Catalog continuation | Continue the verified Ultima dry root, removing only `-n`. | SUCCESS | feature_implementation | Gate 1 | Codex | `-live` receipt created at 2026-08-17T14:57Z; it submitted external job `23`. No basecalling. Workflow execution remains separately active. |
| AUTH-001 | Source-test scope | Record user's permission to edit the three headnode controller clone dirs if a concrete repair is needed. | SUCCESS | plan_amendment | Gate 1 | Codex | User authorized clone-dir source edits after the first failure. No versioned source edit has been made; each concrete edit/test will be recorded against exact root `15.0.15` commit `6ac26d834b5492bd4dfced7fe67f22a02c09c0eb`. |
| SRC-001 | Static source diagnosis | Identify the initialization path without executing a repository test suite. | SUCCESS | config_or_startup_contract | Gate 1 | Codex | In the ILMN clone, `dyoainit` sees no named `DAYOA` env and sources `config/day/day_env_installer.sh`; that installer creates the globally named `DAYOA` env and uses the default shared cache. Details below. |
| REC-001 | Runtime recovery | Diagnose the shared package-cache failure and run one serial, same-root, controller-owned dry retry before any other retry. | SUCCESS | legitimate_safety_handling | Gate 1 | Codex | ILMN `retry1` built the globally named DAYOA environment successfully and its dry controller finished `rc=0`. No cache deletion, replacement, or source edit was needed. |
| FIN-001 | Acceptance | Reconcile receipts and terminalize every row without workflow/Slurm intervention. | SUCCESS | contract_test | Gate 5 | Codex | All three dry receipts are attributable `rc=0` with zero jobs, and each supported live continuation has an attributable receipt plus first submitted Slurm job. No workflow completion or Slurm intervention is claimed. |

## Current status counts

- SUCCESS: 14
- OPEN: 0
- IN_PROGRESS: 0
- ATTEMPTING_BUGFIX: 0
- BLOCKED: 0
- FAIL: 0

## Execution observations

- `2026-08-17T14:28Z`: The headnode readiness check ran as `ubuntu` in an
  interactive bash login shell through DYEC's supported SSM helper. The
  `dy-r` alias is expected to be defined only after controller-owned
  `source dyoainit`; no manual workflow shell or raw Snakemake command was run.
- `2026-08-17T14:29Z`: All three catalog dry renders used the activated
  `18.0.26` executable's `current` view, explicit `--git-tag 15.0.15`, exact
  existing context TSV, and a fresh analysis root with a distinct `-dry` tmux
  session. ILMN resolves `produce_illumina_run_qc` without BCL Convert; ONT
  resolves `produce_ont_run_qc_and_demux_multiqc` without basecalling; Ultima
  resolves `produce_ultima_run_qc` without basecalling. Each rendered command
  has exactly one `-n` dry flag.
- `2026-08-17T14:33Z`: All three concurrently launched dry controllers became
  attributable terminal failures with `exit_code=1`, zero submitted/finished
  Slurm jobs, and no workflow failure markers. Supported controller logs show
  `dyoainit` failed while creating the DAYOA environment: archives under the
  shared `/home/ubuntu/miniconda3/pkgs/` cache were missing or had failed
  `.partial` renames. No DayOA rule ran.
- `2026-08-17T14:35Z`: Read-only clone/cache inspection (after required
  analysis-root visits) found all three controller clones pinned cleanly to
  `6ac26d834b5492bd4dfced7fe67f22a02c09c0eb`, with only expected untracked
  `.dyec/` runtime state and retained `config/runs.tsv`. The shared packages
  `jedi`, `go-shfmt`, `libgfortran5`, and `numpy` named in the logs are absent,
  and the DAYOA environment is absent. The concurrency relationship is an
  evidence-based inference; a serialized same-root controller retry is the
  first user-authorized test. No repository test suite will be run.
- `2026-08-17T14:43Z`: Static inspection of the ILMN clone at the same pinned
  commit (SSM command `67d01735-214e-4e76-8ca0-ca5706507179`) established the
  exact initialization route. `dyoainit` invokes
  `config/day/day_env_installer.sh DAYOA` whenever the global named `DAYOA`
  environment is absent. The installer runs `conda env create -n DAYOA` and
  globally changes Conda configuration; it does not set a clone-local
  `CONDA_PKGS_DIRS`. Conda reports shared cache directories
  `/home/ubuntu/miniconda3/pkgs` and `/home/ubuntu/.conda/pkgs`, and a
  `libharfbuzz-14.3.1-h23af247_0.conda.partial` remains in the first cache.
  Thus the three concurrent controllers contended both for one environment
  name and one package cache. This is stronger causal evidence for the
  initialization failure, but it does not authorize cache deletion or a source
  edit. The next bounded test is one serial, controller-owned retry in the
  existing ILMN analysis root; no Git test suite will run.
- `2026-08-17T14:44Z`: The supported catalog render for the serial ILMN retry
  reconfirmed `illumina_run_qc`, tag `15.0.15`, the QC-only
  `produce_illumina_run_qc` target, and the exact dry command
  `bin/day_run produce_illumina_run_qc -p -j 20 -k -n --config run_context_file=config/runs.tsv run_context_only=true --produce-analysis-artifact-manifest true --produce-rulegraph true --produce-filegraph false --produce-dag false`.
  The retry will use lower-level supported `workflow launch` continuation
  flags because catalog launch cannot continue an existing analysis root. It
  will preserve the existing `config/runs.tsv` and will not stage, replace, or
  delete anything.
- `2026-08-17T14:45Z`: A local invocation carrying `--json` was rejected by
  the current CLI before headnode dispatch (`workflow/launch` does not support
  JSON mode). It did not create a controller or modify the analysis root; the
  supported serial continuation will be invoked without that presentation flag.
- `2026-08-17T14:47Z`: ILMN same-root dry controller
  `prerel18025_ilm_seq_qc_18026_15015_20260817T1427Z-retry1` completed with
  attributable `exit_code=0` and zero submitted/finished Slurm jobs. The
  status points to its exact plan log at
  `.snakemake/log/2026-08-17T144755.741767.snakemake.log`. Zero jobs is the
  expected acceptance condition for its `-n` dry command, not a budget or
  scheduler failure.
- `2026-08-17T14:49Z`: The attempted stagger was stopped before Ultima launch.
  ONT `retry1` had already reached an attributable running dry controller
  (PID `132535`) with zero jobs before the stop; it will not be duplicated.
  A follow-up status query for Ultima reported its `retry1/status.json` is
  absent (SSM command `399bc520-d301-4240-9edb-3f68f367167d`), proving no
  Ultima retry controller exists.
- `2026-08-17T14:51Z`: Per the user's continuation instruction, the same
  verified ILMN root was launched immediately without only `-n`, in session
  `prerel18025_ilm_seq_qc_18026_15015_20260817T1427Z-live` (controller PID
  `141114`). Its command remains QC-only and has no BCL Convert. The next
  platform will not be advanced until this live controller actually reports a
  submitted Slurm job; its already-running ONT dry controller is monitored
  without intervention.
- `2026-08-17T14:53Z`: ONT `retry1` also reached attributable terminal
  `exit_code=0` with zero submitted/finished Slurm jobs; its exact plan log is
  `.snakemake/log/2026-08-17T144941.162442.snakemake.log`. Per the same
  immediate-continuation instruction, its existing root was launched without
  only `-n` in session
  `prerel18025_ont_seq_qc_18026_15015_20260817T1427Z-live` (controller PID
  `148658`). It excludes basecalling. Ultima remains unstarted and no source
  edit or repository test suite has run.
- `2026-08-17T14:54Z`: The requested live-submission gate was observed. ILMN
  live submitted external Slurm job `20` for
  `illumina_run_qc_fetch_metric_subset-20260618_LH01106_0011_A23MFMCLT3`;
  ONT live submitted jobs `21` and `22` for the mounted ONT QC target. All
  were legitimately `CONFIGURING`; no Slurm intervention was performed.
- `2026-08-17T14:55Z`: After that gate, the supported Ultima same-root dry
  continuation launched in session
  `prerel18025_ultima_seq_qc_18026_15015_20260817T1427Z-retry1` (controller
  PID `155732`) at tag `15.0.15`. It uses its QC-only `-n` command, has no
  basecalling, and will be continued immediately without only `-n` if its
  attributable dry status reaches `rc=0` with zero submitted Slurm jobs.
- `2026-08-17T14:56Z`: Ultima `retry1` reached attributable terminal
  `exit_code=0` with zero submitted/finished Slurm jobs; its exact plan log is
  `.snakemake/log/2026-08-17T145530.749480.snakemake.log`. Per the immediate
  continuation instruction, the same root launched without only `-n` in
  session `prerel18025_ultima_seq_qc_18026_15015_20260817T1427Z-live`
  (controller PID `163973`). It is QC-only with no basecalling. No source
  file, cache entry, or repository test suite was changed or run.
- `2026-08-17T14:58Z`: All three live-launch acceptance gates are met. ILMN
  reports submitted external Slurm job `20`; ONT reports `21` and `22`; and
  Ultima reports `23`. Each controller is attributable and running from its
  pinned `15.0.15` checkout. Current Slurm states are `PENDING` with reason
  `(BeginTime)`, which is reported only; no job, scheduler, budget, source, or
  cache intervention has been taken. This terminalizes the requested
  controller-launch objective, not the longer-running QC workflows themselves.
