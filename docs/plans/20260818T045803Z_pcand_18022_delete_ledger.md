# `pcand-18022` cluster deletion ledger

Controlling plan: this ledger
Ledger path: `docs/plans/20260818T045803Z_pcand_18022_delete_ledger.md`

## Gate 0: Inventory Freeze

- Repository: `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster`
- Branch: `codex/dayoa-15-0-18-pinned`
- Baseline: `git status --short --branch` showed only pre-existing user-owned untracked paths; no existing files were modified by this task.
- Profile/region: `lsmc` / `us-west-2`
- Candidate clusters inspected read-only: `pre-rel-18025` and `pcand-18022`
- Older target: `pcand-18022`, `creationTime=2026-08-17T06:09:02.168Z`
- Retained cluster: `pre-rel-18025`, `creationTime=2026-08-17T11:29:39.246Z`
- Target status before deletion: `UPDATE_COMPLETE`; compute fleet `RUNNING`
- Target resources reported by DYEC dry run: FSx `fs-09eb220d064d55d92`; DRAs `dra-0a739dd6aa2d433a0`, `dra-0cc3051c7e460429e`, and `dra-0adba8261208342cd`
- Dry-run command: `dyec delete --dry-run --profile lsmc --region us-west-2 --cluster-name pcand-18022`
- Dry-run result: no AWS resources changed; DYEC reported the cluster-bound external P2 FSx will also be deleted after the ParallelCluster is absent.
- Approval: user explicitly confirmed a second time: delete only `pcand-18022`, then stop.

## Ledger

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| DEL-001 | Scope | Delete only `pcand-18022` in `us-west-2` using profile `lsmc`; leave `pre-rel-18025` untouched. | SUCCESS | legitimate_safety_handling | Gate 0 | orchestrator | `dyec delete --profile lsmc --region us-west-2 --cluster-name pcand-18022 --yes` returned the `Cluster deleted` summary; final `dyec --json cluster list` showed only `pre-rel-18025`. |  | `pcand-18022` was the only deletion target. |
| DEL-002 | Verification | Verify the target cluster reaches terminal absence and confirm the retained cluster remains present. | SUCCESS | contract_test | Gate 5 | orchestrator | Final DYEC inventory contained only `pre-rel-18025`; its describe payload remained `UPDATE_COMPLETE` with compute fleet `RUNNING`. FSx `fs-09eb220d064d55d92` returned `FileSystemNotFound`; the three target DRAs were absent. |  | Target cluster, cluster-bound FSx, and target DRAs are absent; retained cluster remains live. |
| DEL-003 | Handoff | Stop after deletion verification with no additional cluster or workflow actions. | SUCCESS | legitimate_safety_handling | Gate 5 | orchestrator | Final verification completed; no further AWS mutation was performed. |  | Task stopped after scoped deletion and verification. |

## Executed Command

```bash
source ./activate
dyec delete --profile lsmc --region us-west-2 --cluster-name pcand-18022 --yes
```

Result: exit `0`; DYEC reported `Cluster deleted` for `pcand-18022`.
