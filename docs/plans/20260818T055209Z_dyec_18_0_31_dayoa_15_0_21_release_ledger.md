# DYEC 18.0.31 / DayOA 15.0.21 release ledger

UTC opened: 2026-08-18T05:52:09Z
Owner: Codex
Scope: preserve eligible outstanding DYEC records, merge them into a release branch from the latest DYEC tag, and move only DYEC's default and `current` command-catalog DayOA references to the already-pushed DayOA `15.0.21` tag.

## Gate 0 — baseline

- Primary worktree: `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster`, branch `codex/dayoa-15-0-18-pinned`, `HEAD=8a7c5348273758c2f8e2cd8dcb9ab94ad1cdc597` (`18.0.28`); it has no tracked modifications and 28 untracked durable `docs/plans/**` records/artifacts.
- Excluded as non-repository runtime or separate-repository material: `.playwright-cli/`, nested `TrusSV/`, and `tmp/dayoa-ont-headnode-proof/`. These stay only in the primary worktree.
- Clean integration worktree: `/Users/jmajor/projects/lsmc/.codex-worktrees/clone-resident-status-v2-dyec`, initially `codex/clone-resident-status-v2` at `843140e059d3dc2bb7b59114a5442b9cd340b495`, a pushed post-`18.0.30` status-v2 export-evidence commit.
- Latest DYEC annotated tag: `18.0.30`, tag object `81f0940afd3f08cc3c04ec9f5d851b49b383fd84`, peeled commit `bad195d6417197817a6443cdb289699825d6575d`.
- Required DayOA tag is present remotely and annotated: `15.0.21`, tag object `4271c7a1fd9a986795dca5cf87e76604a9832241`, peeled commit `16e11e843252695dca3f396dfe682fdb32e11435`.
- Current DYEC source/payload catalog defaults and `current` entries reference DayOA `15.0.19`; historical DYEC catalog snapshots are out of scope and must remain immutable.

## Tracked rows

| ID | Status | Work item | Evidence / terminal note |
|---|---|---|---|
| G0 | SUCCESS | Record baseline and release boundaries | Baseline above; no runtime workflow, AWS, FSx, or package-registry action is in scope. |
| T1 | SUCCESS | Commit and push eligible outstanding DYEC records to a temporary branch | Pushed `codex/dyec-18-0-31-dayoa-15-0-21-temp` at `e3b4bd9710a6316ca83e498a61c4d395c03f4ff4`, preserving status-v2 evidence plus the 28 durable operation records. |
| T2 | SUCCESS | Create release branch from immutable `18.0.30` | `codex/dyec-18-0-31-dayoa-15-0-21-release` starts at peeled `18.0.30` commit `bad195d6417197817a6443cdb289699825d6575d`; no existing tag was changed. |
| T3 | SUCCESS | Merge temporary branch into release branch | True no-fast-forward merge `7f666e99fd63fcac42520564b2526b7c6f8ac432` has parents `bad195d6` and `e3b4bd97`. |
| T4 | SUCCESS | Update default DayOA pin and only `current` catalog calls to `15.0.21` | Catalog default and all 30 `current` commands now resolve to the annotated DayOA `15.0.21` peeled commit `16e11e843252695dca3f396dfe682fdb32e11435`; prior validation-version provenance is retained. |
| T5 | SUCCESS | Maintain source/payload catalog parity and update focused contract expectations | Source and packaged catalog are byte-identical. Current docs/tests reference the new pins. A stray `16.1.81` validation-run append was removed from both historical snapshots to restore their immutable `18.0.17` content; active/current evidence remains in its proper current records. |
| T6 | SUCCESS | Run focused local release validation | Catalog audit passed (`current_commands=30`, `aliases=1`, historical snapshots unchanged); focused suite `88 passed`; targeted render/registry checks `3 passed`. No raw Snakemake or live cluster/workflow action ran. |
| T7 | IN_PROGRESS | Commit, push, annotate `18.0.31`, and push tag | Tag only a clean exact release commit after this ledger update. |
| T8 | OPEN | Verify remote branch/tag and final clean state | Record annotated tag object, peeled commit, and test evidence. |

## Release boundary

This is a Git-source release only. It does not publish a package to a registry and does not alter any existing workflow, controller, DRA, FSx, S3, or Slurm state.
