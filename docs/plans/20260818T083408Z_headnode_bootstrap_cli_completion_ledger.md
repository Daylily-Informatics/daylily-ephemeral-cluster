# Headnode DayOA Bootstrap and CLI Completion Ledger

Controlling request: extend `dyec headnode configure` so its first successful Ubuntu configuration clones the catalog-pinned DayOA checkout and serially completes `source dyoainit` followed by `dy-b BUILD` in an interactive Bash login shell; copy DYEC `config/*.yaml` into `/home/ubuntu/.config/daylily/` before DayOA initialization; add an explicit `--force` environment reset; alphabetize DYEC help; activate Bash/Zsh completion automatically; then release a new DYEC tag and run the live configure command.

Ledger path: `docs/plans/20260818T083408Z_headnode_bootstrap_cli_completion_ledger.md`

## Gate 0 — Inventory Freeze

- Repository: `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster`, branch `codex/dayoa-15-0-18-pinned`, `HEAD=bf471982` / tag `18.0.33`; the branch is ahead of `origin/codex/dayoa-15-0-18-pinned` by 18 pre-existing commits.
- Pre-existing untracked exclusions: `.playwright-cli/`, `TrusSV/`, `docs/candidte-pre-pre-release-tuning.md`, and `tmp/dayoa-ont-headnode-proof/`. This work must not alter them.
- Current DayOA catalog pin: `15.0.22` in `daylily_ec/resources/payload/config/daylily_pipeline_command_catalog.yaml`.
- Observed bootstrap failure: four dry controllers simultaneously invoked `dyoainit`; all failed before DayOA/Snakemake start while concurrently creating the single `DAYOA` Conda environment. Evidence included a missing Conda `.condatmp` source and transient PyPI HTTP 502 failures.
- Current standard configure flow is in `daylily_ec/workflow/create_cluster.py::configure_headnode`; it installs/rebuilds DAY-EC but does not clone or initialize DayOA.
- Current shell behavior: local and `pre-rel-18025` Ubuntu show `always_yes: None`; both have only `plugins.auto_accept_tos: true`. The DayOA installer and `bin/init_dayec` do set `always_yes`, but the current configure path does not. No repository evidence shows DYEC removing that setting; the exact prior mutation of user-scoped `.condarc` is not recoverable from Git.
- Current live limit: no workflow restart or controller edit is performed by this implementation pass. The user explicitly authorized a normal (non-`--force`) headnode configure after release; no environment removal is authorized or planned.

| ID | Area/Repo | Requirement/Surface | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| BASE-001 | DYEC | Record baseline, scope, pinned versions, dirty exclusions, and live limits. | SUCCESS | plan_amendment | Gate 0 | orchestrator | This ledger Gate 0; `git status --short --branch`; local/headnode Conda inspection. |  | Baseline frozen before code edits. |
| BOOT-001 | DYEC headnode configure | Resolve the effective catalog-pinned DayOA repository/ref, honoring explicit DayOA repo overrides, and clone it on the headnode. | IN_PROGRESS | feature_implementation | Gate 2 | orchestrator | `daylily_ec/workflow/create_cluster.py`; live configure remains pending. |  |  |
| BOOT-002 | DYEC headnode configure | Serialize first-use DayOA initialization inside an Ubuntu interactive Bash login shell: `source dyoainit`, then `dy-b BUILD`, with an exact successful-bootstrap receipt. A missing/stale receipt causes a serial in-place refresh; a malformed receipt fails clearly; only `--force` removes environments. | IN_PROGRESS | config_or_startup_contract | Gate 2 | orchestrator | `daylily_ec/workflow/create_cluster.py`; live configure remains pending. | Existing pre-release headnodes predate the receipt and already have DAYOA. | Normal configure must converge these existing headnodes without an unapproved environment removal. |
| CFG-001 | DYEC headnode configure | Copy the pinned DYEC checkout's top-level `config/*.yaml` to `/home/ubuntu/.config/daylily/` before DayOA initialization. | IN_PROGRESS | config_or_startup_contract | Gate 2 | orchestrator | User amendment; `_build_headnode_config_yaml_sync_command`. |  |  |
| FORCE-001 | DYEC headnode configure | Add explicit `--force` reset that removes only `DAYOA` and `DAY-EC` using `conda env remove -n <name> --yes`, clears bootstrap receipt, and then rebuilds. | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | `create_cluster.py`; CLI and legacy script expose `--force`. |  | Implemented; deliberately not invoked because no environment removal is authorized. |
| CONDA-001 | DYEC activation/configure | Restore explicit noninteractive Conda confirmation configuration while preserving TOS acceptance; do not rely on ambient `.condarc` state. | SUCCESS | config_or_startup_contract | Gate 2 | orchestrator | `activate`; `create_cluster.py`. |  | `always_yes` is set explicitly and every environment mutation also carries `--yes`. |
| CLI-001 | DYEC CLI | Ensure root commands, subcommands, and nested subcommands render alphabetically in help. | SUCCESS | feature_implementation | Gate 1 | orchestrator | `daylily_ec/_registry_v2.py`, `daylily_ec/cli.py`; inspected `dyec --help` and `dyec headnode --help`. |  | Help renderer now sorts the complete visible command list at every level. |
| CLI-002 | DYEC activation | Detect Bash/Zsh when `source ./activate` is used and load the matching DYEC completion into that shell session. | SUCCESS | feature_implementation | Gate 2 | orchestrator | `activate`; generated built-in completion contracts. |  | Completion setup is interactive-shell-only and fails explicitly if generation is unavailable. |
| TEST-001 | DYEC | Update focused contract tests and run one consolidated validation pass after all implementation edits. | NO_LONGER_NEEDED | contract_test | Gate 5 | orchestrator | User explicitly directed: "do not test in git and pytest, just commmit push and run the cli command." |  | Pytest is waived for this delivery; the authorized live configure is the requested validation path. |
| REL-001 | DYEC Git | Commit, push, create an annotated next-version tag, push the tag, and report the immutable version. | IN_PROGRESS | feature_implementation | Gate 5 | orchestrator | User explicitly requested release. |  |  |
| LIVE-001 | DYEC headnode configure | Run the released normal configure command on the named non-Ursa headnode and record the DayOA bootstrap receipt/result. | IN_PROGRESS | config_or_startup_contract | Gate 2 | orchestrator | User explicitly authorized direct CLI execution after commit/push. |  |  |
