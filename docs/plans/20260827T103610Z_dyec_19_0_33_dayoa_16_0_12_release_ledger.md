# DYEC 19.0.33 DayOA 16.0.12 release ledger

Controlling request: incorporate `codex/ganon2-broad-blood-oral-dyec`, add any
DYEC support required by the HIOMR2 release, pin both global and command-catalog
DayOA versions to `16.0.12`, validate, and release the next DYEC patch.

Ledger path:
`docs/plans/20260827T103610Z_dyec_19_0_33_dayoa_16_0_12_release_ledger.md`

## Scope and authorization

- Runtime-asset installation and its non-destructive FSx/DRA publication are
  authorized for Sentieon `202503.04`, following the prior `2025*.*` pattern.
- DYEC source implementation, tests, commits, release preparation, and release
  are authorized by the controlling request.
- DayOA workflow live recall, result publication, Slack mutation, and cleanup
  remain outside this ledger and are not authorized.
- No staged runtime asset may be deleted by the export path.

## Gate 0 baseline

- Base annotated tag: `19.0.32`, peeled commit
  `572e74a5ddd1c6d274e42a8bfd293bb6a56f8d73`.
- Candidate branch: `codex/dyec-19-0-33-dayoa-16-0-12`.
- Ganon source tip: `6e10f4f3`; cherry-pick commit on this branch:
  `ebf1167871f625d8bbd0dcf5d11a894dd2ebe640`.
- Initial owned dirty paths after the cherry-pick:
  `daylily_ec/cli.py`, `daylily_ec/workflow/export_data.py`, and
  `tests/test_export.py`.
- No unrelated user-owned dirty paths were present in this clean worktree.
- Runtime source staged at
  `/fsx/analysis_results/runtime_assets/cached_envs/sentieon-genomics-202503.04`.
- Intended independent destination:
  `s3://daylily-dayoa-runtime-assets-usw2/runtime_assets/cached_envs/sentieon-genomics-202503.04/`.
- Intended read-only projection:
  `/fsx/runtime_assets/cached_envs/sentieon-genomics-202503.04/`.
- Baseline focused command:
  `source ./activate && pytest -q tests/test_export.py tests/test_run_mounts.py tests/test_fsx_mounts_additional_coverage.py`
  -> `110 passed`.

## Control ledger

| ID | Area | Requirement | Status | Category | Gate | Evidence | Root cause / terminal note |
|---|---|---|---|---|---|---|---|
| BASE-001 | Lineage | Start from the clean peeled `19.0.32` release and preserve the branch baseline. | SUCCESS | contract_test | Gate 0 | Tag and commit identities above; clean worktree before the Ganon cherry-pick. | Baseline frozen. |
| GANON-001 | Ganon | Incorporate `codex/ganon2-broad-blood-oral-dyec` without dropping its novel broad blood/oral work. | SUCCESS | feature_implementation | Gate 1 | Clean cherry-pick at `ebf1167871f625d8bbd0dcf5d11a894dd2ebe640`. | Ganon tip retained as one auditable commit. |
| EXPORT-001 | Runtime assets | Add a bounded public export contract for one immutable `/fsx/analysis_results/runtime_assets/cached_envs/<asset>` entry. | IN_PROGRESS | feature_implementation | Gate 1 | `--export-kind runtime-asset`; exact source validation; no clone-status requirement; source retention enforced; 110 focused tests pass. | Live no-delete DRA export and receipt remain to be proven. |
| EXPORT-002 | Runtime assets | Export Sentieon `202503.04` to a non-overlapping, empty runtime-assets prefix and verify the completed task. | IN_PROGRESS | active_product_contract | Gate 2 | Source and destination recorded above; canonical reference-bucket attempt correctly rejected overlap with `dra-04d8e3d020818a855`. | Awaiting the independent-bucket DRA run. |
| MOUNT-001 | Runtime assets | Mount the exported asset read-only on the active FSx and verify executable, bundle members, hashes, and provenance. | OPEN | active_product_contract | Gate 2 | Planned `custom` DRA path is outside `/references/` and does not overlap active associations. | Depends on `EXPORT-002`. |
| PIN-001 | Global DayOA | Change the shipped global DayOA pin from `16.0.11` to `16.0.12`. | OPEN | config_or_startup_contract | Gate 3 | Pending source sweep after DayOA release exists. | DayOA `16.0.12` is not released yet. |
| PIN-002 | Catalog | Change every in-scope command-catalog DayOA pin to `16.0.12` and prove no stale `16.0.11` pin remains. | OPEN | config_or_startup_contract | Gate 3 | Pending source sweep after DayOA release exists. | DayOA `16.0.12` is not released yet. |
| TEST-001 | Validation | Run focused and full DYEC tests and compare failures with the pristine `19.0.32` baseline. | OPEN | contract_test | Gate 4 | Focused runtime export/mount suite: 110 passed. | Full candidate/baseline comparison remains. |
| TEST-002 | Catalog | Render and test the Ganon and HIOMR2 catalog contracts with exact `16.0.12` pins. | OPEN | contract_test | Gate 4 | Pending final pins. | Depends on `PIN-001` and `PIN-002`. |
| REL-001 | Release | Commit a clean candidate, push it, and create/push annotated tag `19.0.33` only after all dependencies and tests pass. | BLOCKED | active_product_contract | Gate 5 | Release tags are immutable; no tag has been created. | Blocked on the unreleased CLI `1.7.3i` and DayOA `16.0.12`, including the unresolved Sentieon Hybrid haploid small-variant contract. |

## Acceptance

- No runtime cache is copied object-by-object to S3.
- The export destination is empty before attach, does not overlap an active
  DRA, reaches a successful export task, and is detached safely.
- The imported DRA is read-only and exposes the exact staged runtime at the
  configured stable path.
- Global and command-catalog DayOA pins both equal `16.0.12`.
- The full test delta is attributable and accepted before the annotated
  `19.0.33` tag is created.

The ledger is active. The objective is not yet complete while any row is
`OPEN`, `IN_PROGRESS`, or `BLOCKED`.
