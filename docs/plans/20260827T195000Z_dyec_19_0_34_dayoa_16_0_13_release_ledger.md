# DYEC 19.0.34 / DayOA 16.0.13 release ledger

## Objective

Publish the immutable DYEC patch that replaces the failed DayOA `16.0.12`
runtime pin with `16.0.13`, whose only behavioral change is splitting
ExpansionHunter from the incompatible TIDDIT/Hybrid CLI conda environment.

## Gate 0

- Base: annotated DYEC `19.0.33`, peeled commit
  `eb305cb97db556333979f3a2b3e5069d2a0df71f`.
- Candidate: `19.0.34`; tag absent.
- DayOA `16.0.12` and DYEC `19.0.33` remain immutable failed releases.
- Failed capsule `hg002-slim5x5-bjuice-inflection-20260827t193512z`
  submitted no HG002 jobs.
- Preserve every historical catalog build; add a new `19.0.34` build and
  retarget only the active repository commands to DayOA `16.0.13`.

## Ledger

| ID | Work | Status | Evidence / terminal criterion |
|---|---|---|---|
| PIN-001 | Set the active global and command pins to released DayOA `16.0.13`; preserve `19.0.33` history. | SUCCESS | Source and packaged catalogs are byte-identical; active/global and new `19.0.34` commands all pin `16.0.13`; the `19.0.33` object equals its tagged catalog exactly. |
| TEST-001 | Run focused catalog, version, controller, and rendering tests. | SUCCESS | `39 passed` across both release-contract files, dy-r preflight, workflow observability, and the edited launch controller test; `git diff --check` passes. |
| REL-001 | Publish annotated DYEC `19.0.34`. | IN_PROGRESS | DayOA `16.0.13` is an annotated origin tag; the production-shaped catalog render resolved DYEC build `19.0.34`, runtime `daylily-omics-analysis@16.0.13`, the normal three targets, and the exact six-manifest dry launch contract. Commit/tag the clean candidate. |
| CONFIG-001 | Configure Bjuice with exact state file and verify remote version. | BLOCKED | Remote `dyec --version` is `19.0.34`; no Slurm intervention. |
| LIVE-001 | Start a new fresh HG002 5x5 dry/live capsule. | BLOCKED | Dry `rc=0`/zero submissions, then live only removes `-n`. |

No tag movement, same-root retry of the failed release, Ganon2 job mutation,
S3 export, cleanup, or Slurm administration is in scope.
