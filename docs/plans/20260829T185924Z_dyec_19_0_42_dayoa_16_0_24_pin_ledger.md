# DYEC 19.0.42 / DayOA 16.0.24 pin and release ledger

Date opened: 2026-08-29T18:59:24Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA candidate.

## Boundaries

- Build from annotated DYEC `19.0.41`, peeled commit `af4e4f6285afcd9621338a5d9bdedf3646efb06a`.
- Preserve the dirty operator DYEC checkout. Use only the clean worktree `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-42-dayoa-16-0-24`.
- Change the global DayOA default and only the production and slim-validation `inflection-bjuice-product-v0.9` active command pins to DayOA `16.0.24`.
- Preserve both catalog target lists, command strings, input contracts, and every historical catalog snapshot.
- Do not release DYEC before DayOA `16.0.24` is an immutable annotated origin tag and the DayOA ledger release gates pass.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze base, candidate worktree, tag availability, and dirty operator boundary. | SUCCESS | Clean branch `codex/dyec-19-0-42-dayoa-16-0-24` starts at peeled `19.0.41`; `19.0.42` was absent from origin at inventory. |
| PIN-001 | Set the active global DayOA default to `16.0.24`. | IMPLEMENTED_PENDING_RELEASE | Source and packaged catalogs use the `16.0.24` default; public catalog receipts resolve the `19.0.42` snapshot while the inspecting installed runtime remains correctly identified as `19.0.41`. Release remains gated on the DayOA tag. |
| PIN-002 | Pin only `inflection-bjuice-product-v0.9` and its slim 5x5 validation entry to `16.0.24`. | IMPLEMENTED_PENDING_RELEASE | Public show/render proves both entries resolve `16.0.24` without changing targets or command strings. Release remains gated on the DayOA tag. |
| SNAP-001 | Add immutable `19.0.42` snapshot reflecting the mixed active pin contract while preserving all historical snapshots. | SUCCESS | Snapshot contains 33 commands and tags `16.0.23` plus `16.0.24`; the two approved commands resolve `16.0.24` and every other command remains `16.0.23`. All 78 pre-existing build objects are semantically identical to annotated `19.0.41`. |
| PAYLOAD-001 | Keep source and packaged catalog byte-identical. | SUCCESS | Direct `cmp` passes after active-pin and snapshot generation. |
| CAT-001 | Prove public show/render preserves exact production and slim target/command shapes and resolves DayOA `16.0.24`. | SUCCESS | Public `dyec --json catalog show` resolves both entries to the exact three existing targets and DayOA `16.0.24`. Public slim `catalog render --dry-run` preserves the production-shaped command, config path, `-j 333 -T 0 -p`, and the same target list while adding only the expected dry `-n`. |
| REL-001 | Commit, push, and publish annotated immutable DYEC `19.0.42`. | IN_PROGRESS | DayOA `16.0.24` is now an annotated origin tag at `ceb2beae35270e748067125bbf07322650ebe165`. The authorized no-delete Ganon repair passed exact 14-component/42-artifact validation, removing the reference blocker. Pre-release DYEC checks pass: `git diff --check`, `compileall`, YAML parse, source/payload byte identity, and public catalog show for both unchanged three-target commands. |

## Release rule

Never move a pushed tag. If `19.0.42` becomes occupied or a released validation exposes a DYEC defect, use the next free patch and amend this ledger.
