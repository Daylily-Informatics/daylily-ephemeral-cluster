# DYEC 19.0.43 / DayOA 16.0.25 pin and release ledger

Date opened: 2026-08-29T19:44:51Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA release worktree.

## Boundaries

- Build from annotated DYEC `19.0.42`, peeled commit `e7530322ab597a22b080e405da841560394e9fd9`.
- Preserve the dirty operator DYEC checkout. Use only the clean worktree `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-43-dayoa-16-0-25`.
- Change the global DayOA default and only the active production and slim-validation `inflection-bjuice-product-v0.9` pins to DayOA `16.0.25`.
- Preserve both catalog target lists, command strings, input contracts, and every historical catalog snapshot.
- DayOA `16.0.25` contains only the rule include-order repair exposed by the first released `16.0.24` dry capsule; it does not change the approved scientific or package contract.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze base, candidate worktree, tag availability, and dirty operator boundary. | SUCCESS | Clean branch `codex/dyec-19-0-43-dayoa-16-0-25` starts at peeled annotated `19.0.42`; `19.0.43` was absent from origin at inventory; the dirty operator checkout remains untouched. |
| PIN-001 | Set the active global DayOA default to `16.0.25`. | SUCCESS | Source and packaged catalogs name `16.0.25`; public catalog resolution returns runtime identity `daylily-omics-analysis@16.0.25`. |
| PIN-002 | Pin only `inflection-bjuice-product-v0.9` and its slim 5x5 validation entry to `16.0.25`. | SUCCESS | A semantic comparison against `19.0.42` proves the two approved `git_tag` values are the only active-command changes; targets, command strings, input contracts, and runtime fields are identical. |
| SNAP-001 | Add immutable `19.0.43` snapshot while preserving all earlier snapshots. | SUCCESS | All 79 prior build objects are semantically identical to annotated `19.0.42`; new snapshot contains 33 commands, tags `16.0.23`, `16.0.24`, and `16.0.25`, and differs from `19.0.42` only by the tag list and the two approved pins. |
| PAYLOAD-001 | Keep source and packaged catalog byte-identical. | SUCCESS | Direct `cmp` passes; parsed catalog content is valid. |
| CAT-001 | Prove public show/render preserves exact production and slim target/command shapes and resolves DayOA `16.0.25`. | SUCCESS | Public `catalog show` resolves both entries at `16.0.25` with the unchanged three targets. Public slim dry render retains the normal command shape and adds only the expected `-n` plus standard graph-render flags. |
| REL-001 | Commit, push, and publish annotated immutable DYEC `19.0.43`. | IN_PROGRESS | DayOA `16.0.25` is an annotated origin tag at `8660b520669c08df366706c362f94145721ef3d0`; candidate checks pass: `git diff --check`, compileall, YAML parse, semantic snapshot audit, source/payload byte identity, and public show/render. |
| HEAD-001 | Configure `bjuiceval-19024` from the exact saved state through released DYEC `19.0.43`. | PENDING | Public `dyec headnode configure --force` returns zero and remote clean DYEC/DayOA identities resolve `19.0.43`/`16.0.25`. |
| DRY-001 | Run a brand-new HG002 5x5 full-genome production-shaped dry capsule. | PENDING | Controller/DayOA/Snakemake `0/0/0`, zero Slurm submissions, complete expected DAG, and clean pinned source. |
| LIVE-001 | Continue the exact successful dry capsule live by removing only `-n`. | PENDING | Same root, checkout, inputs, config, targets, and command; final controller/DayOA/Snakemake `0/0/0`. |
| VAL-001 | Validate Inflection/Bjuice packaging and final MultiQC against the accepted contract. | PENDING | Configured RTG/Truvari/VCF statistics, Peddy rollup, Ganon SR/LR, receipt matrix, package status, and final MultiQC all validate. |

## Release rule

Never move a pushed tag. If `19.0.43` becomes occupied or validation exposes another defect, use the next free patch and a new dry/live capsule.
