# DYEC 19.0.44 / DayOA 16.0.26 pin and release ledger

Date opened: 2026-08-29T20:02:28Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA release worktree.

## Boundaries

- Build from annotated DYEC `19.0.43`, peeled commit `bed20df7df9fc56fd2aca8188f27b560c1460b61`.
- Preserve the dirty operator checkout and use only `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-44-dayoa-16-0-26`.
- Move the global DayOA default and only the two approved Bjuice command pins to `16.0.26`; preserve target lists, command strings, input contracts, and all earlier snapshots.
- DayOA `16.0.26` changes only Snakemake brace escaping in the configured RTG rule after the released `16.0.25` dry gate failed before Slurm submission.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze clean annotated `19.0.43`, tag availability, and dirty-operator boundary. | SUCCESS | Worktree starts at peeled `19.0.43`; `19.0.44` was absent from origin; operator checkout untouched. |
| PIN-001 | Update global DayOA default and only the approved production/slim Bjuice pins to `16.0.26`. | SUCCESS | Semantic comparison proves the default plus exactly those two `git_tag` fields changed; all command/target/runtime fields are identical to `19.0.43`. |
| SNAP-001 | Add immutable `19.0.44` snapshot and preserve all earlier snapshots. | SUCCESS | All 80 earlier snapshots are semantically identical; new snapshot has 33 commands, appends `16.0.26`, and differs from `19.0.43` only by the tag list and two approved pins. |
| CAT-001 | Validate source/payload identity and public show/render command shape. | SUCCESS | Source/payload `cmp`, YAML parse, `git diff --check`, compileall, and public show pass; the three production targets are unchanged and runtime identity resolves DayOA `16.0.26`. |
| REL-001 | Publish clean annotated DYEC `19.0.44`. | IN_PROGRESS | DayOA `16.0.26` is an annotated origin tag at `20b09e86f3ffa602b4e209a920278a5602361b8c`; DYEC commit/tag publication is next. |
| HEAD-001 | Configure `bjuiceval-19024` through released public DYEC. | PENDING | Exact saved-state `--force` configure and clean `19.0.44`/`16.0.26` remote identities required. |
| DRY-001 | Run a new HG002 5x5 full-genome dry capsule. | PENDING | Controller/DayOA/Snakemake `0/0/0`, zero submissions, expected DAG, clean source. |
| LIVE-001 | Continue exact successful dry root live by removing only `-n`. | PENDING | Final controller/DayOA/Snakemake `0/0/0`. |
| VAL-001 | Validate packages, receipts, configured benchmarks, Peddy, Ganon2, and final MultiQC. | PENDING | All acceptance checks from the controlling DayOA ledger pass. |

## Release rule

Never move a pushed tag. A further released validation defect requires the next DayOA/DYEC patch and a new dry/live capsule.
