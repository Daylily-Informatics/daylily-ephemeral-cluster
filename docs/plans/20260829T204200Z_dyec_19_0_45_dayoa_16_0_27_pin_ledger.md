# DYEC 19.0.45 / DayOA 16.0.27 pin and release ledger

Date opened: 2026-08-29T20:42:00Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA release worktree.

## Boundaries

- Build from annotated DYEC `19.0.44`, peeled commit `27c2ded7ba39aad816c5f8a4b904510011b55e95`.
- Preserve the dirty operator checkout and use only `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-45-dayoa-16-0-27`.
- Move the global DayOA default and only the two approved Bjuice command pins to `16.0.27`; preserve target lists, command strings, input contracts, and every earlier snapshot.
- DayOA `16.0.27` changes only the Ganon2 constraint resource from a bare positional token to the complete submitter-native `--constraint=bigmem192nvme` option after the `16.0.26` live gate failed before Ganon execution.
- Do not patch pinned source, resume the failed `16.0.26` capsule, manipulate Slurm, export, publish results, or clean resources.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze clean annotated `19.0.44`, tag availability, and dirty-operator boundary. | SUCCESS | Worktree starts at peeled `19.0.44`; operator checkout remains untouched. |
| PIN-001 | Update global DayOA default and only the approved production/slim Bjuice pins to `16.0.27`. | SUCCESS | Semantic comparison identified exactly `inflection-bjuice-product-v0.9` and `inflection-bjuice-product-v0.9-slim-5x5x-validation`; their targets, command strings, input contracts, and all other fields match `19.0.44`. |
| SNAP-001 | Add immutable `19.0.45` snapshot and preserve every earlier snapshot. | SUCCESS | All 81 earlier snapshots are semantically identical. The new 33-command snapshot appends `16.0.27` and differs from `19.0.44` only by the tag list and the two approved pins. |
| CAT-001 | Validate source/payload identity and public show/render command shape without pytest. | SUCCESS | Source/payload identity, YAML parse, semantic snapshot comparison, `git diff --check`, compileall, and public `dyec catalog show` pass. Both approved rows resolve DayOA `16.0.27` and preserve the three production targets. No pytest was run. |
| REL-001 | Publish clean annotated DYEC `19.0.45`. | OPEN | Tag resolves to the intended clean commit; never move a pushed tag. |
| HEAD-001 | Configure `bjuiceval-19024` through released public DYEC and exact state file. | OPEN | Headnode reports clean DYEC `19.0.45` and DayOA `16.0.27` identities. |
| DRY-001 | Run a new HG002 5x5 full-genome dry capsule. | OPEN | Controller/DayOA/Snakemake `0/0/0`, zero submissions, exact production targets, Ganon argv includes the complete constraint flag. |
| LIVE-001 | Continue the exact successful dry root live by removing only `-n`. | OPEN | Controller/DayOA/Snakemake `0/0/0`; no pinned-source mutation or Slurm intervention. |
| VAL-001 | Validate packages, receipts, configured benchmarks, Peddy, Ganon2, and final MultiQC. | OPEN | Every acceptance check in the controlling DayOA ledger passes. |

## Release rule

Never move a pushed tag. A further released validation defect requires the next DayOA/DYEC patch and a new dry/live capsule.
