# DYEC 19.0.47 / DayOA 16.0.29 pin and release ledger

Date opened: 2026-08-29T23:44:41Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA `16.0.29` release worktree.

## Boundaries

- Build from clean annotated DYEC `19.0.46`, peeled commit `fdb58beedfbf677c5329356a45c6dc984627ff8b`.
- Preserve dirty operator checkouts and use only `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-47-dayoa-16-0-29`.
- Move the global DayOA default and only the approved production/slim Bjuice pins to `16.0.29`.
- Preserve command strings, target lists, input contracts, all unrelated command pins, and every earlier numeric snapshot exactly.
- Do not patch pinned headnode source, resume failed validation capsules, intervene in Slurm, export, publish results, or clean resources.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze clean `19.0.46`, tag availability, and operator-checkout boundary. | SUCCESS | Worktree starts at peeled annotated `19.0.46`; local and remote `19.0.47` are absent; operator checkouts are not modified. |
| PIN-001 | Update the global DayOA default and only the approved production/slim Bjuice pins to `16.0.29`. | SUCCESS | Semantic comparison identifies exactly `inflection-bjuice-product-v0.9` and `inflection-bjuice-product-v0.9-slim-5x5x-validation`; every other top-level DayOA command field remains identical to `19.0.46`. Both retain their exact three production targets and public command shape. |
| SNAP-001 | Add immutable snapshot `19.0.47` and preserve all earlier snapshots. | SUCCESS | Every earlier `dyec_builds` value compares equal to annotated `19.0.46`. The new 33-command snapshot equals `19.0.46` except for appending DayOA `16.0.29` and updating exactly the two approved pins. Source and packaged payload catalogs are byte-identical. |
| CAT-001 | Validate source/payload identity and public catalog shape without pytest. | SUCCESS | YAML semantic parse/comparison, source-payload byte identity, `git diff --check`, compileall, and public `dyec catalog show` for both approved commands pass. No pytest or git test suite was run. |
| REL-001 | Publish clean annotated DYEC `19.0.47`. | IN_PROGRESS | Commit first; create and push an annotated non-`v` tag; verify tag type and peeled commit. |
| HEAD-001 | Configure `bjuiceval-19024` through released public DYEC and the exact state file. | OPEN | Headnode must report clean DYEC `19.0.47` and DayOA `16.0.29` identities. |
| DRY-001 | Run a fresh released HG002 5x5 all-chromosome dry capsule. | OPEN | Attributable controller/DayOA/Snakemake `0/0/0`, zero submissions, correct per-AU benchmark and Ganon DAG shape. |
| LIVE-001 | Continue the exact successful released dry root live by removing only `-n`. | OPEN | Attributable `0/0/0`; no pinned-source mutation or Slurm intervention. |
| VAL-001 | Validate packages, receipts, RTG, analytical Truvari, VCF statistics, Peddy, Ganon2, and final MultiQC. | OPEN | Every acceptance check in the controlling DayOA ledger passes. |

## Release rule

Never move a pushed tag. Any additional released validation defect requires the next DayOA/DYEC patches and a fresh dry/live capsule.
