# DYEC 19.0.48 / DayOA 16.0.30 pin and release ledger

Date opened: 2026-08-30T02:01:32Z  
Controlling DayOA ledger: `docs/plans/20260829T172911Z_dayoa_16_0_24_benchmarks_peddy_ganon2_resilient_delivery_ledger.md` in the clean DayOA release worktree.

## Boundaries

- Build from clean annotated DYEC `19.0.47`, peeled commit `b0243ce4a923975051ec5cb8c3c9e260273f31b3`.
- Preserve dirty operator checkouts and work only in `/Users/jmajor/projects/lsmc/.codex-worktrees/dyec-19-0-48-dayoa-16-0-30`.
- Move the global DayOA default and only the approved production/slim Bjuice pins to `16.0.30`.
- Preserve command strings, target lists, input contracts, all unrelated command pins, and every earlier numeric snapshot exactly.
- Do not tag DYEC until annotated DayOA `16.0.30` exists and the exact catalog contracts pass.
- Do not patch pinned headnode source, intervene in Slurm, export, publish results, or clean resources.

## Control ledger

| ID | Requirement | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze clean `19.0.47`, tag availability, and operator-checkout boundary. | SUCCESS | Worktree starts at peeled annotated `19.0.47`; `19.0.48` was absent locally and on origin at Gate 0; operator checkouts are not modified. |
| DAY-001 | Require a clean DayOA `16.0.30` candidate and fresh production-shaped dry proof. | SUCCESS | Exact release commit `c14ba0bbee547ff5439f76f43e135af7838bf14c` passed capsule `bjuiceval19024-hg002-5x5-full-dayoa16030cand-r4-20260830t031115z`, attempt `f151186a-216a-42c4-9cec-8ac2aaedafef`, at attributable controller/DayOA/Snakemake `0/0/0`, zero Slurm submissions, and no failure markers. The immutable annotated `16.0.30` tag peels to that exact commit. |
| PIN-001 | Update the global DayOA default and only the approved production/slim Bjuice pins to `16.0.30`. | SUCCESS | Semantic comparison identifies exactly `inflection-bjuice-product-v0.9` and `inflection-bjuice-product-v0.9-slim-5x5x-validation`; each differs only by `git_tag: 16.0.30` and both retain `produce_sentdhiomr2_slim_kitchensink_mega`, `produce_sentdhiomr2_inflection_analytical_package`, and `bjuice_manifest`. |
| SNAP-001 | Add immutable snapshot `19.0.48` and preserve all earlier snapshots. | SUCCESS | All 84 earlier numeric snapshots compare exactly with annotated `19.0.47`; `19.0.48` is the `19.0.47` command set with DayOA `16.0.30` appended and exactly the two approved pins changed. Source and packaged payload catalogs are byte-identical. |
| CAT-001 | Validate source/payload identity and public catalog shape without pytest. | SUCCESS | YAML semantic parse/comparison, source-payload byte identity, `git diff --check`, compileall, and public `dyec catalog show --dyec-version 19.0.48` for both approved commands pass. No pytest or git test suite was run. |
| REL-001 | Publish clean annotated DayOA `16.0.30`. | SUCCESS | Annotated origin tag `16.0.30` exists, has Git object type `tag`, and peels to clean commit `c14ba0bbee547ff5439f76f43e135af7838bf14c`. |
| REL-002 | Publish clean annotated DYEC `19.0.48`. | OPEN | Commit first; create and push an annotated non-`v` tag only after DayOA `16.0.30` is published; verify tag type and peeled commit. |
| HEAD-001 | Configure `bjuiceval-19024` through released public DYEC and the exact state file. | OPEN | Wait for zero active controllers and Slurm jobs; require clean DYEC `19.0.48` and DayOA `16.0.30` identities. |
| DRY-001 | Run a fresh released HG002 5x5 all-chromosome dry capsule. | OPEN | Attributable controller/DayOA/Snakemake `0/0/0`, zero submissions, unchanged public targets, correct per-AU benchmark and Ganon DAG shape. |
| LIVE-001 | Continue the exact successful released dry root live by removing only `-n`. | OPEN | Attributable `0/0/0`; no pinned-source mutation or Slurm intervention. |
| VAL-001 | Validate packages, receipts, RTG, analytical Truvari, VCF statistics, Peddy, Ganon2, and final MultiQC. | OPEN | Every acceptance check in the controlling DayOA ledger passes, including native Ganon SR/LR and the custom top-hits table. |

## Release rule

Never move a pushed tag. Any additional released validation defect requires the next DayOA/DYEC patches and a fresh dry/live capsule.
