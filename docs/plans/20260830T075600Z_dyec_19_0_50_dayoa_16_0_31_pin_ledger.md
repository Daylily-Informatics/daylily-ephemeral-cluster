# DYEC 19.0.50 / DayOA 16.0.31 pin and release ledger

## Objective

Publish the smallest forward DYEC patch that selects released DayOA `16.0.31`
globally and in the two approved Bjuice catalog entries, configure
`bjuiceval-19024`, and then run one fresh production-shaped HG002 5x Illumina
plus 5x ONT all-chromosome dry/live validation through final Bjuice and
Inflection packaging.

## Boundaries

- Build only from clean annotated DYEC `19.0.49` at
  `714e5e075d982ba0e62764c2d94c9334de741234`.
- Preserve the dirty operator checkout without reset, cleanup, overwrite, or
  inferred incorporation.
- Keep the production and slim-validation target lists, command arguments, and
  six-manifest contract unchanged.
- Do not run pytest or raw Snakemake. Use the public DYEC CLI and `dy-r` only.
- Do not export to S3, post to Slack, clean FSx, or intervene in Slurm.

## Control ledger

| Row | Work | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze the release baseline and tag state. | SUCCESS | Clean worktree from annotated `19.0.49`; `19.0.50` was absent locally and on origin. The operator checkout had 99 pre-existing changed paths and was not modified. |
| DAY-001 | Require the immutable DayOA fix and exact-final-commit dry proof. | SUCCESS | Annotated DayOA `16.0.31` peels to clean commit `ac8a21b0c6a3e5c47712b12b797b926f723dba88`. Fresh capsule `bjuiceval19024-hg002-5x5-full-dayoa16031cand-r2-20260830t075024z`, attempt `73c39c37-7dfd-47ed-bd68-1c68ab6b1a27`, completed controller/DayOA/Snakemake `0/0/0` with zero submissions and no failure markers. |
| PIN-001 | Update the global DayOA default and only the two approved Bjuice command pins to `16.0.31`. | SUCCESS | Root catalog now selects `16.0.31` for `inflection-bjuice-product-v0.9` and `inflection-bjuice-product-v0.9-slim-5x5x-validation`; their target lists remain kitchensink, Inflection analytical package, and `bjuice_manifest`. |
| SNAP-001 | Add immutable catalog snapshot `19.0.50` and preserve earlier snapshots. | SUCCESS | New snapshot is derived from `19.0.49`, adds DayOA `16.0.31`, and changes only the two approved Bjuice pins. Source and packaged catalog mirrors are byte-identical. |
| VAL-001 | Prove source/payload parity, YAML validity, build resolution, and exact command contracts without pytest. | SUCCESS | `compileall`, `git diff --check`, byte-identical catalog mirrors, YAML parsing, and a direct semantic comparison passed. Public `catalog show` and dry `catalog render` resolve build `19.0.50`, DayOA `16.0.31`, the exact three unchanged targets, `-j 333 -T 0 -p`, and the six-manifest HG002 fixture. No pytest was run. |
| REL-001 | Commit, push, and publish annotated immutable DYEC `19.0.50`. | IN_PROGRESS | Source contracts are validated; tag the clean pushed commit and verify tag object plus peeled commit. |
| HEAD-001 | Configure `bjuiceval-19024` using released DYEC `19.0.50` and the exact saved state file. | OPEN | Public `dyec headnode configure --force` succeeds and remote DYEC/DayOA identities are clean and exact. |
| DRY-001 | Run one fresh released HG002 all-chromosome production-shaped dry capsule. | OPEN | Controller/DayOA/Snakemake `0/0/0`, zero submissions, normal catalog targets and complete configured DAG. |
| LIVE-001 | Continue the exact successful dry root live by removing only `-n`. | OPEN | Same root, release commit, manifests, configuration, targets, and command; final controller/DayOA/Snakemake `0/0/0`. |
| AUDIT-001 | Validate final Bjuice/Inflection packages and MultiQC. | OPEN | Complete receipts and expectation matrices; configured hard-VCF RTG, four Truvari comparisons, VCF statistics, Peddy, SR/LR Ganon, sex/ploidy provenance, final MultiQC, and valid package status. |

## Stop conditions

Any pin drift, target-list change, malformed catalog, failed exact dry proof,
dirty released source, or package-integrity defect blocks release acceptance. A
scientific component `NO_CALL` or `FAIL` is evaluated through its configured
receipt and resilient-delivery status rather than silently omitted.
