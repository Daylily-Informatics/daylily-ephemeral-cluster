# DYEC 19.0.51 / DayOA 16.0.32 pin and release ledger

## Objective

Publish the smallest forward DYEC patch selecting released DayOA `16.0.32`
globally and in the two approved Bjuice catalog entries. Configure
`bjuiceval-19024`, then run a fresh production-shaped HG002 5x Illumina plus
5x ONT all-chromosome dry/live validation through final Bjuice and Inflection
packaging.

## Boundaries

- Build only from clean annotated DYEC `19.0.50` at
  `af78af7fdcd826d19ada09d91ff8380d06627cb3`.
- Preserve the dirty operator checkout without reset, cleanup, overwrite, or
  inferred incorporation.
- Keep the production and slim-validation target lists, command arguments, and
  six-manifest contract unchanged.
- Do not run pytest or raw Snakemake. Use the public DYEC CLI and `dy-r` only.
- Do not export to S3, post to Slack, clean FSx, or intervene in Slurm.

## Control ledger

| Row | Work | Status | Evidence / terminal criterion |
|---|---|---|---|
| BASE-001 | Freeze the release baseline and tag state. | SUCCESS | Clean worktree from annotated `19.0.50`; `19.0.51` was absent locally and on origin. The operator checkout had 99 pre-existing changed paths and was not modified. |
| DAY-001 | Require the immutable DayOA fix and exact-final-commit dry proof. | SUCCESS | Annotated DayOA `16.0.32` peels to clean commit `c808b5267f8e349cf31f18d09be39b28525caa99`. Fresh exact-final capsule `bjuiceval19024-hg002-5x5-full-dayoa16032cand-r2-20260830t123814z`, attempt `c255be26-2419-4ecd-8a47-1113761f7159`, completed controller/DayOA/Snakemake `0/0/0` with zero submissions and no failure markers. |
| PIN-001 | Update the global DayOA default and only the two approved Bjuice command pins to `16.0.32`. | SUCCESS | Root catalog selects `16.0.32` for `inflection-bjuice-product-v0.9` and `inflection-bjuice-product-v0.9-slim-5x5x-validation`; their targets remain kitchensink, Inflection analytical package, and `bjuice_manifest`. |
| SNAP-001 | Add immutable catalog snapshot `19.0.51` and preserve earlier snapshots. | SUCCESS | New snapshot is derived from `19.0.50`, appends DayOA `16.0.32`, and changes only the two approved pins. Source and packaged catalog mirrors are byte-identical. |
| VAL-001 | Prove source/payload parity, YAML validity, build resolution, and exact command contracts without pytest. | SUCCESS | `compileall`, `git diff --check`, byte-identical catalog mirrors, YAML parsing, and a direct semantic comparison passed. Public `catalog show` and dry `catalog render` resolve build `19.0.51`, DayOA `16.0.32`, the exact three unchanged targets, `-j 333 -T 0 -p`, and the six-manifest HG002 fixture. No pytest was run. |
| REL-001 | Commit, push, and publish annotated immutable DYEC `19.0.51`. | IN_PROGRESS | Source contracts are validated; tag the clean pushed commit and verify tag object plus peeled commit. |
| HEAD-001 | Configure `bjuiceval-19024` using released DYEC `19.0.51` and the exact saved state file. | OPEN | Public `dyec headnode configure --force` succeeds and remote DYEC/DayOA identities are clean and exact. |
| DRY-001 | Run one fresh released HG002 all-chromosome production-shaped dry capsule. | OPEN | Controller/DayOA/Snakemake `0/0/0`, zero submissions, normal catalog targets, and complete configured DAG. |
| LIVE-001 | Continue the exact successful dry root live by removing only `-n`. | OPEN | Same root, release commit, manifests, configuration, targets, and command; final controller/DayOA/Snakemake `0/0/0`. |
| AUDIT-001 | Validate final Bjuice/Inflection packages and MultiQC. | OPEN | Complete receipts and expectation matrices; configured hard-VCF RTG, four Truvari comparisons, VCF statistics, Peddy, SR/LR Ganon, sex/ploidy provenance, final MultiQC, and valid package status. |

## Stop conditions

Any pin drift, target-list change, malformed catalog, failed exact dry proof,
dirty released source, or package-integrity defect blocks release acceptance. A
scientific component `NO_CALL` or `FAIL` is evaluated through its configured
receipt and resilient-delivery status rather than silently omitted.
