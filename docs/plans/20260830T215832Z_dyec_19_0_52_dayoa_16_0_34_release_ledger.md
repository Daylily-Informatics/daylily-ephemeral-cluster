# DYEC 19.0.52 / DayOA 16.0.34 release ledger

## Objective

Create a clean forward DYEC release from annotated tag `19.0.51`, retain the
useful no-delete adaptive analysis-export work, pin DayOA `16.0.34` globally
and for every command-catalog command, and update the dirty operator checkout
without discarding user-owned work.

## Boundaries

- Do not clean or reset `/Users/jmajor/projects/lsmc/daylily-ephemeral-cluster`.
- Ignore operator-only documentation and generated outputs for the release.
- Never add an S3 delete path. Existing-destination export is additive or
  replacement-only for files present on FSx.
- Do not configure a headnode or launch a workflow in this release task.
- Use focused tests only.

## Gate 0 baseline

| Row | State | Evidence / terminal criterion |
|---|---|---|
| `BASE-001` | `SUCCESS` | Maximum remote annotated tag is `19.0.51`, peeling to `b1491e688e2d425034725756084b9ba15bafee4d`; candidate branch starts at that clean commit. |
| `BASE-002` | `SUCCESS` | `19.0.52` and branch `codex/dyec-19-0-52-dayoa-16-0-34` were absent before worktree creation. |
| `BASE-003` | `SUCCESS` | Operator checkout tracked runtime/code diff is empty; tracked changes are plans/evidence. Untracked code-like paths are `.codex/hiomr2_remote_localrules.patch`, `TrusSV/`, and `tests/test_dclu1904_slim_catalog_materializer.py`. |
| `BASE-004` | `SUCCESS` | Exact `19.0.51` tree has no analysis-export `update-existing` interface, still enforces an empty destination, performs an S3 delete probe, and retains `s3:DeleteObject` permissions. The CUR `--update-existing-export` option is unrelated. |
| `BASE-005` | `SUCCESS` | The operator test and its imported 562-line materializer are both absent from `19.0.51`; the test cannot be imported alone without becoming a guaranteed failure. |

## Execution ledger

| Row | State | Work / terminal criterion |
|---|---|---|
| `EXP-001` | `SUCCESS` | Reconciled the audited adaptive no-delete export candidate onto `19.0.51`, including the newer shared-reference contract. `update-existing` is restricted to analysis exports. |
| `EXP-002` | `SUCCESS` | Static implementation evidence preserves default fresh-prefix failure, requires explicit update-existing admission, removes the mutating S3 write/delete probe, removes direct delete permission, and places an explicit delete deny after the remaining broad S3 grant. Runtime tests are deferred below. |
| `IGN-001` | `SUCCESS` | Added root-scoped ignores for `.codex/`, `TrusSV/`, `outputs/`, and `tmp/`. |
| `OPT-001` | `NO_LONGER_NEEDED` | User accepted the recommendation to omit both the historical operator test and its absent 562-line operational materializer. |
| `PIN-001` | `SUCCESS` | Global DayOA ref is `16.0.34`; remote annotated tag resolves to commit `54c8255f0c0b6a5d730b43ecd60a1a3eca2b52f9`. |
| `PIN-002` | `SUCCESS` | All 33 top-level commands and all 33 commands in new `19.0.52` snapshot pin `16.0.34`; source and payload catalogs are byte-identical. Historical snapshots remain unchanged. |
| `VAL-001` | `DEFERRED_BY_USER` | On 2026-08-30 the user explicitly directed that no tests run before the new-cluster, three-full-package live validation. Focused tests are checked in but not executed. |
| `VAL-002` | `DEFERRED_BY_USER` | CLI and full-suite runtime tests move to the post-live DayOA `17.0.0` / DYEC `20.0.0` release gate. Only static diff/catalog consistency checks are permitted for `19.0.52`. |
| `REL-001` | `PENDING` | Commit and push the clean candidate branch. |
| `REL-002` | `PENDING` | Create and push annotated non-`v` tag `19.0.52`; verify local and remote peel. |
| `OP-002` | `PENDING` | Merge the released tag into the operator branch only after conflict/dirty-path preflight; preserve all user-owned changes and verify the tag is an ancestor. |

## Release acceptance

The objective is complete only when all execution rows are terminal, tag
`19.0.52` resolves to a clean tested commit, both DayOA pin surfaces use
`16.0.34`, the adaptive export cannot delete S3 objects, and the operator
checkout contains the release without loss of its prior dirty state.

## 2026-08-30 validation amendment

The user changed the gate after implementation began: publish `19.0.52`
without running tests, use that release with DayOA `16.0.34` to build a new
cluster and complete three full Bjuice+Inflection packages, and only after all
three succeed run the complete suites and publish DayOA `17.0.0` and DYEC
`20.0.0`. Therefore `19.0.52` is an explicitly untested integration release;
its tag is not scientific acceptance evidence.
