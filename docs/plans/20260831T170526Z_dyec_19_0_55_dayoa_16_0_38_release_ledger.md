# DYEC 19.0.55 / DayOA 16.0.38 release ledger

## Objective

Integrate the already-reviewed crosswalk-complete Bjuice staging and solo Ganon
catalog work onto exact DYEC `19.0.54`, pin released DayOA `16.0.38` globally
and in every active catalog command, publish immutable DYEC `19.0.55`, update
the preserved operator checkout, and force-configure `clu-19052x` and
`bjuiceval-19024` when each headnode is safe to reconfigure.

## Boundaries

- Base: annotated DYEC `19.0.54`, peeled commit
  `03bdcb7f10e864410d2f96caf641e9811f0e36bf`.
- DayOA: annotated `16.0.38`, peeled commit
  `0313a535c4223ad5fecd6d77096270bdc8e30a0b`.
- Preserve all earlier numeric catalog snapshots and user-owned operator files.
- Run no test suite. Static release readback and tag/configuration receipts are
  permitted.
- Use only the public `dyec` CLI and exact state files for headnode
  configuration.
- Do not intervene in Slurm. A headnode with active jobs is not reconfigured
  until a fresh read-only check shows it idle.

## Control ledger

| ID | Requirement | Status | Evidence / terminal note |
|---|---|---|---|
| BASE-001 | Freeze exact DYEC and DayOA release identities. | SUCCESS | Exact annotated tags and peeled commits recorded above; `19.0.55` was absent before work. |
| SRC-001 | Integrate the existing feature commit on a clean branch from `19.0.54`. | SUCCESS | Feature `9a85ef91` was cherry-picked as `39bbf0d7` on `codex/dyec-19-0-55-dayoa-16-0-38-release`. |
| PIN-001 | Set `CURRENT_DYEC_BUILD` to `19.0.55`. | SUCCESS | Static source readback. |
| CAT-001 | Pin the active default and all 33 active commands to DayOA `16.0.38`. | SUCCESS | Static YAML readback: default `16.0.38`; 33/33 commands select only `16.0.38`. |
| CAT-002 | Append immutable `19.0.55` with the integrated command definitions; preserve `19.0.54`. | SUCCESS | New snapshot has 33 commands and only DayOA `16.0.38`; prior snapshot remains 33 commands and DayOA `16.0.36`. |
| CAT-003 | Keep source and packaged catalogs byte-identical. | SUCCESS | `cmp` readback succeeded. |
| REL-001 | Commit, push, annotate, and publish DYEC `19.0.55`. | IN_PROGRESS | Pending release commit and publication. |
| LOCAL-001 | Preserve/stash operator changes and update the operator checkout to exact `19.0.55`. | OPEN | Pending release tag. |
| HEAD-001 | Force-configure idle `clu-19052x` from its exact state file. | OPEN | Precheck at 2026-08-31T17:02Z showed zero controllers and zero jobs. |
| HEAD-002 | Force-configure `bjuiceval-19024` from its exact state file after it becomes idle. | BLOCKED | Precheck at 2026-08-31T17:02Z showed zero controllers but 17 Slurm jobs; no Slurm intervention is authorized. |

## Exact configuration authority

- `clu-19052x`:
  `/Users/jmajor/.config/daylily/state_clu-19052x_20260830223106.json`
- `bjuiceval-19024`:
  `/Users/jmajor/.config/daylily/state_bjuiceval-19024_20260824000053.json`

All rows terminal: no.

Objective complete: no.
