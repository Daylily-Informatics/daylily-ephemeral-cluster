# Historical Plans And Ledgers

Files in this directory are execution records or implementation plans. They are preserved for provenance and may contain stale command examples, old pins, failed live attempts, or superseded design assumptions.

Use the active operator docs in `README.md` and direct `docs/*.md` files for current DayEC behavior.
